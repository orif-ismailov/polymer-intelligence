---
phase: 06-acceptance-handover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/uv.lock
  - backend/pyproject.toml
  - backend/tests/test_prices_api.py
  - backend/tests/test_source_wizard.py
  - .github/workflows/ci.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "A committed uv.lock pins every backend dependency so the customer can rebuild deterministically"
    - "uv sync --frozen succeeds against the committed lock (no resolution drift)"
    - "The 2 previously-failing route-introspection tests pass"
    - "The full backend suite is green and CI installs from the frozen lock"
  artifacts:
    - path: "backend/uv.lock"
      provides: "Deterministic, fully-pinned backend dependency lock (D-07)"
      contains: "fastapi"
    - path: "backend/tests/test_prices_api.py"
      provides: "Starlette-1.x-compatible /api/v1/prices route-mount assertion"
      contains: "url_path_for"
    - path: "backend/tests/test_source_wizard.py"
      provides: "Starlette-1.x-compatible /api/v1/sources route-mount assertion"
      contains: "url_path_for"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "backend/uv.lock"
      via: "uv sync --frozen install step"
      pattern: "uv sync --frozen"
---

<objective>
Handover hygiene (D-07, folded todo): make the backend build deterministic and the
test suite fully green/reproducible. Commit a `backend/uv.lock`, add a FastAPI/Starlette
ceiling so route registration stays clean, modernise the 2 stale route-introspection tests
to a Starlette-1.x-compatible form, and switch CI to install from the frozen lock.

Purpose: "the system is documented and handed over" (Phase-6 goal / SC#4) requires a clean,
green, reproducible build the customer can stand up from the repo. This plan runs early and
independently so the green baseline is established before the executable proofs (06-02..06-05)
and docs (06-06..06-07) cite "CI green" as evidence.

Output: `backend/uv.lock`, an updated `backend/pyproject.toml` dependency ceiling, two
modernised tests, and a CI step that installs via `uv sync --frozen`.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `backend/uv.lock` — NEW committed lock file (deterministic backend rebuild)
- `backend/pyproject.toml` — MODIFIED (FastAPI/Starlette ceiling added)
- `backend/tests/test_prices_api.py` — MODIFIED (`test_prices_path_mounted` modernised)
- `backend/tests/test_source_wizard.py` — MODIFIED (`test_sources_router_mounted` modernised)
- `.github/workflows/ci.yml` — MODIFIED (uv-frozen install step)
</artifacts_produced>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/backend-dep-reproducibility-and-stale-route-tests.md
@.planning/phases/06-acceptance-handover/06-CONTEXT.md
@.planning/phases/06-acceptance-handover/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit uv.lock + pin a FastAPI/Starlette ceiling</name>
  <files>backend/uv.lock, backend/pyproject.toml</files>
  <read_first>
    - backend/pyproject.toml (current unbounded `fastapi>=0.111.0` dependency line + the existing dev pin comment pattern at lines 58-66)
    - .planning/todos/pending/backend-dep-reproducibility-and-stale-route-tests.md (the exact two failure modes: Starlette 1.3.1 route-introspection break vs older-FastAPI 204-response assertion)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the `backend/uv.lock` pattern section, lines 467-484)
  </read_first>
  <action>
    In `backend/pyproject.toml`, change the unbounded `fastapi>=0.111.0` entry to a bounded
    range pinned to the known-good line the app was developed against (a FastAPI 0.11x line
    whose Starlette pulls a `0.x` series, NOT Starlette 1.x), and add an explicit `starlette`
    ceiling (e.g. `starlette<0.42` / the version that ships with the chosen FastAPI) so a fresh
    resolve cannot pull Starlette 1.x. Match the existing dev-pin comment convention (a comment
    explaining why the ceiling exists and not to upgrade without re-verifying the suite). Then
    run `uv lock` inside `backend/` to generate `backend/uv.lock` from `pyproject.toml`, and
    commit the lock. Per D-07 / Claude's-Discretion, `uv` is the active tool (confirmed
    available, 0.11.2) so `uv.lock` is the pin mechanism — not `requirements-pinned.txt`. Do NOT
    bump ruff (0.15.17) / mypy (2.1.0) — preserve the reproducible lint/type gate exactly.
  </action>
  <verify>
    <automated>cd backend && uv lock --check && uv sync --frozen --extra dev 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `backend/uv.lock` exists and is tracked by git (`git ls-files backend/uv.lock` non-empty)
    - `cd backend && uv lock --check` exits 0 (lock is consistent with pyproject.toml — no drift)
    - `cd backend && uv sync --frozen --extra dev` exits 0 (frozen install succeeds)
    - `backend/uv.lock` contains a `fastapi` entry and a `starlette` entry resolved to the
      bounded (non-1.x) versions
    - `backend/pyproject.toml` no longer has an unbounded `fastapi>=0.111.0` line and now
      declares a `starlette` upper bound
  </acceptance_criteria>
  <done>uv.lock is committed, `uv lock --check` passes, and `uv sync --frozen` installs the bounded dependency set without resolution drift.</done>
</task>

<task type="auto">
  <name>Task 2: Modernise the 2 stale route-introspection tests + run full green suite + frozen CI install</name>
  <files>backend/tests/test_prices_api.py, backend/tests/test_source_wizard.py, .github/workflows/ci.yml</files>
  <read_first>
    - backend/tests/test_prices_api.py (the `test_prices_path_mounted` body at lines 213-221 — walks `app.routes` with `getattr(r, "path", "")`)
    - backend/tests/test_source_wizard.py (the `test_sources_router_mounted` body at lines 492-501 — same anti-pattern)
    - .github/workflows/ci.yml (the backend job "Install dependencies" step that runs `pip install -e ".[dev]"`)
    - .planning/todos/pending/backend-dep-reproducibility-and-stale-route-tests.md (recommended fix: use `client.app.url_path_for("endpoint_name")` or assert via TestClient responses instead of walking `app.routes`)
  </read_first>
  <action>
    Rewrite both mount assertions to be Starlette-version-agnostic. Replace the
    `[getattr(r, "path", "") for r in app.routes]` introspection with `app.url_path_for(...)`
    by the route's endpoint function name (preferred), OR assert the route resolves by making a
    real `TestClient` request and asserting a non-404 status. Keep each test asserting the same
    contract it asserted before (`/api/v1/prices*` exists; `/api/v1/sources*` exists). Then run
    the FULL backend suite and confirm it is green (the previously-failing two now pass; no
    regressions). Finally, update `.github/workflows/ci.yml` backend job: replace the
    `pip install -e ".[dev]"` install step with a `uv sync --frozen --extra dev` step (install
    uv via `astral-sh/setup-uv` or `pip install uv` first) so CI builds from the committed
    `uv.lock` for reproducibility, and run pytest/ruff/mypy through the uv environment.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_prices_api.py::TestPricesApiRoutes::test_prices_path_mounted tests/test_source_wizard.py::test_sources_router_mounted -x -q && pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_prices_api.py::TestPricesApiRoutes::test_prices_path_mounted` exits 0
    - `pytest tests/test_source_wizard.py::test_sources_router_mounted` exits 0
    - `cd backend && pytest -q` reports 0 failed (full suite green; skips allowed)
    - Neither modernised test body contains `getattr(r, "path"` over `app.routes`
    - `.github/workflows/ci.yml` backend job contains the string `uv sync --frozen`
    - `cd backend && ruff check .` exits 0 and `mypy app/services` / `mypy app/schemas` exit 0
  </acceptance_criteria>
  <done>Both route-mount tests pass under the pinned stack, the full backend suite is green, and CI installs from the frozen uv.lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → customer rebuild | The customer rebuilds the backend image from the committed repo; an unpinned dependency graph is the attack/instability surface |
| CI → registry (PyPI) | CI resolves and installs packages from PyPI at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | dependency resolution (unpinned `fastapi>=0.111.0`) | mitigate | Commit `uv.lock`; CI installs with `uv sync --frozen` so the resolved graph is byte-stable and cannot silently pull a compromised/incompatible transitive version |
| T-06-02 | Tampering | npm/pip/uv installs | mitigate | No NEW packages added; only a ceiling is tightened on already-audited `fastapi`/`starlette`. No `[ASSUMED]`/`[SUS]` packages introduced — `T-06-SC` register row remains satisfied with zero new install checkpoints |
| T-06-SC | Tampering | uv/pip installs | mitigate | This plan introduces no new third-party package — it bounds existing ones. No legitimacy checkpoint required because the package set is unchanged. |
</threat_model>

<verification>
- `backend/uv.lock` committed; `uv lock --check` and `uv sync --frozen --extra dev` both exit 0.
- Full backend suite green (`pytest -q` → 0 failed); the two named route-mount tests pass.
- `ruff check .` and the two scoped `mypy` gates exit 0 (reproducible lint/type gate preserved).
- CI `ci.yml` installs via `uv sync --frozen`.
</verification>

<success_criteria>
The backend rebuilds deterministically from a committed lock, the previously-failing
route-introspection tests pass, and CI is green and reproducible — satisfying the
"clean, green, reproducible build" component of the Phase-6 handover goal (SC#4).
</success_criteria>

<output>
Create `.planning/phases/06-acceptance-handover/06-01-SUMMARY.md` when done.
</output>
