---
phase: 04-dashboard-source-constructor
plan: 09
type: execute
wave: 6
depends_on: ["04-03", "04-05", "04-08"]
files_modified:
  - backend/tests/test_feed_performance.py
  - backend/tests/test_rbac_dashboard.py
  - .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md
autonomous: false
requirements: [REQ-live-feed, REQ-purchase-requests, REQ-price-trends, REQ-alerts, REQ-bot-team, REQ-source-builder]
user_setup: []

must_haves:
  truths:
    - "A performance test proves GET /feed stays ≤500 ms at ~1M simulated v_live_feed rows using keyset pagination (REQ-nfr-performance, TZ §5 NFR)"
    - "Role-based screen/action gating is verified at the API layer: viewer is rejected (403) from request PATCH and source/alert-rule writes; admin/analyst/trader allowed per matrix"
    - "An acceptance doc maps the 5 Phase-4 success criteria to deploy-time verification steps, honoring the SC#5 cross-phase caveat (website html_table/rss onboarding is end-to-end; telegram_channel is saved-pending only — its 'signals appear in feed' slice is a Phase-5/6 item)"
    - "The full backend suite is green and the dashboard typechecks/lints/builds before the verification gate"
  artifacts:
    - path: "backend/tests/test_feed_performance.py"
      provides: "≤500 ms feed performance test at ~1M rows (marker: performance)"
      contains: "performance"
    - path: "backend/tests/test_rbac_dashboard.py"
      provides: "role-gating matrix test for the new dashboard endpoints"
      contains: "viewer"
    - path: ".planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md"
      provides: "SC#1-SC#5 -> deploy-time drill mapping with SC#5 caveat"
      contains: "SC#5"
  key_links:
    - from: "backend/tests/test_feed_performance.py"
      to: "GET /feed keyset query"
      via: "EXPLAIN/timing at 1M rows"
      pattern: "v_live_feed"
    - from: "backend/tests/test_rbac_dashboard.py"
      to: "require_role guards"
      via: "viewer -> 403 on writes"
      pattern: "403"
---

<objective>
Close Phase 4 with the acceptance gate: a feed performance test proving ≤500 ms at ~1M rows
(REQ-nfr-performance / TZ §5), a role-based access-control matrix test for the new dashboard endpoints
(REQ-roles enforcement across the phase), and the `04-ACCEPTANCE.md` doc mapping the 5 Phase-4 success
criteria to a deploy-time live drill — honoring the SC#5 cross-phase caveat (website onboarding is
end-to-end now; telegram_channel is saved-pending, its feed slice lands Phase 5/6).

Purpose: phase verification readiness for all six requirements + the NFR performance bar + the
TZ §6.1.6 source-constructor acceptance. This is a checkpoint plan — it ends in a human-verify gate.
Output: performance test, RBAC matrix test, acceptance doc, human-verify checkpoint.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-source-constructor/04-CONTEXT.md
@.planning/phases/04-dashboard-source-constructor/04-RESEARCH.md
@.planning/phases/04-dashboard-source-constructor/04-VALIDATION.md
@.planning/phases/04-dashboard-source-constructor/04-01-SUMMARY.md
@.planning/phases/04-dashboard-source-constructor/04-06-SUMMARY.md
@.planning/phases/04-dashboard-source-constructor/04-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feed performance test (≤500 ms @ ~1M rows) + dashboard RBAC matrix test</name>
  <files>backend/tests/test_feed_performance.py, backend/tests/test_rbac_dashboard.py</files>
  <read_first>
    - backend/app/api/feed.py (from 04-01 — keyset query under test)
    - backend/tests/test_rbac.py (existing role-matrix test pattern: _make_staff_user, _auth_headers, role assertions)
    - backend/app/api/deps.py lines 107-144 (require_role/require_admin behavior: 403 for wrong role)
    - 04-RESEARCH.md Pattern 1 (keyset), Pitfall 2 (v_live_feed index), Assumption A6 (query planner index use)
    - 04-VALIDATION.md performance row (`pytest -m performance`, skip without Postgres) + sampling
    - backend/pyproject.toml [tool.pytest.ini_options] (markers — register `performance` if not present)
  </read_first>
  <action>
    Create `test_feed_performance.py` marked `@pytest.mark.performance`: seed ~1M v_live_feed-backing rows
    (bulk insert into signals, or use a synthetic table with the same shape) against a real Postgres
    (skip if no DB / not -m performance), then assert a keyset-paginated `GET /feed` page returns within
    500 ms and that EXPLAIN does NOT show a Seq Scan on the cursor path (Pitfall 2). Register the
    `performance` marker in pyproject.toml if missing so the marker doesn't warn.
    Create `test_rbac_dashboard.py` (analog test_rbac.py): assert the role matrix for the new endpoints —
    viewer -> 403 on PATCH /requests/{id}, POST /sources, PATCH /sources/{id}, POST/PATCH /alert-rules;
    admin allowed on all; analyst/trader allowed on PATCH /requests per require_role(admin,analyst,trader);
    all-staff reads (GET /feed, /requests, /alerts, /prices/series) allowed for viewer.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_rbac_dashboard.py -x -q && pytest tests/test_feed_performance.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest tests/test_rbac_dashboard.py -x` exits 0 (viewer 403 on writes; reads allowed; analyst/trader/admin matrix).
    - Source: `test_feed_performance.py` is marked `performance`, asserts a ≤500 ms (0.5s) bound, and checks for absence of Seq Scan on the keyset path.
    - Behavior: `pytest tests/test_feed_performance.py --collect-only` exits 0 (test collects; runs under `-m performance` with a DB).
    - Test: `cd backend && pytest tests/ -q` (full suite) passes; `cd dashboard && npm run typecheck && npm run lint && npm run build` all green (regression gate before checkpoint).
  </acceptance_criteria>
  <done>Performance test (≤500 ms @ 1M) + RBAC matrix test created; full backend suite + dashboard build green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-4 acceptance doc (SC#1-SC#5 -> deploy-time drill, with SC#5 caveat)</name>
  <files>.planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 4 section (goal + 5 success criteria)
    - 04-CONTEXT.md <domain> cross-phase boundary note (SC#5 telegram slice is Phase 5/6)
    - docs/polymer-intelligence-tz.md §6.1.6 (source-constructor acceptance: admin onboards site + channel, failed-test source cannot be enabled)
    - .planning/STATE.md Deferred Items (Phase-2 / Phase-3 deploy-time UAT deferral precedent + format)
    - 04-VALIDATION.md Manual-Only Verifications table
  </read_first>
  <action>
    Write `04-ACCEPTANCE.md` mapping each of the 5 Phase-4 success criteria to concrete deploy-time
    verification steps and the automated proxy that gates CI now:
    SC#1 feed + filters + SSE refresh + ≤500 ms (proxy: test_feed_api.py, test_feed_sse.py, test_feed_performance.py -m performance);
    SC#2 request detail + status/assign/note + audit_log (proxy: test_dashboard_requests.py incl. test_audit_trail);
    SC#3 price chart + source health enable/disable (proxy: test_prices_api.py, test_source_wizard.py, test_source_health.py);
    SC#4 alert rule -> delivery via deliveries queue + rate limits (proxy: test_alert_service.py::test_delivery_dispatch; live delivery is deploy-time with a real chat_id);
    SC#5 admin onboards a public website (html_table/rss) end-to-end AND saves a telegram_channel in pending —
    EXPLICITLY note the SC#5 caveat: "telegram-channel signals appear in feed" is satisfied in Phase 5/6
    (userbot + llm extraction land then); Phase 4 delivers website onboarding end-to-end + pending channel save
    (proxy: test_source_wizard.py enable-gate + pending-source).
    Include a deploy-time live drill checklist and register it as a deferred deploy-time UAT item consistent
    with the Phase-2/Phase-3 precedent in STATE.md.
  </action>
  <verify>
    <automated>grep -q "SC#5" .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md && grep -qi "Phase 5" .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md && grep -qi "telegram_channel" .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `04-ACCEPTANCE.md` contains a row/section for each of SC#1-SC#5 with its automated proxy command and deploy-time step.
    - Source: the SC#5 entry explicitly states the telegram-channel "signals appear in feed" slice is a Phase-5/6 acceptance item (caveat honored).
    - Source: each SC maps to a named test command from 04-VALIDATION.md.
  </acceptance_criteria>
  <done>Acceptance doc maps all 5 SCs to proxies + deploy-time steps with the SC#5 caveat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase-4 dashboard + source constructor: live feed (SSE), flagship Purchase Requests
    master-detail with team actions + audit, price trends, alerts rules + team delivery, sources health,
    and the no-code add-source wizard. Automated gates are green: backend suite passes, dashboard
    typechecks/lints/builds, RBAC matrix + alert interpreter (90%+) + enable-gate + audit-trail tests pass,
    and the ≤500 ms feed performance test is ready to run under `-m performance` with Postgres.
  </what-built>
  <how-to-verify>
    1. Bring up the stack (docker compose) and log in to the dashboard as admin.
    2. Live feed: open `/`, confirm rows render and a new signal appears without reload (SSE; or within 30 s polling fallback).
    3. Requests: open a request detail, change status, assign an owner, add a note, click Contact Buyer — confirm each reflects and that audit_log has a row per action.
    4. Prices: open `/prices`, confirm the chart renders for a product/market with the date-range + currency controls.
    5. Alerts: create a rule with a real Telegram chat_id, fire a matching signal, confirm delivery to the chat within rate limits.
    6. Sources (TZ §6.1.6): add a public website via the wizard (html_table or rss) -> Run Test -> see ≤10 preview rows -> Enable; confirm a source with NO passing test cannot be enabled; add a telegram_channel and confirm it saves in "Pending activation (Phase 5)" state.
    7. Run `cd backend && pytest tests/test_feed_performance.py -m performance` against Postgres and confirm ≤500 ms.
    8. Confirm the SC#5 caveat: telegram-channel signals appearing in the feed is expected in Phase 5/6, not now.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which SC failed and how).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All dashboard endpoints (cross-phase) | The RBAC matrix test re-verifies the role boundary across every new endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33 | Elevation | all dashboard write endpoints | mitigate | test_rbac_dashboard.py asserts viewer -> 403 on every write and read allowance per role matrix (re-verifies require_role across the phase) |
| T-04-34 | Denial of Service | feed at scale | mitigate | test_feed_performance.py proves keyset query ≤500 ms at ~1M rows + no Seq Scan (REQ-nfr-performance) |
| T-04-35 | Repudiation | acceptance evidence | mitigate | 04-ACCEPTANCE.md records the proxy + deploy-time drill so SC pass/fail is auditable |
| T-04-SC | Tampering | pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && pytest tests/ -q` full suite green; `pytest tests/test_feed_performance.py -m performance` ≤500 ms with Postgres.
- `cd dashboard && npm run typecheck && npm run lint && npm run build` green.
- Human-verify checkpoint covers the live SC#1-SC#5 drill with the SC#5 caveat.
</verification>

<success_criteria>
- Performance test proves ≤500 ms @ 1M rows; RBAC matrix enforced; acceptance doc maps all 5 SCs.
- Human verifies the end-to-end dashboard + website source onboarding (TZ §6.1.6) and the SC#5 caveat.
</success_criteria>

## Artifacts this phase produces (Plan 09)

- New test files: `backend/tests/test_feed_performance.py`, `backend/tests/test_rbac_dashboard.py`
- Acceptance doc: `.planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md`
- pyproject.toml: `performance` pytest marker registration (if missing)

<output>
Create `.planning/phases/04-dashboard-source-constructor/04-09-SUMMARY.md` when done.
</output>
