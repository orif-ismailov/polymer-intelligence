# Sourcing domain: the AI broker waterfall for a buyer request —
# inventory (1) -> partner suppliers (2) -> marketplace offers (3) -> import — plus market intel.
# Unrelated to app/domains/signals: it never touches Source or RawItem.
