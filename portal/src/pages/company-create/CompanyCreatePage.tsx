import { CompanyWizard } from "@/features/company-wizard";

/** Company registration wizard page. Orchestration only — the wizard owns state. */
export function CompanyCreatePage() {
  return <CompanyWizard />;
}
