---
phase: 04-dashboard-source-constructor
plan: 08
type: execute
wave: 5
depends_on: ["04-05", "04-06", "04-07"]
files_modified:
  - dashboard/components/sources/SourcesList.tsx
  - dashboard/components/sources/AddSourceWizard.tsx
  - dashboard/components/sources/JsonSchemaForm.tsx
  - dashboard/components/alerts/AlertFeed.tsx
  - dashboard/components/alerts/RuleBuilder.tsx
  - dashboard/components/prices/PriceChart.tsx
  - dashboard/app/(dashboard)/sources/page.tsx
  - dashboard/app/(dashboard)/alerts/page.tsx
  - dashboard/app/(dashboard)/prices/page.tsx
  - dashboard/app/(dashboard)/admin/users/page.tsx
autonomous: true
requirements: [REQ-source-builder, REQ-alerts, REQ-price-trends, REQ-sources-health]
user_setup: []

must_haves:
  truths:
    - "The add-source wizard auto-builds its config form from GET /admin/source-types config_schema (JsonSchemaForm), runs a real Test (≤10 normalized preview rows D-06), and cannot enable until the test passes (D-04); telegram_channel/llm_page are selectable but pre-stage to pending with a 'Phase 5' badge (D-05)"
    - "The sources list shows health (last fetch, consecutive failures), enable/disable toggle + Test, and a 'Pending activation (Phase 5)' badge with disabled controls for pending types (D-05, REQ-sources-health)"
    - "The alert rules builder exposes the full predicate set with lead_score_gte labeled 'Activates with Phase 5 AI' (visible+disabled) and per-rule chat_id delivery targets (D-07, D-08); the alert feed lists triggered alerts"
    - "The Price Trends page renders a Recharts LineChart from GET /prices/series with product/market/date-range/currency controls (REQ-price-trends)"
    - "JsonSchemaForm unwraps Pydantic anyOf:[T,null] for Optional fields and mirrors required[] client-side (RESEARCH Pitfall 4)"
  artifacts:
    - path: "dashboard/components/sources/JsonSchemaForm.tsx"
      provides: "auto-form renderer from Pydantic JSON schema (anyOf unwrap)"
      contains: "anyOf"
    - path: "dashboard/components/sources/AddSourceWizard.tsx"
      provides: "4-step wizard (pick/configure/test/enable)"
      contains: "Run Test"
    - path: "dashboard/components/alerts/RuleBuilder.tsx"
      provides: "full predicate rule form (lead_score disabled + per-rule chat_id)"
      contains: "Activates with Phase 5 AI"
    - path: "dashboard/components/prices/PriceChart.tsx"
      provides: "Recharts LineChart from /prices/series"
      contains: "LineChart"
  key_links:
    - from: "dashboard/components/sources/AddSourceWizard.tsx"
      to: "/api/v1/admin/source-types"
      via: "fetch config_schema -> JsonSchemaForm"
      pattern: "source-types"
    - from: "dashboard/components/sources/AddSourceWizard.tsx"
      to: "/api/v1/sources/{id}/test"
      via: "Run Test -> preview rows"
      pattern: "/test"
    - from: "dashboard/components/prices/PriceChart.tsx"
      to: "/api/v1/prices/series"
      via: "useQuery -> LineChart"
      pattern: "prices/series"
---

<objective>
Build the remaining Surface-B feature screens (REQ-source-builder, REQ-alerts, REQ-price-trends,
REQ-sources-health): the Sources list + the no-code Add Source wizard (auto-form from config_schema via
JsonSchemaForm, real Test with ≤10-row preview, enable-on-pass; pending types pre-stageable with Phase-5
badge), the Alerts feed + rules builder (full predicate set, lead_score disabled label, per-rule chat_ids),
the Price Trends Recharts chart, and the admin users screen.

Purpose: REQ-source-builder (FR-22 / TZ §6.1.6), REQ-alerts (FR-14), REQ-price-trends (FR-12),
REQ-sources-health (FR-13). D-04/D-05/D-06 wizard contract, D-07/D-08 rule contract.
Output: sources/alerts/prices/admin-users components + pages.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-source-constructor/04-CONTEXT.md
@.planning/phases/04-dashboard-source-constructor/04-RESEARCH.md
@.planning/phases/04-dashboard-source-constructor/04-PATTERNS.md
@.planning/phases/04-dashboard-source-constructor/04-UI-SPEC.md
@.planning/phases/04-dashboard-source-constructor/04-06-SUMMARY.md
@.planning/phases/04-dashboard-source-constructor/04-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: JsonSchemaForm + AddSourceWizard + SourcesList + sources page</name>
  <files>dashboard/components/sources/JsonSchemaForm.tsx, dashboard/components/sources/AddSourceWizard.tsx, dashboard/components/sources/SourcesList.tsx, dashboard/app/(dashboard)/sources/page.tsx</files>
  <read_first>
    - 04-RESEARCH.md Pattern 4 (JsonSchemaForm renderer: string/integer/enum/uri field mapping, required[] mirror), Pitfall 4 (anyOf:[T,null] unwrap for Optional)
    - 04-UI-SPEC.md Sources section (list columns, pending badge bg-amber-500/20 text-amber-400, disabled Test/Enable for pending) + Add Source wizard (4 steps, type radio cards 2x2 with Phase 5 badges, config auto-form, Test step ≤10-row preview columns Product/Grade/Volume/Price/Currency/Section/Event At, Enable/Save-as-Pending) + Copywriting (test loading/success/failure copy)
    - 04-PATTERNS.md JsonSchemaForm "No Analog" row (lines 759) + frontend token patterns
    - dashboard/lib/api.ts (apiFetch) + dashboard/components/ui (dialog, select, button, table from 04-02)
    - backend/app/schemas/dashboard.py SourceHealthItem/SourceCreate/SourceTestOut (from 04-06)
  </read_first>
  <action>
    Create `JsonSchemaForm` per RESEARCH Pattern 4: render fields from a Pydantic v2 JSON schema's
    `properties` — text/url (SSRF hint "Public URLs only."), integer/number, enum->Select; unwrap
    `anyOf:[T,null]` to T for Optional fields (Pitfall 4 `resolveType`); mark required from `required[]` and
    mirror required checks client-side. No hardcoded hex.
    Create `AddSourceWizard` (shadcn Dialog, centered, max-w 560px, numbered step indicator): Step 1 pick type
    (2x2 radio cards: HTML Table, RSS Feed, Telegram Channel [Phase 5 badge], LLM Page [Phase 5 badge] —
    Phase-5 types selectable but warn, D-05); Step 2 configure (JsonSchemaForm from
    `GET /admin/source-types`); Step 3 Test (html_table/rss only): "Run Test" -> POST /sources then
    POST /sources/{id}/test -> render ≤10-row preview (D-06 columns) or failure banner; telegram_channel/llm_page
    skip Test and save to pending; Step 4 Enable ("Enable Source" only if test passed) / "Save as Pending".
    Create `SourcesList`: rows with Name, Type badge, Status chip (enabled/disabled; pending types ->
    "Pending activation (Phase 5)" amber badge with disabled Test/Enable D-05), Last Fetch (Asia/Tashkent),
    Consecutive Failures (urgency-high red if >0), Enable/Disable toggle (PATCH /sources/{id}), Test button.
    Create `app/(dashboard)/sources/page.tsx`: header + "Add Source" button (opens wizard) + SourcesList +
    empty state ("No sources configured" / "Add your first data source to start collecting signals.").
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `JsonSchemaForm.tsx` contains `anyOf` (Optional unwrap) and reads `required` from the schema.
    - Source: `AddSourceWizard.tsx` fetches `source-types`, calls `/test`, and renders a ≤10-row preview; Phase-5 types route to a pending/"Save as Pending" path.
    - Source: `SourcesList.tsx` renders the "Pending activation (Phase 5)" badge and disables Test/Enable for pending types.
    - Source: no hardcoded hex in sources components (`grep -rE "#[0-9a-fA-F]{6}" dashboard/components/sources/` returns nothing).
    - Test: `cd dashboard && npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Wizard auto-form + real Test + enable-gate UI + pending pre-staging + sources list render; typecheck+lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Alerts feed + RuleBuilder + alerts page</name>
  <files>dashboard/components/alerts/AlertFeed.tsx, dashboard/components/alerts/RuleBuilder.tsx, dashboard/app/(dashboard)/alerts/page.tsx</files>
  <read_first>
    - 04-UI-SPEC.md Alerts section (alert feed table + severity chips; rules builder fields: name, kind multiselect, product, volume>=, urgency checkboxes, source kind, Lead Score>= labeled "Activates with Phase 5 AI" disabled, delivery chat_id textarea D-08, urgency channel DM/Group, Save Rule) + Copywriting (delete-rule destructive confirm)
    - 04-CONTEXT.md D-07 (full predicate set; lead_score_gte labeled+disabled), D-08 (per-rule chat_ids)
    - backend/app/schemas/dashboard.py AlertRuleCreate/AlertRuleOut/AlertOut (from 04-07)
    - dashboard/lib/api.ts + dashboard/components/ui (select, input, alert-dialog, badge from 04-02)
  </read_first>
  <action>
    Create `AlertFeed`: table of triggered alerts (message, severity chip info=blue/warning=amber/critical=red,
    source, triggered-at Asia/Tashkent, rule name) from `GET /alerts`; empty state ("No alerts triggered yet" /
    "Configure a rule to start receiving alerts.").
    Create `RuleBuilder`: form with name, kind multiselect (buy_request/sell_offer/deal/price_quote), product
    select, volume>= number, urgency checkbox group, source-kind multiselect, Lead Score>= number rendered
    DISABLED with the amber label "Activates with Phase 5 AI" + tooltip "Available after Phase 5" (D-07 — visible
    but inert), delivery chat_id textarea (one per line, help text per UI-SPEC, D-08), urgency channel DM/Group.
    Save -> POST /alert-rules (condition built from the predicate fields, channels from chat_ids); existing rules
    list with predicate summary + delete (AlertDialog confirm). Use useMutation + invalidate.
    Create `app/(dashboard)/alerts/page.tsx`: AlertFeed + "Create Rule" button -> RuleBuilder (panel/inline).
    Role-gate write actions: viewer cannot create/edit rules (UI hides; backend require_admin is the boundary).
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `RuleBuilder.tsx` contains "Activates with Phase 5 AI" and renders the Lead Score input as disabled (D-07).
    - Source: `RuleBuilder.tsx` has a per-rule chat_id textarea (D-08) and posts to `alert-rules`.
    - Source: `AlertFeed.tsx` reads `/alerts` and renders severity chips with no hardcoded hex (`grep -rE "#[0-9a-fA-F]{6}" dashboard/components/alerts/` returns nothing).
    - Test: `cd dashboard && npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Alert feed + rule builder (full predicate set, lead_score disabled, per-rule chat_ids) render; typecheck+lint green.</done>
</task>

<task type="auto">
  <name>Task 3: PriceChart + prices page + admin users page</name>
  <files>dashboard/components/prices/PriceChart.tsx, dashboard/app/(dashboard)/prices/page.tsx, dashboard/app/(dashboard)/admin/users/page.tsx</files>
  <read_first>
    - 04-PATTERNS.md PriceChart section (lines 655-671): Recharts SERIES_COLORS hex-in-stroke-only exception (comment required)
    - 04-UI-SPEC.md Price Trends section (full-page LineChart 100%x400px, product pill tabs, market select, date presets 7d/30d/90d + custom range, currency toggle USD/UZS via fx_rates, empty state) + chart line colors + Admin Users section (table, role badges, admin-only)
    - backend/app/schemas/dashboard.py PriceSeriesOut + StaffUserItem (from 04-07 / 04-04)
    - dashboard/package.json (recharts v2 — use LineChart, ResponsiveContainer, XAxis, YAxis, Tooltip, Legend, CartesianGrid)
    - dashboard/lib/api.ts
  </read_first>
  <action>
    Create `PriceChart` (`"use client"`): Recharts `LineChart` in `ResponsiveContainer` (100% x 400px) fed by
    `useQuery(['prices', filters], () => apiFetch('/prices/series?...'))`. Multi-series stroke colors via the
    SERIES_COLORS hex map (the ONLY allowed hex — Recharts stroke props don't accept CSS vars; add the required
    explanatory comment). Grid stroke uses the border-subtle value; axis labels 12px foreground-muted.
    Create `app/(dashboard)/prices/page.tsx`: product pill tabs (PP Raffia/HDPE/LDPE/LLDPE/PVC/PET/PS/ABS),
    market select (UZEX/CBU/All), date-range presets + custom picker, currency toggle USD/UZS (on-read fx
    conversion), PriceChart, empty state ("No price data for the selected filters. Adjust the date range or
    product selection.").
    Create `app/(dashboard)/admin/users/page.tsx`: admin-only table from `GET /admin/users` (name, email, role
    badge admin=accent/analyst=blue/trader=amber/viewer=muted, created at); no user creation (out of scope).
    Hide the screen/redirect for non-admin (UI; backend require_admin is the boundary).
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `PriceChart.tsx` contains `LineChart`, reads `/prices/series`, and the only hex values are in Recharts stroke props with the explanatory comment present.
    - Source: `prices/page.tsx` has product pill tabs, market select, date-range controls, and a USD/UZS currency toggle.
    - Source: `admin/users/page.tsx` reads `/admin/users`, renders role badges, and does not display password_hash.
    - Test: `cd dashboard && npm run typecheck` exits 0, `npm run lint` exits 0, `npm run build` completes.
  </acceptance_criteria>
  <done>Price chart + prices page + admin users render; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → wizard / rule builder | Admin-supplied config + rule predicates cross to backend |
| Browser → /prices/series, /alerts, /admin/users | Reads cross to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-29 | Tampering (XSS) | preview rows, alert messages, user emails | mitigate | React text rendering (escaped); no dangerouslySetInnerHTML; preview rows shown as table text |
| T-04-30 | Elevation | wizard/rule-builder/admin-users write actions | mitigate | UI hides write actions for non-admin/viewer; backend require_admin/require_role is the enforced boundary (04-06/04-07/04-04) |
| T-04-31 | Tampering (enable bypass) | wizard Enable button | mitigate | UI disables Enable until test passes; backend PATCH /sources enforces last_test_ok_at invariant (04-06) — UI is UX-only |
| T-04-32 | Info Disclosure | admin users screen | mitigate | Reads /admin/users which excludes password_hash (04-04); screen admin-only |
| T-04-SC | Tampering | npm installs | accept | No new packages (recharts/shadcn already installed) |
</threat_model>

<verification>
- `cd dashboard && npm run typecheck && npm run lint && npm run build` green.
- Manual (deferred to 04-09 / deploy): wizard auto-form + Test preview + enable, rule builder save, chart render, dark-theme smoke.
</verification>

<success_criteria>
- Add-source wizard auto-form + real Test + enable-gate UI + pending pre-staging work end-to-end.
- Alert feed + rule builder (full predicate set, lead_score disabled, per-rule chat_ids) render.
- Price chart + admin users render; build green.
</success_criteria>

## Artifacts this phase produces (Plan 08)

- Components: `JsonSchemaForm`, `AddSourceWizard`, `SourcesList`, `AlertFeed`, `RuleBuilder`, `PriceChart`
- Pages: `app/(dashboard)/sources/page.tsx`, `alerts/page.tsx`, `prices/page.tsx`, `admin/users/page.tsx`

<output>
Create `.planning/phases/04-dashboard-source-constructor/04-08-SUMMARY.md` when done.
</output>
