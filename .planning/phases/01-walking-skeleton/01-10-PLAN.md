---
phase: 01-walking-skeleton
plan: 10
type: gap_closure
gap_closure: true
wave: 2
depends_on: ["01-09"]
files_modified:
  - backend/pyproject.toml
  - backend/app/main.py
  - backend/app/entrypoint.py
  - backend/app/core/config.py
  - backend/app/core/security.py
  - backend/app/core/db.py
  - backend/app/api/auth.py
  - backend/app/api/deps.py
  - backend/app/api/health.py
  - backend/app/models/enums.py
  - backend/app/services/auth_service.py
  - backend/app/seed/seed_staff.py
  - backend/app/seed/seed_reference.py
  - backend/tests/conftest.py
  - backend/tests/test_config.py
  - backend/tests/test_jwt.py
  - backend/tests/test_rbac.py
  - backend/tests/test_audit.py
  - backend/tests/test_time.py
  - backend/tests/test_migration.py
  - backend/tests/test_auth_login.py
autonomous: true
requirements: ["REQ-nfr-observability", "REQ-nfr-security"]
must_haves:
  truths:
    - "`ruff check .` run from backend/ (select=[E,F,I,N,UP,B,SIM], ignore E501) exits 0 — the 124 violations the UAT found are resolved with behavior-preserving edits and narrowly-scoped per-rule ignores where a rewrite would change runtime behavior (SC#5)"
    - "`mypy app/services --ignore-missing-imports` and `mypy app/schemas --ignore-missing-imports` (the CI commands) exit 0 (SC#5)"
    - "ruff and mypy are pinned to known-good versions in backend/pyproject.toml [dev] so the lint/type gates are reproducible and do not drift with new tool releases (SC#5)"
    - "The pytest suite is still 100 passed / 17 skipped — every lint fix is behavior-preserving (SC#5)"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Pinned ruff + mypy in [dev]; any narrowly-scoped per-rule lint ignores justified in comments"
      contains: "ruff=="
    - path: "backend/app/models/enums.py"
      provides: "14 str-enums that still serialize to their string values (UP042 resolved without changing PostgreSQL ENUM serialization)"
      contains: "enum"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "backend/pyproject.toml"
      via: "ruff check . and mypy run the pinned versions from [dev]"
      pattern: "ruff check"
    - from: "backend/pyproject.toml"
      to: "backend/app"
      via: "ruff lint select rules now satisfied across the backend source"
      pattern: "select = \\[\"E\", \"F\", \"I\", \"N\", \"UP\", \"B\", \"SIM\"\\]"
---

<objective>
Close UAT Gap 2 / SC#5 (blocker): "CI (ruff, mypy, eslint+tsc, tests, image build) passes green". The backend source was never actually linted — `ruff check .` from backend/ (the CI command, select=[E,F,I,N,UP,B,SIM], ignore E501) reports 124 violations: I001 unsorted-imports ×32, UP042 replace-str-enum ×14, F401 unused-import ×12, UP017 datetime-timezone-utc ×12, SIM117 ×10, B008 function-call-in-default-argument ×8, B017/B904 ×7, N806 ×5, plus others. mypy was never installed locally so the type gate is also unverified. ruff is pinned `>=0.4.0` (no upper bound) so the gate drifts with each ruff release.

Purpose: CI is the quality backbone of the walking skeleton. A truly green ruff + mypy gate, pinned for reproducibility, is required so every later phase builds on a lint-clean, type-checked scaffold. This is the last blocker between Phase 1 and "done".
Output: `ruff check .` exits 0, `mypy app/services` and `mypy app/schemas` exit 0, ruff+mypy are version-pinned, and the pytest suite stays 100 passed / 17 skipped.

CRITICAL — behavior-preserving constraint: This plan touches a broad backend file set. EVERY edit must preserve runtime behavior. The existing pytest suite (100 passed / 17 skipped) is the regression oracle and MUST stay green after every step. Where a ruff-suggested rewrite would change behavior (notably UP042 str-enum serialization and B008 FastAPI `Depends()` defaults), use a narrowly-scoped per-rule ignore instead of a risky mechanical rewrite, and document each ignore.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-UAT.md
@backend/pyproject.toml
@backend/app/models/enums.py
@backend/app/api/deps.py
</context>

<artifacts_this_phase_produces>
New symbols / structures introduced by this gap-closure plan (existing edited items not re-listed):
- Pinned `ruff==<known-good>` and `mypy==<known-good>` (plus the already-listed type stubs) in `backend/pyproject.toml` `[project.optional-dependencies].dev`, replacing the unbounded `ruff>=0.4.0` / `mypy>=1.10.0`.
- Optionally a `[tool.ruff.lint.per-file-ignores]` table (and/or `[tool.ruff.lint]` extend-ignore) in `backend/pyproject.toml` for the two rules whose autofix would change behavior — scoped to the exact files/rules (e.g. `B008` for FastAPI endpoint modules, `UP042` for `app/models/enums.py` if StrEnum conversion changes serialization). Each ignore carries an inline justification comment.
- Behavior-preserving source edits across the backend (import sorting, removed unused imports, `datetime.now(tz=...)`/`datetime.UTC` modernization, combined `with` statements, `raise ... from`, renamed N806 locals) — no net-new runtime symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Autofix the safe ruff violations, then hand-fix the behavior-sensitive ones (keeping pytest green)</name>
  <files>backend/app/main.py, backend/app/entrypoint.py, backend/app/core/config.py, backend/app/core/security.py, backend/app/core/db.py, backend/app/api/auth.py, backend/app/api/deps.py, backend/app/api/health.py, backend/app/models/enums.py, backend/app/services/auth_service.py, backend/app/seed/seed_staff.py, backend/app/seed/seed_reference.py, backend/tests/conftest.py, backend/tests/test_config.py, backend/tests/test_jwt.py, backend/tests/test_rbac.py, backend/tests/test_audit.py, backend/tests/test_time.py, backend/tests/test_migration.py, backend/tests/test_auth_login.py, backend/pyproject.toml</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-UAT.md (Gap 2 — the exact violation counts: I001 ×32, UP042 ×14, F401 ×12, UP017 ×12, SIM117 ×10, B008 ×8, B017/B904 ×7, N806 ×5; ~68 are autofixable, the rest are manual)
    - backend/pyproject.toml ([tool.ruff] line-length 100, target-version py312; [tool.ruff.lint] select=["E","F","I","N","UP","B","SIM"], ignore=["E501"] — the exact rule set CI enforces; [project.optional-dependencies].dev currently `ruff>=0.4.0`, `mypy>=1.10.0`)
    - backend/app/models/enums.py (14 `class X(str, enum.Enum)` definitions — these are the 14 UP042 hits. CAUTION: converting `(str, enum.Enum)` → `enum.StrEnum` changes `str(member)`/f-string output from `"ClassName.member"` to the bare value `"member"`. These enums back PostgreSQL ENUM types via SQLAlchemy and are exercised by test_migration.py / test_rbac.py / seed modules — a mechanical switch can change serialization. Treat UP042 as behavior-sensitive: convert ONLY if the full suite stays green, else scope-ignore.)
    - backend/app/api/deps.py (FastAPI `Depends()` in parameter defaults at lines 32-33, 127 — these are the B008 hits. The `= Depends(...)` default is REQUIRED by FastAPI's DI; rewriting it breaks the framework. B008 must be scope-ignored for endpoint/dependency modules, not "fixed". Also note `except JWTError: raise HTTPException(...)` at lines 59-64 and 78-83 — these are B904 hits needing `from exc` or `from None`.)
  </read_first>
  <action>
    Work from the `backend/` directory so paths match CI. Use the pinned ruff once Task 2 lands — but for THIS task, install the tooling first: `cd backend && pip install -e ".[dev]"` (or `pip install ruff mypy` matching the pins Task 2 will set) so `ruff` is runnable. Then:

    1. AUTOFIX the safe rules: `ruff check . --fix` (resolves the ~68 autofixable: I001 import sorting, F401 unused imports, UP017 `datetime.timezone.utc` → `datetime.UTC`, many SIM117 nested-`with` merges, etc.). Re-run `ruff check .` to see the residual.

    2. After autofix, run `python -m pytest tests -q` and confirm still 100 passed / 17 skipped. If F401 autofix removed an import that was actually used for a side effect or re-export, restore it with an explicit `# noqa: F401` and a comment (e.g. package `__init__` re-exports). Do not let autofix change behavior.

    3. HAND-FIX the non-autofixable, behavior-PRESERVING rules:
       - B904 (`raise ... from`): in `app/api/deps.py` (the `except JWTError: raise HTTPException(...)` blocks ~lines 59-64, 78-83) and any other `raise` inside an `except` flagged by ruff, add `from exc` (capturing the exception as `exc`) or `from None` where chaining is intentionally suppressed. This is behavior-preserving (only sets `__cause__`).
       - B017 (assertRaises/`pytest.raises(Exception)` too broad): in the flagged test(s), narrow `pytest.raises(Exception)` to the specific exception the code raises (e.g. `pytest.raises(ValidationError)` / `pytest.raises(ValueError)`), matching what the code under test actually raises so the test still passes.
       - N806 (non-lowercase local variable) ×5: rename the offending local variables to snake_case at their definition and all in-scope uses. Pure rename — confirm tests still pass. If a name is a deliberate constant-like local that ruff misflags, scope-ignore N806 for that exact line with `# noqa: N806` + comment rather than a rename that hurts readability.
       - Residual SIM117 not autofixed: combine adjacent `with A: with B:` into `with A, B:` only where semantically identical (same suite, no intervening code). Skip any the autofixer left because they are not safe to merge.
       - Any residual UP017 / I001 that autofix did not resolve: apply by hand.

    4. HANDLE the two behavior-SENSITIVE rules deliberately:
       - UP042 (replace-str-enum) ×14 in `app/models/enums.py`: FIRST try converting all 14 `class X(str, enum.Enum)` to `class X(enum.StrEnum)` and run the FULL suite (`python -m pytest tests -q`). If it stays 100 passed / 17 skipped (StrEnum serializes to the bare value, which matches the SQLAlchemy/Postgres ENUM value mapping), keep the conversion. If ANY test regresses (serialization/format change), REVERT the enum changes and instead add a per-file ignore: in `pyproject.toml` add `[tool.ruff.lint.per-file-ignores]` with `"app/models/enums.py" = ["UP042"]` and an inline comment: "UP042 ignored — these enums back PostgreSQL ENUM types; StrEnum changes str()/f-string serialization and would alter stored/serialized values." Pick whichever keeps the suite green; record the choice in the SUMMARY.
       - B008 (function-call-in-default-argument) ×8: these are FastAPI `= Depends(...)` / `= Query(...)` defaults, which the framework REQUIRES. Do NOT rewrite them. Add a scoped ignore in `pyproject.toml`: either `[tool.ruff.lint.per-file-ignores]` entries for the endpoint/dependency modules (`app/api/deps.py`, `app/api/auth.py`, `app/main.py`, `app/seed/*.py`, `app/api/health.py` — exactly the files the read_first grep listed) with `["B008"]`, OR a project-level `[tool.ruff.lint.flake8-bugbear] extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.Body", "fastapi.Path", "fastapi.Form", "fastapi.File"]` which is the canonical FastAPI-aware way to silence B008 only for those DI calls (PREFER this — it is narrowly scoped to the FastAPI DI markers, not a blanket B008 disable). Add a comment explaining the FastAPI DI rationale.

    5. Re-run `ruff check .` from backend/ — it must exit 0. Re-run `python -m pytest tests -q` — it must be 100 passed / 17 skipped. Iterate until both hold. Do NOT add a blanket `# ruff: noqa` or remove rules from `select`; every suppression must be a specific rule scoped to specific files/lines with a justification.
  </action>
  <verify>
    <automated>cd /Users/kholmumin/WebstormProjects/polymer-intelligence/backend && ruff check . && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && ruff check .` exits 0 with no reported violations (select=[E,F,I,N,UP,B,SIM], ignore=[E501] from pyproject — unchanged rule set).
    - The `[tool.ruff.lint] select` list is unchanged (no rule removed from the gate to "pass"); E501 remains the only top-level ignore. Any added suppressions are `per-file-ignores`, `extend-immutable-calls`, or line-level `# noqa: <RULE>` with a justification comment — never a blanket disable.
    - `app/models/enums.py` either uses `enum.StrEnum` (if the suite stayed green) OR retains `(str, enum.Enum)` with a scoped `UP042` per-file-ignore — and either way the full suite passes.
    - FastAPI `Depends()` defaults are NOT rewritten; B008 is silenced via `extend-immutable-calls` (preferred) or a scoped per-file-ignore for the DI modules.
    - `cd backend && python -m pytest tests -q` reports 100 passed / 17 skipped (or the same counts as before this plan — no regressions, no newly-skipped tests).
    - No `# ruff: noqa` file-wide blanket and no rule deleted from `select`.
  </acceptance_criteria>
  <done>`ruff check .` exits 0 across the backend with behavior-preserving edits; the two behavior-sensitive rules (UP042 str-enum, B008 FastAPI Depends) are handled deliberately (convert-if-safe / scope-ignore); the pytest suite is still 100 passed / 17 skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Pin ruff + mypy in [dev] and make the mypy gate actually pass</name>
  <files>backend/pyproject.toml, backend/app/services/auth_service.py</files>
  <read_first>
    - backend/pyproject.toml ([project.optional-dependencies].dev lines 36-44: `ruff>=0.4.0`, `mypy>=1.10.0`, `types-redis>=4.6.0`; [tool.mypy] lines 58-67: `strict = true`, plus an override for `app.services.*` / `app.schemas.*` with `disallow_untyped_defs`/`disallow_any_explicit`)
    - .github/workflows/ci.yml (the mypy steps lines 58-62: `mypy app/services --ignore-missing-imports` and `mypy app/schemas --ignore-missing-imports` — these are the EXACT gate commands that must exit 0; ruff step line 54 is `ruff check .`)
    - .planning/phases/01-walking-skeleton/01-UAT.md (Gap 2 `missing`: pin ruff and mypy to known-good versions; install/run mypy on app/services + app/schemas and resolve findings)
    - backend/app/services/auth_service.py (the only module under the strict `app.services.*` mypy override that may surface findings — read it to resolve any untyped-def / Any findings the strict override flags)
  </read_first>
  <action>
    Pin the lint/type tools for a reproducible gate. In `backend/pyproject.toml` `[project.optional-dependencies].dev`:
    - Replace `ruff>=0.4.0` with an exact pin `ruff==<version>` where `<version>` is the ruff version used to make Task 1 green (run `ruff --version` after Task 1 and pin THAT exact version — e.g. `ruff==0.6.9`; do not invent a version, use the one actually installed and passing).
    - Replace `mypy>=1.10.0` with an exact pin `mypy==<version>` (the mypy version you install and run below; pin the exact installed version).
    - Keep `types-redis` (pin it too if you want full reproducibility, e.g. `types-redis==<version>`). Leave pytest/pytest-asyncio/pytest-cov/httpx as-is unless you choose to pin them (optional, not required by this gap).
    Add an inline comment noting these are pinned for a reproducible CI lint/type gate (UAT Gap 2).

    Then make the mypy gate green. Install mypy (it was never installed locally per the UAT): `cd backend && pip install -e ".[dev]"`. Run the EXACT CI commands:
    - `mypy app/services --ignore-missing-imports`
    - `mypy app/schemas --ignore-missing-imports`
    Resolve every finding with behavior-preserving edits: add missing type annotations / return types, replace `Any` with the precise type, add `from __future__ import annotations` only if already the file's convention, etc. The strict override (`disallow_untyped_defs`, `disallow_any_explicit`) applies to `app.services.*` and `app.schemas.*`, so `app/services/auth_service.py` is the most likely source of findings — annotate its functions fully. Do NOT relax the `[tool.mypy]` strictness or the override to make mypy pass; fix the code. If `app/schemas` has no `.py` modules yet, `mypy app/schemas` should pass trivially (empty/namespace) — confirm it exits 0 regardless.

    After both mypy commands exit 0, re-run `ruff check .` and `python -m pytest tests -q` once more to confirm the pin + any mypy-driven annotations did not regress the lint or test gates.
  </action>
  <verify>
    <automated>cd /Users/kholmumin/WebstormProjects/polymer-intelligence/backend && grep -qE 'ruff==' pyproject.toml && grep -qE 'mypy==' pyproject.toml && python3 -c "import tomllib; tomllib.load(open('pyproject.toml','rb')); print('toml ok')" && mypy app/services --ignore-missing-imports && mypy app/schemas --ignore-missing-imports && ruff check . && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` `[dev]` pins `ruff==<exact version>` and `mypy==<exact version>` (exact `==` pins, not `>=`), where the versions are the ones actually installed and passing Task 1 / this task.
    - `python3 -c "import tomllib; tomllib.load(open('backend/pyproject.toml','rb'))"` exits 0 (valid TOML).
    - `cd backend && mypy app/services --ignore-missing-imports` exits 0.
    - `cd backend && mypy app/schemas --ignore-missing-imports` exits 0.
    - The `[tool.mypy]` strict settings and the `app.services.*` / `app.schemas.*` override are NOT relaxed to make mypy pass (findings fixed in code, not silenced).
    - After pinning + annotations: `cd backend && ruff check .` still exits 0 and `python -m pytest tests -q` is still 100 passed / 17 skipped.
  </acceptance_criteria>
  <done>ruff and mypy are exact-pinned in [dev] for a reproducible gate; `mypy app/services` and `mypy app/schemas` (the CI commands) exit 0 with code-level fixes (strictness intact); ruff and pytest gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer push → CI | Unreviewed code enters via push/PR; ruff + mypy are the static-analysis gates that block defective/insecure code from merging |
| CI runner → package index (PyPI) | Build installs the lint/type tooling; an unpinned tool version makes the gate non-reproducible (a new ruff release can change which code passes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering / Repudiation (false-green gate) | ruff lint gate | mitigate | Make `ruff check .` actually pass on real source (124 violations resolved) so the gate gives true assurance instead of failing every run; no rule removed from `select` to fake green |
| T-10-02 | Repudiation (non-reproducible gate) | ruff/mypy version drift | mitigate | Exact-pin ruff and mypy in [dev] so the CI lint/type result is reproducible and does not silently change when a new tool version ships |
| T-10-03 | Tampering (untyped code slips through) | mypy strict gate on app/services + app/schemas | mitigate | Run the exact CI mypy commands and fix findings in code without relaxing strict/override, so the type gate is genuinely enforced |
| T-10-04 | Information Disclosure (behavior change via lint autofix) | str-enum / DI default rewrites | mitigate | UP042 converted only if the regression suite stays green, else scope-ignored; B008 silenced via FastAPI-aware `extend-immutable-calls` — so no lint fix alters runtime serialization or DI behavior |
| T-03-SC | Tampering | pinned tool installs (ruff, mypy, types-redis) | accept | Pinned exact versions of well-known, already-declared dev tools from PyPI; no new third-party runtime dependency introduced |
</threat_model>

<verification>
Run from `backend/` after both tasks (these are the exact CI gate commands):
- `ruff check .` exits 0 (select=[E,F,I,N,UP,B,SIM], ignore=[E501]; `select` unchanged).
- `mypy app/services --ignore-missing-imports` exits 0.
- `mypy app/schemas --ignore-missing-imports` exits 0.
- `python -m pytest tests -q` → 100 passed / 17 skipped (no regression, no new skips).
- `grep -qE 'ruff==' pyproject.toml && grep -qE 'mypy==' pyproject.toml` → tools exact-pinned.
- `python3 -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0.
- No blanket `# ruff: noqa` and no rule removed from `select` (suppressions are scoped per-file/per-line with justifications, or FastAPI-aware `extend-immutable-calls`).
- (Cross-language, already green per VERIFICATION) dashboard + webapp eslint/tsc unchanged by this plan.
</verification>

<success_criteria>
SC#5 is closed for the backend lint/type gates: `ruff check .` exits 0 with behavior-preserving fixes (no faked green — `select` intact, suppressions scoped+justified), `mypy app/services` and `mypy app/schemas` exit 0 with strictness intact, ruff and mypy are exact-pinned for reproducibility, and the pytest suite remains 100 passed / 17 skipped. Combined with the already-green eslint/tsc gates (plan 01-06) and the deployable stack (plan 01-08), the full CI pipeline can pass green.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-10-SUMMARY.md` when done. Record: the exact ruff/mypy versions pinned, whether UP042 was converted to StrEnum or scope-ignored (and why), how B008 was silenced, and the final `ruff check .` / `mypy` / `pytest` results.
</output>
