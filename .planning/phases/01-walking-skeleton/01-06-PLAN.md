---
phase: 01-walking-skeleton
plan: 06
type: gap_closure
gap_closure: true
wave: 4
depends_on: ["01-01", "01-04"]
files_modified:
  - backend/pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: ["REQ-nfr-observability", "REQ-nfr-security"]
must_haves:
  truths:
    - "pip install -e \".[dev]\" succeeds against backend/pyproject.toml using a valid PEP 517 build backend (SC#5)"
    - "docker build -f deploy/Dockerfile.backend backend/ completes the pip install step (SC#5)"
    - "The eslint CI steps for dashboard and webapp can fail the job — no `|| true` suppresses their exit code (SC#5)"
    - "The dashboard scaffold passes `eslint --max-warnings 0` (eslint 9 flat config) and the webapp scaffold passes `eslint . --ext .ts,.tsx --max-warnings 0` (eslint 8) with zero warnings so the now-enforced gate is green (SC#5)"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Valid PEP 517 build-system using setuptools.build_meta"
      contains: "build-backend = \"setuptools.build_meta\""
    - path: ".github/workflows/ci.yml"
      provides: "Enforced eslint gates with no exit-code suppression (dashboard eslint 9 / webapp eslint 8)"
      contains: "--max-warnings 0"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "backend/pyproject.toml"
      via: "pip install -e \".[dev]\" step"
      pattern: "pip install -e \\\".\\[dev\\]\\\""
    - from: "deploy/Dockerfile.backend"
      to: "backend/pyproject.toml"
      via: "pip install \".[dev]\" step"
      pattern: "build_meta"
---

<objective>
Close Gap 2 (SC#5 — "CI (ruff, mypy, eslint+tsc, tests, image build) passes green on the scaffold"). Two defects block a green CI run: (1) backend/pyproject.toml declares an invalid PEP 517 build-backend (`setuptools.backends.legacy:build`) that breaks `pip install -e ".[dev]"` in CI and `pip install ".[dev]"` in the Dockerfile; (2) both eslint steps in ci.yml end with `|| true`, swallowing failures so the `--max-warnings 0` gate is a dead letter.

Purpose: CI is the quality backbone of the walking skeleton — a green pipeline that actually enforces lint/type/test/build gates is required for every later phase to build on a trustworthy scaffold.
Output: A valid build backend so backend installs everywhere, and enforced eslint gates that can break the build, with the scaffold confirmed clean so the now-live gate stays green.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-VERIFICATION.md
@.planning/phases/01-walking-skeleton/01-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by this gap-closure plan (existing edited symbols not listed):
- None net-new — this plan corrects two existing values (the `build-backend` key in `backend/pyproject.toml` and the two eslint step commands in `.github/workflows/ci.yml`). The reversal of the intentional `|| true` scaffold suppression (documented in 01-04-SUMMARY decisions) is recorded here as the cancelling change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fix the PEP 517 build-backend in pyproject.toml</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - backend/pyproject.toml (the file being modified — line 3 currently reads `build-backend = "setuptools.backends.legacy:build"`; the `[build-system]` block at lines 1-3 also has `requires = ["setuptools>=68", "wheel"]`)
    - .planning/phases/01-walking-skeleton/01-REVIEW.md (CR-03 section — explains why `setuptools.backends.legacy:build` is not a valid PEP 517 backend and that `setuptools.build_meta` is the correct value)
    - deploy/Dockerfile.backend (line 25 `pip install --no-cache-dir ".[dev]"` — the second consumer of this build backend)
  </read_first>
  <action>
    In `backend/pyproject.toml` line 3, replace `build-backend = "setuptools.backends.legacy:build"` with `build-backend = "setuptools.build_meta"`. Leave the `requires = ["setuptools>=68", "wheel"]` line and the rest of the `[build-system]` block unchanged — they are valid. Do not touch `[project]`, dependencies, optional-dependencies, ruff/mypy/pytest config, or `[tool.setuptools.packages.find]`.
  </action>
  <verify>
    <automated>grep -q '^build-backend = "setuptools.build_meta"$' /Users/kholmumin/WebstormProjects/polymer-intelligence/backend/pyproject.toml && ! grep -q 'setuptools.backends.legacy' /Users/kholmumin/WebstormProjects/polymer-intelligence/backend/pyproject.toml && echo BUILDBACKEND_OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains the exact line `build-backend = "setuptools.build_meta"`.
    - The string `setuptools.backends.legacy` no longer appears anywhere in the file.
    - The `[build-system]` block still contains `requires = ["setuptools>=68", "wheel"]`.
    - `python -c "import tomllib; tomllib.load(open('backend/pyproject.toml','rb'))"` exits 0 (file is still valid TOML).
    - `pip install -e "backend/[dev]"` (or `cd backend && pip install -e ".[dev]"`) in a clean Python 3.12 env exits 0. [Human/CI verification — requires a live Python env; static checks above cover the file change.]
  </acceptance_criteria>
  <done>pyproject.toml declares the valid PEP 517 backend `setuptools.build_meta`; `pip install -e ".[dev]"` (CI) and `pip install ".[dev]"` (Dockerfile) no longer fail on an invalid backend.</done>
</task>

<task type="auto">
  <name>Task 2: Remove `|| true` from both eslint steps and confirm scaffolds lint clean</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the file being modified — line ~106 dashboard eslint step and line ~135 webapp eslint step both run `npx eslint . --ext .ts,.tsx --max-warnings 0 || true`; both jobs have `working-directory: dashboard` / `working-directory: webapp` and run `npm ci` first)
    - .planning/phases/01-walking-skeleton/01-04-SUMMARY.md (decisions section — records `|| true` was an INTENTIONAL scaffold suppression "because --max-warnings 0 would block on plugins that warn on missing files"; this plan reverses that, so the scaffold must be confirmed lint-clean or the offending lint resolved/scoped)
    - dashboard/eslint.config.mjs (the dashboard eslint **9 flat config** that will now gate CI — `--ext` is REMOVED in eslint 9, so the dashboard command must NOT pass `--ext`; introduced by the dep-security upgrade commit 799b5bf) and webapp/.eslintrc.cjs (the webapp eslint **8** config — `--ext` is still valid there) — to understand which rules fire
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, remove the trailing ` || true` from BOTH eslint steps and make each command match its job's eslint major (the dep-security upgrade in commit 799b5bf moved the dashboard to eslint 9 / flat config; the webapp stays on eslint 8):
    - Dashboard job (line ~106, "Lint (eslint)"): `run:` must become exactly `npx eslint --max-warnings 0` — NO `--ext` flag (eslint 9 flat config rejects `--ext`; file matching comes from eslint.config.mjs).
    - Webapp job (line ~135, "Lint (eslint)"): `run:` must become exactly `npx eslint . --ext .ts,.tsx --max-warnings 0` (eslint 8 still accepts `--ext`).
    Do not change the tsc steps, npm ci steps, working-directory, or any other job.

    Then confirm the scaffolds actually lint clean under the now-enforced gate. Run `cd dashboard && npm ci && npx eslint --max-warnings 0` and `cd webapp && npm ci && npx eslint . --ext .ts,.tsx --max-warnings 0`. (Both were confirmed lint-clean as of commit 799b5bf.) If either reports warnings or errors, resolve them in the scaffold source (e.g., fix the lint issue, or add a narrowly-scoped ignore / rule override ONLY for the specific scaffold-placeholder files such as dashboard/app/login/page.tsx whose TODO stub is a documented Phase-4 placeholder per 01-04-SUMMARY) so that eslint exits 0 with zero warnings. Document any rule override or ignore added and the exact file/rule it targets in the SUMMARY. Do not blanket-disable eslint or re-introduce any global suppression.
  </action>
  <verify>
    <automated>! grep -nE 'eslint .* \|\| true' /Users/kholmumin/WebstormProjects/polymer-intelligence/.github/workflows/ci.yml && grep -c 'npx eslint .*--max-warnings 0' /Users/kholmumin/WebstormProjects/polymer-intelligence/.github/workflows/ci.yml | grep -q '^2$' && echo ESLINT_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains no occurrence of `|| true` on any eslint line (grep for `eslint.*|| true` returns nothing).
    - The dashboard job's eslint `run:` is exactly `npx eslint --max-warnings 0` (no `--ext`, no `|| true`) — eslint 9 flat config.
    - The webapp job's eslint `run:` is exactly `npx eslint . --ext .ts,.tsx --max-warnings 0` (no `|| true`) — eslint 8.
    - A `--max-warnings 0` eslint command appears exactly twice (one per job): `grep -c 'npx eslint .*--max-warnings 0' .github/workflows/ci.yml` returns 2.
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (still valid YAML).
    - `cd dashboard && npx eslint --max-warnings 0` exits 0 (zero warnings) after `npm ci`.
    - `cd webapp && npx eslint . --ext .ts,.tsx --max-warnings 0` exits 0 (zero warnings) after `npm ci`.
    - If any scaffold lint was resolved via a scoped override/ignore, the SUMMARY names the exact file and rule; no global eslint suppression remains.
  </acceptance_criteria>
  <done>Both eslint steps run without `|| true`, so eslint failures can break CI; the dashboard and webapp scaffolds pass `eslint --max-warnings 0` with zero warnings so the now-enforced gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer push → CI | Untrusted/unreviewed code enters via push/PR; CI quality gates (ruff, mypy, eslint, tests, image build) are the control that blocks defective code from merging |
| CI runner → package index (PyPI / npm) | Build steps fetch declared dependencies; a broken build backend or suppressed lint gate silently lets defects through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CI-01 | Tampering / Repudiation | eslint quality gate | mitigate | Remove `|| true` so lint failures fail CI; a neutered gate gives false assurance that linted-clean code merged |
| T-CI-02 | Denial of Service (build) | backend pip install (PEP 517 backend) | mitigate | Valid `setuptools.build_meta` backend so the backend job and image build complete instead of erroring on every run |
| T-03-SC | Tampering | npm/pip dependency installs in CI | accept | No new packages added by this plan; dependency set unchanged from RESEARCH-audited 01-01/01-04 manifests; CI installs are from existing pinned lock files |
</threat_model>

<verification>
Run after both tasks:
- `grep -q '^build-backend = "setuptools.build_meta"$' backend/pyproject.toml` → valid backend.
- `! grep -q 'setuptools.backends.legacy' backend/pyproject.toml` → invalid backend removed.
- `! grep -nE 'eslint .* \|\| true' .github/workflows/ci.yml` → no eslint suppression.
- `grep -c 'npx eslint .*--max-warnings 0' .github/workflows/ci.yml` → returns 2 (dashboard without `--ext`, webapp with `--ext`).
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
- (CI/human) `cd backend && pip install -e ".[dev]"` exits 0; `cd dashboard && npx eslint --max-warnings 0` exits 0; `cd webapp && npx eslint . --ext .ts,.tsx --max-warnings 0` exits 0.
</verification>

<success_criteria>
SC#5 is closed: pyproject.toml uses a valid PEP 517 backend so `pip install` succeeds in CI and in the Docker image build; both eslint gates enforce `--max-warnings 0` with no suppression; and the scaffolds lint clean so the full CI pipeline (ruff, mypy, eslint+tsc, tests, image build) can pass green.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-06-SUMMARY.md` when done.
</output>
