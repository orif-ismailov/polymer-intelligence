# P7 — Live-интеграции: Didox, Escrow-банк, гос-реестры (R6)

> Prereq: `00-CONTEXT.md`, P2 (Deal), P3 (`escrow_payments`, `provider_events`),
> R1 (`verification_cases`/`checks`), R3 (шлюз `integrations/eimzo`, evidence-паттерн).
> Факты и контракты адаптеров — **`INTEGRATIONS.md`** (главный документ этого плана).
> **Методология: строгий TDD** (тест до кода per task; полный `pytest tests/ -q` +
> фронт-гейты перед каждым коммитом). Внешние API в CI не вызываются никогда — клиенты
> мокаются по образцу `integrations/eimzo`; против реальных тест-контуров (testapi3,
> банковский sandbox) — только ручные интеграционные прогоны вне CI, журналируемые
> в Progress-секции.

## Что здесь на самом деле блокировано, а что нет

Скелет читался как «весь план ждёт доступов». Это неверно: доступ нужен **исходящему**
вызову, а у двух подпланов из четырёх основная работа — **входящая или наша собственная**.
Разделение проведено по одному критерию: *можно ли это написать и доказать тестом,
не имея чужого токена*.

| Подплан | Строится и доказывается сейчас | Требует доступа | Сессия |
|---|---|---|---|
| **P7.b** Escrow-банк | входящий контур целиком: маршрут вебхука, нормализация события, идемпотентность, `apply_provider_event`, provider-отметка платежа, правило дрейфа, очередь оператора | `LiveEscrowClient` (спека банка) — **только исходящая половина** | **эта** |
| **P7.c** Гос-реестры | контракт адаптера, immutable-снапшоты, два чека, **полуавтомат оператора** (реальная польза сегодня) | ПЦД/OneID — только `LiveGovRegistryClient` | **эта** |
| **P7.a** Didox | — (см. ниже, почему не сейчас) | partner-token + договор | отдельная |
| **P7.d** Chem-registry | нечего делать: источник истины — наш `substances` (P5) | гос-API не существует | — |

**Почему P7.a не в этой сессии.** У Didox единственный подплан, где *ничего* нельзя
проверить: и клиент, и доменный шов, и polling пишутся против пересказа документации
(`api-docs.didox.uz` программно не отдаётся), а первая же сверка с testapi3 перепишет
маппинг полей. Скелет сам требует «по одному провайдеру за сессию»; сессия без токена
даст ~600 строк недоказуемого кода и решение о схеме контракта, принятое вслепую.
Поэтому P7.a **детализирован здесь до задач** (W8–W10 ниже, вместе с четырьмя
проектными решениями, которые от токена не зависят), а реализуется в сессии, где
токен есть. Оценка INTEGRATIONS.md §5 («маппинг — дни») от этого не страдает.

**Demo (Definition of Done этой сессии):**
1. Банк-эмулятор шлёт `POST /api/v1/webhooks/escrow/generic` c `funded` → платёж
   становится `funded` **без оператора**, сделка уезжает в `paid_escrow`, повтор того же
   вебхука ничего не меняет; событие `refunded` платёж **не** трогает, а встаёт в
   очередь «банк говорит X, у нас Y» на `/escrow`.
2. Оператор на карточке верификации жмёт «Проверить в реестре», вносит результат
   открытого сервиса (my.soliq.uz / license.gov.uz) + прикладывает скриншот →
   в `registry_snapshots` ложится immutable-строка с sha256, чек `gov_registry`
   становится `passed`, кейс переоценивается.

---

## Поправки к скелету (правило 3 из `00-CONTEXT` — прав код)

1. **Миграция `0029`, не `0028`.** Голова — `0028_lab` (P6). Миграция в этом плане
   одна и обслуживает только P7.c; P7.b схемы не требует вовсе (см. п. 2–3).
2. **`provider_events` уже существует** (P3, миграция 0024) вместе с
   `UNIQUE (provider, external_id)`. Идемпотентность вебхука — уже инвариант схемы;
   P7.b пишет только маршрут и обработчик, ничего не добавляя в БД.
3. **`escrow_payments.*_marked_by` уже nullable**, и docstring модели уже говорит
   «NULL on the live rail, where the provider event is the evidence instead».
   Provider-отметка платежа ложится в существующие колонки.
4. **`ESCROW_WEBHOOK_SECRET` — секрет с пустым дефолтом**, а не «без дефолта».
   Правило «секреты без дефолтов» защищает от тихой мисконфигурации на старте, но
   рельс `live` — **runtime-настройка**, и стартовый валидатор её не видит: обязательный
   секрет заставил бы каждый деплой (включая тех, кто escrow вообще не включает)
   его придумывать. Образец уже есть — `ESKIZ_EMAIL`/`ESKIZ_PASSWORD` (пустой дефолт,
   обязательны только при `SMS_PROVIDER=eskiz`). Проверка переезжает в маршрут.
5. **Ответ на вопрос, оставленный P3** в docstring `apply_provider_event`
   (`cancelled` недостижим для `system` — нужен сервисный аккаунт или расширение
   `_ACTOR_RULES`): **ни то, ни другое**. См. решение в W2 — `funded`/`released`
   применяются автоматически, `refunded` — никогда.
6. **`registry_snapshots` не существует** — таблица упомянута только в docstring
   `models/integration.py` («evidence lives in … `registry_snapshots` instead»).
   Создаётся здесь.
7. **`gov_registry`/`vat_status` в `VerificationCheckType`** — docstring enum'а обещает
   их как «P2 adds»; фактически добавляются здесь (`ALTER TYPE … ADD VALUE`).
8. **`chem_registry_mode` уже в `_SPECS`** (P5) и `integrations/chem_registry/` уже
   существует stub-ом. P7.d не добавляет ни строки — только абзац в документации о том,
   что это осознанно.
9. **Скелет просил очередь `integrations`** — не заводим. `00-CONTEXT` разрешает
   выделять её «только если появится реальная конкуренция за воркеров». Исходящие
   вызовы идут на существующую `verify`, входящий применятель — на `default`
   (он не звонит наружу, он двигает сделку).

---

# P7.b — Escrow-банк: входящий контур

Банк неизвестен, но **форма события известна нам**: у нас есть `EscrowPayment`,
`provider_ref` и машина `pending → funded → released/refunded`. Поэтому контур
строится от нашей нормализованной формы, а адаптер конкретного банка — это одна
функция-маппер в реестре.

## Wave 1 — Нормализация события и применение

### T1.1 `integrations/escrow/events.py` — нормализованное событие + реестр мапперов
Тест первым: `tests/test_escrow_events.py`.

```python
@dataclass(frozen=True)
class NormalizedEscrowEvent:
    external_id: str      # идемпотентность (provider_events.external_id)
    provider_ref: str     # ключ к escrow_payments.provider_ref
    status: str           # 'pending' | 'funded' | 'released' | 'refunded'
    amount: Decimal | None
    currency: str | None
    occurred_at: datetime | None
    raw_status: str       # что банк сказал буквально — в evidence
```
- `MAPPERS: dict[str, Callable[[dict], NormalizedEscrowEvent]]`, одна запись —
  `"generic"` (наш собственный контракт: те же имена полей). Добавить `kapitalbank` =
  добавить функцию, ничего больше.
- `extract_external_id(provider, body) -> str` — вызывается **до** маппинга (сырой
  инбокс пишется раньше разбора); если провайдер не дал id — `sha256(body)`, чтобы
  повторная доставка того же тела схлопнулась по существующему UNIQUE.
- Исключения `UnknownProvider`, `MalformedEvent`.
- **Границы:** `integrations/` не импортирует доменные модели — статус нормализуется
  в `str` из константного набора, `EscrowStatus` из него делает сервис.

### T1.2 `escrow_service.mark_from_provider` (рефакторинг `mark`)
`mark()` требует `staff_user` и note (`StaffRequired`, `NoteRequired`) — это правильно
для stub-рельса и неверно для live. Общее ядро выносится в `_apply_mark(db, payment,
to_status, *, note, staff_user_id, provider_event_id)`; публичные двери:
- `mark(...)` — сигнатура и семантика не меняются (staff обязателен) — **все
  существующие тесты P3 остаются зелёными без правок**;
- `mark_from_provider(db, payment, to_status, event)` — `*_marked_by = NULL`,
  `note = f"provider:{event.provider}:{event.external_id}"`, тот же row-lock, тот же
  `_drag_the_deal`, то же событие и аудит (`escrow.mark_provider`).
- Инвариант, который тест обязан зафиксировать: **provider не может отметить платёж,
  открытый на stub-рельсе** (`payment.mode == 'stub'` → `WrongRail`). Иначе флип
  runtime-настройки задним числом изменил бы правила для уже открытых платежей —
  ровно то, ради чего `mode` заморожен на строке.

### T1.3 `escrow_service.apply_provider_event` — настоящее тело
Заменяет заглушку P3. Порядок: `processed?` → маппер → платёж по `provider_ref` →
проверки → отметка **или** удержание.

**Решение по вопросу P3 (`cancelled` недостижим для `system`):** не расширяем
`_ACTOR_RULES` и не заводим сервисный аккаунт.
- `funded`, `released` — **применяются автоматически**: они только двигают сделку
  вперёд и целевые статусы (`paid_escrow`, `completed`) и так `system`-only.
- `refunded` — **никогда не применяется автоматически**. Возврат убивает сделку, а
  решение убить сделку имеет причину и человека (FR-D8: из `paid_escrow` возврат
  вообще требует сперва спора). Событие записывается, помечается `processed` и
  удерживается с префиксом `hold:` в `provider_events.error` — это и есть очередь
  оператора.
- `released`, пришедший на неотгруженную сделку, тоже удерживается
  (`EscrowReleaseBeforeDelivery` остаётся в силе для provider-пути — тест обязателен).
- Прочие удержания: неизвестный `provider_ref`, платёж на stub-рельсе, недопустимый
  переход. Каждое — со своим суффиксом, чтобы оператор в дашборде читал причину, а
  не «409».
- `HOLD_PREFIX = "hold:"` — модульная константа; «очередь» = выборка по префиксу.
  Новой колонки не заводим: `provider_events` — evidence, `error` уже свободный текст,
  а строка **самоочищается** — как только оператор отметит платёж вручную, событие
  перестаёт попадать в выборку (условие сверяет заявленный статус с фактическим).

### T1.4 Тесты W1 (красные до кода)
`tests/test_escrow_provider_events.py`: идемпотентность повторной доставки; `funded`
двигает сделку; повтор `funded` — no-op; `refunded` не двигает ничего и удерживается;
`released` до `delivered` удерживается; неизвестный `provider_ref` удерживается;
stub-платёж отказывает provider-отметке; неизвестный провайдер; кривой payload.

## Wave 2 — Маршрут вебхука и задача

### T2.1 `POST /api/v1/webhooks/escrow/{provider}` (`app/api/webhooks_escrow.py`)
- Аутентификация: заголовок `X-Escrow-Token`, сверка `hmac.compare_digest` с
  `settings.ESCROW_WEBHOOK_SECRET` (образец `telegram_webhook.py`).
  **Секрет не задан → 404** (эндпоинт не должен подтверждать своё существование
  неаутентифицированному сканеру), **не совпал → 401**.
- Порядок ровно такой: прочитать сырое тело → `extract_external_id` →
  `record_provider_event` → **commit** → поставить Celery-задачу → `200 {"ok": true}`.
  Разбор и применение — в задаче, поэтому банк получает 200 даже на payload, который
  мы не поняли: непонятое событие всё равно evidence.
- Не-JSON тело записывается как `{"_raw": "<text>"}` — 200, разбор упадёт в задаче.
- Роутер регистрируется в `create_app()`; в OpenAPI помечен `include_in_schema=False`
  (внешний контур, не часть публичного API).

### T2.2 Celery: применятель + подметатель
- `app.tasks.payments.apply_escrow_provider_event(event_id)` — очередь `default`
  (наружу не звонит), идемпотентен, никогда не поднимает исключение наружу.
- Beat `sweep_provider_events` (каждые 5 мин) — берёт `processed=false` пачкой:
  страховка на случай, когда commit прошёл, а `apply_async` не долетел (Redis моргнул).
  Именно ради этого запись и постановка задачи разделены.
- Модуль уже в `_TASK_MODULES`; в `schedule.py` добавляется одна запись.

### T2.3 Тесты W2
404 без секрета; 401 с чужим токеном; 200 + строка инбокса; повторная доставка → одна
строка; не-JSON → 200 и `_raw`; падение брокера не роняет 200; подметатель добирает
незаявленное событие.

## Wave 3 — Сверка (дрейф) и очередь оператора

### T3.1 Beat `reconcile_escrow_payments` (очередь `verify`)
Единственное место, которое **звонит наружу**, поэтому `verify`. Для каждого
не-терминального платежа на live-рельсе: `client.get_status(provider_ref)`.
- Провайдер продвинулся и переход автоприменим → применить (тот же путь, что вебхук).
- Провайдер разошёлся с нами (банк `released`, сделка не `delivered`; банк `refunded`)
  → **алерт, не автопереход** (требование скелета) + удержанная запись в инбоксе.
- `ProviderUnavailable`/отсутствие клиента → тихий no-op: недоступность банка не
  двигает и не ломает пайплайн (инвариант деградации).
- Клиент инжектится параметром, чтобы тест гонял сценарии на фейке.
- `LiveEscrowClient` **не пишется** — `get_escrow_client` для `live` по-прежнему
  поднимает `ProviderUnavailable` с текстом «нужна спека банка». Так задача сегодня
  честно ничего не делает, а правило дрейфа уже зафиксировано тестом.

### T3.2 `GET /admin/escrow/provider-events` + блок в дашборде
Аналитик читает, admin действует существующей кнопкой отметки. Список удержаний:
что сказал банк, что у нас, почему удержано, ссылка на платёж. Отдельной кнопки
«разрешить» нет — разрешение это и есть ручная отметка платежа, после которой строка
уходит из выборки сама.

---

# P7.c — Гос-реестры: контракт, снапшоты, полуавтомат

ПЦД нет и, судя по INTEGRATIONS.md §3, может не быть долго. Но открытые сервисы
(my.soliq.uz — НДС, license.gov.uz — лицензии) работают **сегодня** и их результат
сегодня же никуда не записывается: оператор смотрит в браузер и решает в уме.
Полуавтомат превращает это в evidence.

## Wave 4 — Схема и шлюз

### T4.1 Миграция `0029_gov_registry` + модель + enum'ы
- `ALTER TYPE verification_check_type ADD VALUE 'gov_registry'`, `'vat_status'` —
  внутри `op.get_context().autocommit_block()` (образец `0020_eimzo`, `0027`, `0028`).
- **`registry_snapshots`** (`app/models/registry.py`, регистрируется в
  `models/__init__.py`): `id`, `company_id` FK NOT NULL, `kind` Text NOT NULL
  (`company | licenses | vat`), `source` Text NOT NULL (`registry | manual`),
  `provider` Text NOT NULL (`pcd | oneid | manual`), `payload` JSONB NOT NULL
  (нормализованный DTO), `raw_status` Text NULL, `evidence_path`/`evidence_sha256`
  Text NULL (скриншот полуавтомата — тот же паттерн, что `signature_evidence`),
  `note` Text NULL, `created_by` FK `staff_users` NULL (NULL = автоматический),
  `fetched_at` timestamptz NOT NULL, `created_at`.
  Индекс `(company_id, kind, id DESC)` — «последний снапшот вида X» единственный
  горячий запрос. CHECK на допустимые `kind`/`source`.
  **Строка immutable:** нет `updated_at`, нет ни одного пути на UPDATE. Новая проверка —
  новая строка; история проверок компании — это и есть история строк.

### T4.2 `integrations/gov_registry/`
DTO `CompanySnapshot` (name, status, director, oked, address, registered_at),
`LicenseSnapshot` (regime, number, issued_at, expires_at, issuer, status),
`VatSnapshot` (registered: bool, certificate_no, valid_from).
Protocol `GovRegistryClient` (`lookup_company/lookup_licenses/lookup_vat`),
`StubGovRegistryClient` (все методы → `ProviderUnavailable`: у нас нет канала, и
делать вид, что есть, хуже, чем сказать «нет»), фабрика `get_gov_registry_client(db)`
по новой runtime-настройке `gov_registry_mode` (`stub | live`, ships `stub`, `choices`).

### T4.3 `registry_service`
`record_snapshot(...)` — immutable-вставка + аудит в той же транзакции;
`latest(db, company_id, kind)`; `fetch_and_record(db, company, kind)` — live-путь,
`ProviderUnavailable` → `None` (не исключение наружу).

## Wave 5 — Чеки и полуавтомат оператора

### T5.1 Чистые функции чеков (`verification_checks.py`)
`check_gov_registry(company, snapshot)`:
нет снапшота → `unavailable`; реестр говорит «ликвидирована» → `failed`; ИНН/название
совпали → `passed`; название разошлось → `warning` (реестр авторитетен, но у нас может
лежать вариант написания — это повод посмотреть, а не отказать).
`check_vat_status(company, snapshot)`: нет снапшота → `unavailable`; есть свидетельство
→ `passed`; **нет свидетельства → `warning`, не `failed`** — не каждая компания
обязана быть плательщиком НДС, и отказ по этому основанию был бы неверен юридически.

### T5.2 Эвалюатор перестаёт залипать на `unavailable`
**Найденный при детализации латентный баг R1**, который P7.c обязан починить до того,
как заведёт первый чек, способный стать `unavailable`: `on_check_completed` считает
`unavailable` «ещё не закончился», а `approve()` работает только из `pending_review` —
значит чек, исчерпавший 5 ретраев, навсегда запирает кейс в `checks_running`, и
**ручной путь перестаёт работать**, что прямо нарушает инвариант R1 («недоступность
провайдера не блокирует ручной путь»).
Правка минимальна и точна: `unavailable` блокирует эвалюацию, **пока ретраи не
исчерпаны** (`check.attempts < MAX_CHECK_ATTEMPTS`), после чего перестаёт. Константа
`MAX_CHECK_ATTEMPTS = 5` объявляется в `verification_service` и используется в
`tasks/verification.py` (сейчас `max_retries=5` зашит) — одно число в одном месте.
Тест: чек с исчерпанными ретраями не мешает кейсу уехать в `pending_review`.

### T5.3 Чеки заводятся только когда есть чему их наполнять
`_R1_CHECK_TYPES` не трогаем. Registry-чеки добавляются к кейсу **по факту появления
снапшота** (полуавтомат оператора или успешный live-fetch), а автоматически при
`submit_case` — только когда `gov_registry_mode='live'`. Настройка ships `stub`,
поэтому сегодня поведение сабмита не меняется ни на йоту.

### T5.4 `POST /admin/verification/cases/{case_id}/registry-check`
multipart: `kind`, `outcome` (нормализованные поля формы), `note`, опциональный
скриншот (PDF/JPEG/PNG, `storage_service.validate_upload`). Делает в одной транзакции:
снапшот `source='manual'` + evidence → upsert `VerificationCheck` нужного типа →
пересчёт `on_check_completed` → аудит. Гард — `require_analyst_or_admin`
(это работа верификатора, не админа).
`GET /admin/verification/cases/{case_id}` дополняется блоком `registry` —
последние снапшоты по трём видам.

## Wave 6 — Дашборд и i18n

### T6.1 Карточка кейса: блок «Реестры»
Три вида, для каждого — дата, источник (`реестр`/`вручную`), кто внёс, ссылка на
скриншот (presigned, TTL 600 s), кнопка «Проверить в реестре» → диалог с полями по
виду. Плюс блок удержанных provider-событий на `/escrow` (T3.2).
Только примитивы `components/ui`, тёмная тема P0.

### T6.2 Локали дашборда `ru/uz/tr/fa/zh` — полное дерево ключей.

## Wave 7 — Документация и Progress
DB-архитектура (`registry_snapshots` DDL + changelog), `docs/admin-guide-ru.md` §11
(как проверять в реестре и что значит каждый исход; что делать с удержанным
provider-событием), дельты в `backend/CLAUDE.md` / `dashboard/CLAUDE.md` /
`deploy/CLAUDE.md`, `deploy/.env.example` + CI-плейсхолдеры для `ESCROW_WEBHOOK_SECRET`,
Progress-секция.

---

# P7.a — Didox (отдельная сессия; нужен partner-token)

Детализация сделана сейчас, чтобы сессия с токеном была прямой реализацией.
Четыре решения, которые от токена не зависят и должны быть приняты до кода:

1. **Чей документ юридический.** При `signing_provider='didox'` юридический артефакт —
   «Договор НК» внутри Didox (уходит в роуминг my.soliq.uz), а наш отрендеренный PDF
   становится превью. Поэтому Didox-архив кладётся в **отдельную** пару
   `provider_archive_path`/`provider_archive_sha256`, а не переписывает
   `generated_document_path`/`document_sha256`: два артефакта — две пары хешей.
2. **Провайдер замораживается**, как `escrow_payments.mode`: выбран при создании,
   после появления `provider_doc_id` не меняется.
3. **Статус `50` (аннулирован НК) не добавляет состояние в нашу машину.** `active`
   терминален, и на активном контракте уже может ехать Deal; тихий перевод в новое
   терминальное состояние оставил бы сделку без опоры. Пишем `provider_status`,
   поднимаем алерт стаффу — это юридическое событие для человека.
4. **Активация по Didox идёт не через `_maybe_activate`.** Тот требует двух строк
   `contract_signatures` с нашим `signature_evidence`; подписи Didox мы не видели.
   Нужна отдельная дверь `contract_service.activate_from_provider(contract, archive)`,
   которая пишет архив как evidence — подделывать наши строки подписей нельзя.

Волны: **W8** шлюз (`integrations/didox/`: `auth_user` с кэшем user-key в Redis на
360 мин, `create_contract`, `timestamp`, `sign`, `join_signatures`, `get_document`,
`list_updated`, `fetch_archive`; `StubDidoxClient`; `didox_mode`; секреты
`DIDOX_PARTNER_TOKEN`/`DIDOX_BASE_URL`) → **W9** доменный шов (миграция 0030:
`contracts.signing_provider|provider_doc_id|provider_status|provider_synced_at|
provider_archive_*`; маппинг наших variables в `ContractDoc`; `didox_service`) →
**W10** `poll_didox_statuses` (инкрементально по `dateFromUpdated`, идемпотентно) +
шаг timestamp в портальном `features/eimzo-sign` + приёмка на testapi3.

**Первое, что делает та сессия, — сверяет пути и имена полей с api-docs.didox.uz**;
всё, что написано о путях в INTEGRATIONS.md §1, — пересказ, а не первоисточник.

---

# P7.d — Chem-registry: осознанно ничего

`integrations/chem_registry/` (stub) и `chem_registry_mode` уже стоят с P5.
Машиночитаемого гос-реестра не существует (INTEGRATIONS.md §4), источник истины —
наш `substances`. Live-адаптер появится, только если НРОХВ/ПЦД дадут API; триггер —
принятие закона «О химической безопасности». Мониторинг — операторская задача, не код.

---

## Правила для всех подпланов
Паттерн шлюза из `00-CONTEXT.md` (circuit breaker, `integration_call_log`, stub-фабрика,
`ProviderUnavailable`, деградация); каждый подплан детализируется в задачи только после
получения фактической документации провайдера; **ничего не менять в моделях P2/P3/P5** —
только клиенты, задачи и маппинг (единственное исключение оговорено в T5.2 и является
починкой бага, а не изменением модели).

## Заведомые пробелы (не молча)
- **`LiveEscrowClient` не написан** — исходящая половина ждёт спеку банка. Правило
  дрейфа и автоприменение уже зафиксированы тестами на фейковом клиенте.
- **`LiveGovRegistryClient` не написан** — ждёт ПЦД. Полуавтомат заменяет его функцию
  на 90 % и даёт то, чего у ПЦД-пути не будет: подпись оператора под результатом.
- **P7.a целиком** — см. выше.
- **e2e-спеки нет** (общий пробел P1–P6: в репозитории нет e2e-пути к верифицированной
  компании). Проверка — ручная браузерная.

---

## Progress
- [x] детализация плана → задачи T1.1…T7.1 (`4d4a7fc`, вместе с W1)
- [x] W1 — нормализация provider-события + `mark_from_provider` + `apply_provider_event` (`4d4a7fc`)
- [x] W2 — маршрут вебхука + применятель + подметатель (`cf5a189`)
- [x] W3 — сверка/дрейф + очередь оператора + панель дашборда (`f87446a`)
- [x] W4 — миграция 0029 + `integrations/gov_registry` + `registry_service` (`badf1f8`)
- [x] W5 — чеки + починка эвалюатора + полуавтомат оператора (`3644b06`)
- [x] W6 — дашборд (блок «Гос-реестры») + i18n ×5 (`fb01913`)
- [x] W7 — документация + Progress
- [x] **P7.a Stage 1 — справочник компаний Didox + автозаполнение регистрации** (токен получен
      15.08.2026; `integrations/didox/` + `DidoxGovRegistryClient` + `GET /portal/companies/lookup`
      + префилл шагов 2–3 визарда). Отклонения от плана, принятые по факту живого API:
      * **Это НЕ новый субдомен, а третий рельс `gov_registry_mode`** (`stub|didox|live`). Didox
        отдаёт запись налогового реестра — ровно то, что P7.c уже смоделировал и оставил заглушкой,
        ожидая ПЦД. Поэтому `registry_snapshots`, чеки `gov_registry`/`vat_status` и админ-UI
        работают без единой правки.
      * **«Компании нет» — это HTTP 200 с пустым конвертом**, а не 404 (проверено на живом
        контуре). `CompanyNotFound` наследует `ProviderUnavailable`, чтобы все существующие
        вызывающие деградировали в `unavailable`-чек, а форма могла сказать «не найдено».
      * **401 — это про наш `user-key`, а не про компанию**: прод требует его для этого эндпоинта,
        тест-контур нет. Поэтому 401 → `ProviderUnavailable`, никогда не вердикт.
      * `ChannelDisabled` отделён от аварии: на дефолтном (выключенном) рельсе форма молчит.
      * `didox_mode` (документы) заведён отдельно от `gov_registry_mode=didox` (чтение реестра) —
        читать реестр безопасно, отправлять юридически значимые документы нет.
- [ ] **P7.a Stage 2 (W8–W10 — договор 007 / ЭСФ 002 / акт 005)** — подписной контур.
      **Блокер, найденный при Stage 1:** портал не грузит E-IMZO-загрузчик (`window.CAPIWS`
      не определён нигде в репозитории), а `capiws.ts` отдаёт только `pkcs7_64` без
      `signature_hex`, который обязателен для `/v1/dsvs/timestamp`. Это ломает и R3-подписание
      контрактов, не только Didox.
- [x] **W0 — разведка на testapi3 (19.08.2026).** Всё ниже проверено вживую, ключами
      `test.e-imzo.uz` на смонтированном `/Volumes/DSKEYS`, партнёрским токеном из корневого
      `.env`. Аккаунт продавца заведён через **настоящую форму** `test3.didox.uz/registration`
      (OOO TREADWAY INC, ИНН 562353400).

      1. **`create_pkcs7` действительно отдаёт `signature_hex`** — 128 hex-символов (64 байта,
         ГОСТ) рядом с `pkcs7_64`. `POST /v1/dsvs/timestamp` принял пару и вернул
         `timeStampTokenB64` (7536 симв., `isAttachedPkcs7: true`). **Это первый раз, когда
         таймстемп-плечо вообще прошло в этом репозитории** — блокер выше подтверждён как
         чисто наш: модуль отдаёт поле, `capiws.ts:356` его выбрасывает.
      2. **`_id` ≠ `contractid`.** Создание 007 вернуло `_id = 8ca023ec…` (32 hex) и
         `document_json.contractid = 6a857945…` (24 hex, ObjectId). Это ДВА разных
         идентификатора — поэтому в `didox_documents` заведены обе колонки.
      3. **Метод 18 (`/v1/documents/contract/{id}/info`) не берёт ни один из них**: оба дают
         `422 {"contractId":["Значение поля contract id должно быть целым числом."]}`.
         Значит его `contractId` — это id договора, **зарегистрированного в my.soliq.uz**
         (поле `ContractId` в JSON ЭСФ), а не id документа Didox. Связка ЭСФ→договор идёт через
         служебное `didoxcontractid` с hex-идентификатором.
      4. **Оферта НЕ блокирует чтение.** `GET /v2/documents` с неподписанной офертой отдаёт
         `200 {"data":[],"total":0}`. Проба оферты через список документов, заложенная в план
         W5, неверна — гейт срабатывает на отправке, и определять состояние надо по
         `didox_companies`, а не по 422 от чтения. **Создание черновика 007 тоже прошло (200)
         без подписанной оферты.**
      5. **Регистрация = signup + подписание оферты, и это их собственный UI.** Форма после
         подписи сама дёргает `POST /v1/documents/offer/create`, который на тест-контуре упал
         с **500** («Истисно холати юз берди»). При этом **signup прошёл**: вход по паролю
         (`POST /v1/auth/{tin}/password/ru`) отдаёт 200 и `user-key`. То есть трёхстадийная
         модель W5 (`not_registered` / `offer_unsigned` / `ready`) — не абстракция, а ровно то
         состояние, в котором аккаунт остаётся после сбоя.
      6. **Валидатор email у Didox отвергает `+`** («Электрон почтанинг формати нотўғри»).
         Если мы когда-нибудь дёрнем `/v1/auth/signup` сами — plus-адреса передавать нельзя.
      7. **Тест-контур частично мёртв вверх по течению, и молчит об этом по-разному:**
         * `/v1/profile/productClassCodes` → `422 {"success":false,"error":"cURL error 6:
           Could not resolve host: gnk-gw.didox77.uz …"}`. Их ИКПУ-шлюз недоступен, так что
           **вопрос «глобальный tasnif или только привязанные коды» остаётся открытым** — но
           утёкший upstream-URL (`/codes/integration-mxik/company/get/basket-products?…&tin=`)
           намекает на корзину компании, а не на общий справочник. Перепроверить, когда шлюз
           поднимут.
         * `/v1/profile` → `422 "Failed to get Phis By Tin Info info from soliq"`.
         * `/v1/profile/vatRegStatus/{tin}` → **`200 {"status":"failed"}`**. Двухсотка с
           конвертом-неудачей: `build_facture_002` обязан проверять `status == "success"`,
           иначе отсутствие данных о НДС уедет в документ как пустой код.
      8. **PascalCase → lowercase подтверждён вживую** на ответе создания 007.
      9. `GET /v1/documents/{_id}?owner=1` отдаёт `data.{json,document,toSign,isValid,…}` —
         подписывать надо `data.json` (dict), `toSign` лежит рядом.
- [x] **W1 — портальный мост E-IMZO (19.08.2026).** `window.CAPIWS` теперь ставится нами
      (`shared/lib/eimzo/capiwsSocket.ts`), `sign()` возвращает `{pkcs7_64, signature_hex}`,
      добавлена key-сессия. Проверено в живом браузере против работающего модуля: до —
      `window.CAPIWS === undefined` и `probe() === false` у ВСЕХ пользователей (ломалось и
      R3-подписание контрактов), после — оба сертификата перечисляются, реальная подпись даёт
      `pkcs7_64` 4016 симв. и `signature_hex` 128 симв.
- [x] **W2 — миграция 0041 + домен `edi`.** `didox_documents` (оба типа документов, архив и
      его sha256 — здесь, а не на `contracts`), `didox_companies`, `contracts.signing_provider`,
      `contract_templates.kind`, `seller_offers.ikpu_*` + `sample_letter_*`,
      `sample_requests.public_id|deal_id|letter_*`, `sample_request_status += pending_letter`.
- [x] **W3 — шлюз документов.** Порт всего Stage-2-контура на существующую обвязку
      (breaker, `integration_call_log`, 4xx/5xx). Живая проверка: user-key, список документов,
      создание 007, чтение, корректная классификация ошибки ИКПУ, удаление черновика.
- [x] **W4 — сборщики payload'ов 007 и 002** (`domains/edi/payloads.py`, чистые).
      **007 принят живым контуром**, и все числа вернулись байт-в-байт (`count=1000`,
      `deliverysumwithvat=16299998.40`). Два вывода из этой волны:
      * `str(Decimal("1000").normalize())` — это `"1E+3"`. Научная нотация в поле количества
        документа, уходящего в налоговую, — не вопрос вкуса; форматирование идёт только через
        `:f`. Поймано голден-тестом, а не на контуре.
      * Избыточная точность **не округляется, а падает** (`PrecisionError`). Реальные
        спецификации правда содержат цены с 3 знаками (7 686,525 сум/кг — из настоящего
        договора MGBUS), Didox разрешает 2, и тихое округление изменило бы сумму договора.
- [x] **W5 — Didox-сессия + онбординг компании.** Прогнано ВЖИВУЮ: локальный стек
      (pi-pg/pi-redis/pi-minio), миграция 0041 применена к настоящей БД, API на :8000,
      портал на :5173, подпись — реальным ключом KRAEMER в браузере.
      `not_registered` → браузер подписал ИНН → `POST /didox/session` 200 →
      `offer_unsigned` + `has_session: true`; строка в `didox_companies`, user-key в
      Redis (TTL 17977 s), `integration_call_log` = `timestamp` 200 + `auth_by_eimzo` 200.
      Гейты проверены на живой БД: stub-рельс → `disabled`, действие на нём → 409
      `didox_disabled`, чужая компания → **404**, оферта без сессии → 409
      `didox_session_required`.

      **Два бага, которых моки не увидели:**
      1. `/v1/newoffer/base64` и `/documentBase64` отдают **голый base64, а не JSON**.
         `resp.json()` падал, и это выглядело как авария провайдера: шаг оферты отвечал
         `didox_unavailable`, хотя Didox вернул 200 и нормальный PDF. Добавлен
         `_base64_body` + регрессионные тесты с сырым телом.
      2. **Подписывается НЕ PDF оферты, а JSON из ответа `offer/create`**
         (`reference/11-offer-signing.md`, шаг 3). Значит браузер не может знать байты
         до того, как документ создан, — поток обязан быть в два круга, как подписание
         документов в W6. Односоставный `accept_offer` подписывал бы не то.
- [x] **W6 — машина документов + `activate_from_provider`.** `edi/service.py`
      (создание, два круга подписи, применение статуса), `edi/numbering.py`,
      `contracts.activate_from_provider` + `ProviderEvidence` + `WrongRail`,
      `storage_service.store_didox_archive`, две портальные ручки.
      `activate_from_provider` **не пишет `contract_signatures`**: та дверь требует
      `signature_evidence` с PKCS#7, который проверили МЫ, а на рельсе Didox мы не
      видели ни одной подписи — контрагент мог подписать в кабинете любого из 27
      операторов. NOT NULL на `signature_evidence_id` говорит то же самое схемой.

      Живая проверка: машина создала настоящий 007 (`didox_id` + `contractid`
      сохранены), `sign-payload` вернул точные байты, браузер подписал их реальным
      ключом, повтор той же заначки дал **400 `challenge_expired`** — одноразовость
      подтверждена на живом Redis. `/sign` у Didox ответил **500** (их сторона, тот
      же куст 500-ок, что и оферта); наш код классифицировал как аварию → 503.

      **Баг, который нашёлся только в браузере:** подписывать уже-base64 данные
      через `sign()` НЕЛЬЗЯ. `atob()` даёт latin-1-строку UTF-8-байтов, а `sign()`
      прогоняет её через `encodeURIComponent` ещё раз — «Поставка полимеров»
      подписывалась как «ÐÐ¾ÑÑÐ°Ð²ÐºÐ°...», то есть подпись покрывала байты,
      которые проверяющая сторона никогда не воспроизведёт. Молча, и только на
      документе с кириллицей. Добавлен `signBase64` (сессия + мост) и три теста,
      включая тот, что показывает: **ASCII проходит round-trip без потерь**, — вот
      почему это не всплыло раньше.
- [x] **W7 — шов сделка↔договор и проба→сделка.** `attach_contract` и
      `contract_prefill` существовали и были покрыты тестами с P2, но **не имели
      ни одной ручки**, а `ContractCreateIn` — поля для `deal_id`. Итог:
      `deals.contract_id` всегда NULL, `CONTRACT_ACTIVATED` ищет сделку ПО договору
      и не находит, сделка не доходит до `contract_signed`, эскроу не открывается.
      Живая проверка на настоящей БД: сделка → договор → `attach_contract` →
      `activate_from_provider` (0 строк `contract_signatures`) → `deal_for_contract`
      находит → `contract_signed`. Затем через реальный UI: вход по OTP, форма по
      `?deal_id=31`, `GET .../contract-prefill?template_id=1 → 200`, отправка —
      **`deals.contract_id = 25`**.
      Отклонение от плана: консьюмер `SAMPLE_REQUEST_STATUS_CHANGED` НЕ добавлен.
      `sample_service.transition` уже шлёт `KIND_SAMPLE_REQUEST_STATUS`, а на
      `received` покупатель — сам актор: получился бы колокольчик на собственный
      клик под дублирующим kind'ом. Есть тест, фиксирующий это решение.
- [x] **W8 — письмо-обязательство (внутреннее, E-IMZO).** Шаблон
      `SAMPLE_LETTER_V1` живёт в `contract_templates` (`kind` разделяет) и
      рендерится тем же чистым `render.py`; заявка с `sample_letter_required`
      садится в `pending_letter`, и **продавцу ничего не уходит**, пока покупатель
      не подпишет. Челлендж выведен из sha256 письма, условия продавца
      снапшотятся при подписании, `purpose='sample_letter'`.
      Живая проверка: письмо `ПО-2026-000001` отрендерено (17 972 байта PDF в
      MinIO, sha256 совпадает со строкой), челлендж привязан к хешу,
      подпись → `pending_letter` → **`requested`**, повтор → `400
      challenge_expired`, повторный челлендж → `409 already_signed`, строка
      `signature_evidence` с `purpose='sample_letter'` рядом с `contract_sign`.
      **Ограничение:** локально `EIMZO_STUB=true`, поэтому НАСТОЯЩИЙ PKCS#7 от
      браузера отвергается стабом (он ждёт свой JSON-конверт) — проверено, что
      именно так и происходит. Криптопроверка — это неизменённый R3-код
      (`verify_pkcs7`), уже проверенный вживую в R3; всё, что добавила W8,
      прогнано через настоящий стек.
- [x] **W9 — выбор ИКПУ на объявлении.** Три ручки (`search` / `packages` /
      `bind`), все требуют user-key компании, поэтому пустое состояние пикера И
      ЕСТЬ гейт Didox-сессии. Код хранится на `seller_offers` (пять полей — все
      или ни одного, CHECK + валидатор + `line_from_offer`), пикер — на 7-м шаге
      мастера, i18n ×3 с одинаковыми деревьями ключей.
      **Легаси-объявления не бэкфилятся никогда:** придумывать налоговую
      классификацию чужого товара — не наше дело, поэтому `IkpuMissing` падает,
      а не подставляет значение.

      **Нашлось только на живом контуре:** их ИКПУ-шлюз отдаёт ДВЕ разные формы
      отказа, с разницей в несколько часов и с одного эндпоинта —
      `cURL error 6: Could not resolve host: gnk-gw.didox77.uz` и
      **`Failed to get class codes by tin`**. Вторая ничем не выдаёт, что это
      их авария: читается как отказ по нашему запросу. С узким списком маркеров
      продавец получил бы «ваш поиск неверен», хотя поиск был в порядке.
      `failed to get` — их общий стиль для «умерла наша зависимость» (ср.
      `Failed to get Phis By Tin Info info from soliq` у `/v1/profile`).
      В браузере проверено: поиск отдаёт **503 `didox_ikpu_unavailable`**, пикер
      показывает «справочник недоступен» и **не показывает пустой список** —
      пустой список читался бы как «такого кода нет», и продавец вписал бы свой.
- [x] **W10 — поллер `poll_didox_documents` + админ-поверхность.** Beat каждые
      10 минут на очереди `verify`, дисциплина скопирована с
      `reconcile_escrow_payments`: mode-гейт ДО любого I/O, никогда не бросает,
      курсор `last_polled_at` пишется на сутки НАЗАД (у `dateFromUpdated`
      гранулярность в день, поэтому страницы обязаны повторяться — это безопасно
      ровно потому, что `apply_status` идёт только вперёд). Компании без
      закэшированного user-key пропускаются молча: мы физически не можем его
      выпустить, а синхронный путь и так покрывает обычный случай.
      Статусы `4`/`50` — алерт стаффу и НИКАКОГО перехода.
      Живая проверка на настоящем стеке: stub → `{"status":"disabled"}` (в Didox
      не ходили вообще), live → реально сходил в Didox за нашим 007
      (`get_document 200` в `integration_call_log`), курсор записан на 1 день
      назад, после удаления ключа из Redis → `skipped_no_key: 1`.
      Админ-ручки отданы стаффу: `/admin/didox/documents` показал наш документ,
      `/admin/didox/companies?stuck=true` — компанию в состоянии `offer_unsigned`.
- [x] **W11 — портальный рельс Didox (сокращён до договора 007).** По решению
      от 19.08 ЭСФ и ИКПУ временно вне объёма, поэтому кнопки «Выставить ЭСФ» и
      гейта на подтверждение поставки НЕТ — вернутся вместе с ЭСФ.
      Сделано: `entities/edi`, `features/didox-session` (+ карточка онбординга),
      `features/didox-sign` (два круга подписи), ветка по `signing_provider` на
      странице договора, i18n ×3 (35 ключей, деревья совпадают).
      Бэкенд начал отдавать `signing_provider` / `didox_document_id` /
      `didox_status` — без них UI не мог бы выбрать рельс.

      Два решения, которые стоит помнить:
      * **`withSession`** — действие выполняется, и если пришёл `409
        didox_session_required`, сессия минтится и действие ПОВТОРЯЕТСЯ. 360-минутная
        стена не должна выглядеть как ошибка: пользователь и так у машины с ключом.
      * **Чья очередь подписывать — по статусу Didox, а не по нашим `signatures`.**
        На этом рельсе `signatures` пуст по построению: чужой PKCS#7 мы не видим,
        контрагент мог подписать у любого из 27 операторов.

      Живая проверка в браузере: карточка на `/cabinet/companies/30/manage`
      показала реальное «Оферта не подписана»; на stub-рельсе карточка исчезает
      и слова «Didox» на странице нет вообще; на договоре с `didox_status=0`
      рисуется кнопка Didox (кнопки E-IMZO нет), при `didox_status=1` —
      «Ждём подписи контрагента».
      **Поймано при этом:** в новых компонентах были классы `text-muted` /
      `border-line` / `text-warn`, которых в конфиге НЕТ (правильные —
      `text-text-muted`, `border-border`, `text-warning`). Это ровно тот
      «тихий» отказ, о котором предупреждает дизайн-система; те же классы были
      и в пикере ИКПУ из W9 — исправлено в обоих.
- [x] **Проверка «боевым» ключом (19.08, вечер) — гипотеза о наших тестовых ИНН
      ОПРОВЕРГНУТА.** Получен ключ на РЕАЛЬНЫЙ ИНН 312616547 (наш собственный),
      срок 1 год, полный subject. Результат: подпись и timestamp — OK,
      **`POST /v1/auth/signup` через наш код → 200** (первая живая проверка нашего
      онбординга), `GET /v1/profile` → 422 «Failed to get tin info from soliq»,
      **`POST /v1/documents/offer/create` → тот же 500 `Failed to store offer`**.
      Значит 500 не зависит ни от типа ключа, ни от реальности ИНН.

      **Настоящая первопричина найдена:** `GET /v1/utils/info/{tin}` отвечает 200
      только для компаний, уже лежащих в их локальном кэше (DIDOX TECH, VENKON,
      MGBUS). Наша реальная компания И заведомо несуществующий `999999999` дают
      ОДИНАКОВЫЙ ответ — `500 cURL error 6: Could not resolve host:
      gnk-gw.didox77.uz`. То есть это не «нет в реестре», а недоступность их
      шлюза; он же стоит за `/v1/profile`, ИКПУ, `vatRegStatus` и, вероятно, ЭСФ.
      `docs/didox-support-report.md` переписан: два дефекта на их стороне,
      с контролем, который это изолирует.
- [ ] **Подписание оферты на testapi3 заблокировано их стороной.**
      `POST /v1/documents/offer/create` → **500**, тот же самый, что ломает их
      собственную страницу регистрации. Наш код отрабатывает верно: PDF получен (200),
      500 классифицирован как авария (`didox_5xx`), наружу 503, компания остаётся
      `offer_unsigned`. Пока это не починят, ни одна компания не дойдёт до `ready`, а
      значит **первая отправка документа будет падать `422 offer required`** — что и
      ловит `DidoxError.offer_not_signed` из W3.
- [ ] **ЭСФ (002) на testapi3 заблокирован их стороной.** Любой 002 отвергается
      `422 "STIR (tin) yoki JSHSHIR (pinfl) kiritishi shart"` — включая **дословный пример из
      их же документации** с подставленными нашими ИНН. Значит дело не в нашем payload'е.
      Согласуется с остальными симптомами того же сломанного upstream'а: `/v1/profile` →
      `422 "Failed to get Phis By Tin Info info from soliq"`, `vatRegStatus` → `200
      {"status":"failed"}`. 007 проходит потому, что берёт все реквизиты сторон инлайн, а 002
      обогащается из профиля. **Перепроверить, когда их связь с soliq поднимется**; до тех пор
      002 покрыт только голден-тестами.
- [ ] P7.b: `LiveEscrowClient` — ждёт спеку банка
- [ ] P7.c: `LiveGovRegistryClient` — ждёт доступ к ПЦД

### Отклонения от плана (сверх девяти поправок к скелету)

1. **`escrow_service.mark` разделён на `_apply_mark` + две публичные двери**, а не «добавлен
   provider-путь рядом». Причина в тесте: любое правило, которое пишется дважды, однажды
   разойдётся — и разойдётся оно ровно на страже (`release_before_delivery`), которую live-рельс
   не должен обходить. Сигнатура и семантика `mark` не изменились, все тесты P3 остались зелёными.
2. **Опрос банка синтезирует callback-строку** и гоняет её через тот же `apply_provider_event`,
   вместо второго пути «опрос → отметка». Побочный выигрыш оказался важнее исходного мотива:
   `external_id = poll:{ref}:{status}` детерминирован, поэтому неразрешённое расхождение
   алертит **один раз**, а не каждые 30 минут.
3. **`amount_mismatch` / `currency_mismatch` — не было ни в скелете, ни в детализации.**
   Частичная оплата стоит открытым вопросом в `INTEGRATIONS.md` §2; принять её как `funded`
   значило бы сказать продавцу, что он получил всю сумму. Удержание.
4. **Починка эвалюатора R1 (`MAX_CHECK_ATTEMPTS`) — найденный при детализации латентный баг**,
   а не запланированная работа. `on_check_completed` считал `unavailable` «ещё не закончился»,
   `approve()` работает только из `pending_review` — значит чек, исчерпавший 5 ретраев, запирал
   кейс в `checks_running`, и **ручной путь перестал бы работать**. R1 уцелел лишь потому, что
   ни один его чек не мог стать `unavailable`; чеки P7.c могут. Это единственное изменение в
   чужом домене, и оно исправление, а не расширение (см. «Правила для всех подпланов»).
5. **`licenses`-снапшот не питает чек.** Лицензия проверяется против **вещества** в момент
   публикации (P5 `company_license_service`), а не против компании при онбординге. Строка всё
   равно полезна: это доказательство для того более позднего решения.
6. **CI-плейсхолдер для `ESCROW_WEBHOOK_SECRET` не добавлен** — сознательно. Правило «секрет →
   `.env.example` + CI в том же коммите» касается **обязательных** секретов; этот условно
   обязателен с пустым дефолтом, ровно как `ESKIZ_EMAIL`/`ESKIZ_PASSWORD`, которых в CI тоже
   нет. `Settings` без него не падает, тест вебхука патчит значение.
7. **Правка `deploy/.env.example` не сделана инструментом** (локальный запрет на `.env*`).
   Готовый блок для вставки лежит в `deploy/CLAUDE.md` — тот же путь, которым шли три
   E-IMZO-переменные R3.

### Что проверено в браузере (Playwright MCP, живой стек)

Изолированный стек (`polymer_r3_verify`, миграция 0029), дашборд на :3000.

**P7.b, вебхуком через curl по живому API:** без токена 401 · чужой токен 401 (и **ничего не
записано**) · `refunded` 200 · `released` 200 · повтор `refunded` 200 и **одна** строка ·
XML-тело 200 и `{"_raw": …}` с ключом-хешем. `sweep_provider_events` разобрал все три
(`{'held': 3}` — постановка задачи не долетела, и подметатель сделал именно то, ради чего
существует). Платёж остался `funded`: **ни одно** уведомление не применилось само.
В дашборде: счётчик «Расхождения с банком = 3», три строки с «Банк: X · У нас: Y» и читаемыми
причинами. Попытка отметить возврат без спора дала на экране `Сделка не может перейти в это
состояние. Для возврата средств сначала откройте спор по сделке.` — FR-D8 держится и тогда,
когда возврат заявил банк. После спора + ручной отметки строка ушла из списка сама, счётчик
3 → 2: самоочистка очереди подтверждена в браузере.

**P7.c, на карточке кейса `checks_running`:** внесение проверки «Компания (ЕГРПО)» со
скриншотом → снапшот с автором (`Сотрудник #1`), нормализованным payload, заметкой и рабочей
presigned-ссылкой (200, `image/png`) → чек «Госреестр: Пройдена», `name_matches: true` при
`ООО "ПОЛИМЕР ТРЕЙД"` vs `ООО «Полимер Трейд»` — нормализация кавычек и регистра работает на
живых данных. «Свидетельство НДС» с выключенной галочкой → **Предупреждение**, не отказ.
Повторная проверка с другим наименованием → Предупреждение, `name_matches: false`, попыток 2,
и в БД **три** строки снапшотов: история дописана, не перезаписана. «Лицензии» записались с
доказательством и без чека.

**Найдено в браузере (моё):** после успешной отправки формы `input[type=file]` продолжал
показывать имя прошлого скриншота, хотя React-состояние уже очищено, — поле обещало отправить
файл, который отправлен не будет. Имя показывает сам input, поэтому лечится только
перемонтированием (`key={fileKey}`); исправлено и проверено.

Демо-данные P2/P3 (сделка 1 `shipped`, платежи 1–2) восстановлены после прогона;
снапшоты P7.c оставлены как след проверки.

### Заведомые пробелы (подтверждены по факту)

- **P7.a (Didox) не начат** — единственный подплан, где ничего нельзя проверить: и клиент, и
  доменный шов, и polling пришлось бы писать против пересказа документации
  (`api-docs.didox.uz` программно не отдаётся — попытка сделана в этой сессии), а первая
  сверка с testapi3 перепишет маппинг полей. Детализирован до задач W8–W10 с четырьмя
  проектными решениями, которые от токена не зависят.
- **`LiveEscrowClient` / `LiveGovRegistryClient` не написаны.** Исходящие половины ждут спеку
  банка и доступ к ПЦД. Оба режима `live` **поднимают** `ProviderUnavailable` с текстом, что
  именно отсутствует, а не подставляют stub: заглушка вместо банка или госреестра — ровно то,
  чего этот шов не должен делать никогда.
- **e2e-спеки нет** — в репозитории по-прежнему нет e2e-пути к верифицированной компании
  (общий пробел P1–P6). Проверка ручная, браузерная.
- **Полный `pytest tests/` против реального Postgres даёт 12 падений** — те же, что и до P7:
  файлы, которые сами гоняют `downgrade base`/`upgrade head`, мешают друг другу в одном
  прогоне. Каждый guarded-набор зелёный по отдельности (что и есть их контракт); единственное
  реальное падение от P7 — тест P3, утверждавший заглушку `apply_provider_event`, — переписан.

---

## Сессия 21.08.2026 — перепроверка ИКПУ после починки на стороне Didox

Didox сообщили, что добавили наш ИНН в кеш. Перепроверка подтвердила: `productClassCodes`,
`vatRegStatus`, `bind` и `check/{code}` отвечают 200. Всё ниже проверено **в браузере**
(Playwright MCP) на живом стеке `pi-pg`/`pi-redis`/`pi-minio`, API :8000, портал :5173,
компания 31 (ИНН 312616547), `didox_mode=live`.

### Что окончательно выяснено про их справочник

- **`?search=` ищет ТОЛЬКО по корзине компании, а не по каталогу tasnif.** Доказано опытом:
  с одним привязанным кодом `search=вода`, `search=Негазированная`, `search=02201001` находят
  его, а `search=полиэтилен` и `search=ноутбук` — ноль. Глобального поиска в партнёрском API
  нет вообще, поэтому пустой ответ — **нормальное первое состояние**, а не тупик.
- **`check/{code}` работает только для уже привязанного кода** — иначе 422
  «танланган МХИКлар рўйхатида мавжуд эмас». Значит порядок «сначала bind, потом читаем строку
  обратно» — не удобство, а единственный рабочий порядок.
- **`origin` они не возвращают никогда** (в их же документации `05-profile.md` он `null`).
  Иначе и быть не может: один и тот же код для завода — «своё производство», для трейдера —
  «перепродажа». Это **ответ продавца**, а не свойство кода.
- Реальный код нашего рынка — `03901001001000000` «Полимер этилена (полиэтилен)», упаковка
  **тонна** (единственная). Ни у одного полимерного кода упаковок больше одной не нашлось.

### Три бага, найденные только в браузере, и все — наши

1. **Объявление с ИКПУ нельзя было опубликовать вообще.** Пикер отправлял `ikpu_origin: null`
   (брал его из ответа Didox, где его нет), а схема требует происхождение вместе с кодом →
   422 на «Опубликовать». Исправлено: в пикере появился выбор происхождения (4 значения ЭСФ),
   объявление не публикуется, пока продавец не ответил.
2. **Редактирование объявления стирало ИКПУ и условия письма.** `CompanyOfferOut` не отдавал
   семь полей (`ikpu_*`, `sample_letter_*`), форма гидратируется из него и PUT-ит черновик
   целиком — то есть правка описания молча обнуляла налоговую классификацию. Тест
   `test_the_read_schema_carries_every_field_the_write_schema_takes` теперь ловит весь класс
   ошибки: **любое** поле записи обязано быть в чтении.
3. **Селект упаковки был недостижим.** `choose()` обнулял `rows`, из которых он выводился, —
   у кода с несколькими упаковками сменить упаковку было нельзя. Выбранная строка переехала в
   собственное состояние + эффект догружает упаковки, когда код пришёл без поиска (правка
   объявления).

Плюс: `ikpu_synced_at` был колонкой, которую **никто никогда не писал**, — теперь ставится на
каждом сохранении с кодом и снимается вместе с ним.

### Закрыт пробел W8: у продавца не было управления письмом-обязательством

Бэкенд, колонки и валидация существовали с W8, а в портале не было ни галочки
«требовать письмо», ни поля с условиями — то есть фича была недостижима. Добавлены на лист 6
(«Условия продажи»), только при включённых образцах; «требую письмо» без текста условий не
пропускает дальше (то же правило, что и на бэкенде).

### Проверено в браузере, по шагам

Объявление #IMX-000093 (id 93) создано целиком через мастер: «Другое» → AI-проверка → 250 MT
по 1150 USD → SDS+TDS → без паспорта → образцы + «Готов подписать договор через D-Doc» →
ИКПУ `03901001001000000` + происхождение «Собственное производство» → «Опубликовать».
В Postgres: все пять `ikpu_*`, `ikpu_synced_at`, `accepts_contract=t`.
Затем правка (`/cabinet/offers/93/edit/*`): ИКПУ гидратировался с происхождением и пустым
полем поиска, письмо-обязательство включено с условиями, сохранение — и **ИКПУ на месте**
(до правки схемы этот же клик его обнулял).

Гейты: `pytest tests/` — **2067 passed, 759 skipped**; `ruff` и `mypy app` чисто; портал
`lint`/`typecheck`/`build` чисто; новая спека `e2e/ikpu-picker.spec.ts` — 3 из 3.
Два падения `e2e/p0-ui-kit.spec.ts` (choice tile, dropzone) **воспроизводятся на чистом
дереве** — они не из этой работы.

### Что осталось незакрытым

- **Didox `POST /v1/documents/offer/create` → 500 `Failed to store offer`** — воспроизводится
  и на их собственной странице регистрации. Пока он не работает, компания не может подписать
  их публичную оферту, а значит **создание документов 007/002 недоступно** независимо от
  нашего кода. По этой же причине в карточке онбординга нет кнопки «подписать оферту»:
  нажимать было бы не на что.
- **ЭСФ 002 → 422** и `GET /v1/profile` → 422 — тоже на их стороне, отчёт у них
  (`docs/didox-support-report.md`).
- **Покупательская половина письма-обязательства в браузере не пройдена**: объявление 93 в
  `pending_moderation`, а покупатель видит только одобренные. Следующий шаг — одобрить его в
  дашборде и пройти `pending_letter → подпись ЭЦП → requested` живьём.

### Сессия 21.08.2026, часть 2 — покупательская половина письма-обязательства

Проверка в браузере вскрыла, что **клиентской половины W8 не существовало вовсе**: строка
`pending_letter` не встречалась в портале ни разу — ни статуса, ни карточки письма, ни кнопки
подписи, ни клиента к трём готовым эндпоинтам. Бэкенд умел рендерить, выдавать challenge и
принимать подпись — и всё это было недостижимо из интерфейса.

Дополнительно нашлось: после отправки заявки покупателю показывали «**Запрос отправлен
продавцу**», хотя продавец его не видит и не увидит до подписи. Сообщение врало о состоянии и
не давало пути дальше.

**Что добавлено**

- Бэкенд: `SampleRequestOut` теперь несёт `letter_required` / `letter_signed_at` /
  `letter_number` (иначе список не может сказать, что подпись ещё должна), и появился
  `GET /portal/samples/{id}/letter/document` — PDF письма по presigned-ссылке, зеркало
  договорного маршрута. Рендер остался только на покупательском GET: продавец, открывший
  письмо, не должен уметь изменить байты, которые вот-вот накроет подпись.
- Портал: `SampleLetterCard` (обе стороны видят одно письмо; покупатель подписывает,
  продавец читает), статус «Ожидает подписи письма» в трёх локалях, честное подтверждение
  после отправки заявки со ссылкой на подписание. Кнопка подписи — существующая
  `EimzoSignButton`, второй диалог не заводился.

**Проверено в браузере, по шагам.** Продавец KRAEMER публикует объявление #94 с требованием
письма и своей формулировкой условий → одобрение в дашборде (админ, живой стек) → покупатель
IMEX запрашивает образец → заявка №13 ложится в `pending_letter`, продавцу **не** уходит
уведомление → карточка письма показывает условия продавца и PDF (200, `application/pdf`,
18 КБ) → подпись → статус `requested`, `letter_signed_at`, доказательство №36
(`purpose=sample_letter`, ИНН сертификата 312616547, PKCS#7 в S3), уведомление
`sample_request_new` продавцу той же секундой → у продавца во «Входящих» та же карточка с
«№ ПО-2026-000003, подписано», без кнопки подписи.

**Оговорка про подпись.** Лицензионного образа сайдкара UNICON на машине нет
(`EIMZO_SERVER_IMAGE` не задан), поэтому API работает с `EIMZO_STUB=true` и настоящий PKCS#7
здесь проверить нечем. Список сертификатов и выбор ключа шли через **живой** модуль E-IMZO
(`wss://127.0.0.1:64443` отвечает, реальные ключи видны), но сама верификация подписи прошла
через штатный dev-стаб — то есть криптография синтетическая, а всё серверное поведение
(гашение challenge, сверка ИНН, запись доказательства, переход статуса, уведомление)
настоящее. Ключа покупателя 590640341 на машине уже нет — остался только ключ IMEX, поэтому
сценарий развёрнут зеркально: продаёт KRAEMER, покупает IMEX.

Заодно починена сломанная спека `e2e/r3-eimzo.spec.ts`: `getByLabel(/pin/i)` стал матчить и
кнопку «Показать PIN-код», добавленную позже, — три теста падали на чистом дереве, теперь 3/3.

Гейты после всего: `pytest tests/` — **2069 passed, 759 skipped**, ruff и mypy чисто,
портал lint/typecheck/build чисто, `e2e/ikpu-picker` 3/3, `e2e/r3-eimzo` 3/3.

### Сессия 21.08.2026, часть 3 — 007 и 002 БЕЗ подписанной оферты

Гипотеза («оферта в тестовом контуре — формальность») подтвердилась наполовину, и это
оказалось самой ценной проверкой за весь трек.

**Создание работает без оферты.** Реальный договор `007` создан нашим же билдером и шлюзом из
живых данных БД: `_id 695fbfae9d6f11f1ac6f1ebdd6719e71`, `contractid 6a88654e0a7a365442b419cb`,
статус 0. Payload принят целиком — PascalCase внутрь, lowercase обратно, ИКПУ
`03901001001000000`, упаковка «тонна», НДС 11 500 + 1 380 = 12 880. То есть **W4 (билдеры) и W3
(шлюз) проверены на живом API**, а не только голденами.

**Отправка без оферты не работает — и это их бизнес-правило, а не баг.**
`PUT /v1/documents/{id}/send` отвечает прямым текстом: «Для успешной отправки документов,
необходимо подписать условия публичной оферты на сайте didox.uz». Это первый и единственный
внятный отказ во всей цепочке.

**Подпись настоящая.** Байты документа подписаны живым модулем E-IMZO в браузере ключом
312616547 (PKCS#7 6 112 симв., signature_hex 128) и приняты их TSA — `timeStampTokenB64`
9 648 симв. Значит связка W1 (мост) + `to_sign_bytes` + `/v1/dsvs/timestamp` рабочая целиком.

**Четыре дефекта на их стороне, теперь с точными телами ответов** (все в
`docs/didox-support-report.md`, переписанном под них):

1. `offer/create` → `500 Failed to store offer` — **не зависит от размера и содержимого**
   (проверено на 499 КБ, 10 КБ и 78-байтовом PDF).
2. Тот же эндпоинт JWT-верифицирует значение `Partner-Authorization`, поэтому обязательный
   для остального API префикс `Bearer` его роняет (`JsonWebTokenError`). Наш клиент шлёт токен
   без префикса, так что мы совпали случайно.
3. `POST /{id}/sign` → `500 Undefined variable $isDraft` — одинаково на настоящей подписи с
   меткой, на сыром PKCS#7 и на строке `"AAAA"`; их же 405 назвал рабочий маршрут `PUT /send`.
4. `vatRegStatus` резолвит **только** 312616547; для 590640341 и шести других ИНН — их
   `{"status":"failed"}`, и это не зависит от того, чьим ключом спрашивать. Отсюда
   `422 Failed to check vatRegStatus` на 002. Тот же payload с покупателем 312616547
   создаётся (`_id b4ad11c89d6f…`) — значит падает проверка контрагента, а не наше тело.

**Что из этого попало в код (TDD, тесты писались первыми):**

- `DidoxClient.send_document` — `PUT /v1/documents/{id}/send`, правильная дверь для черновика.
- `submit_signature` разводит два разных действия: свой черновик уходит через `send`, чужой
  документ принимается через `sign` (и только после `join_signatures`). Раньше обе ветки шли в
  `/sign`, который для черновика 500-ит.
- `DidoxError.offer_not_signed` теперь распознаёт настоящий текст отказа («публичной оферты»).
  До этого он проверял поле `context.offer == "required"`, которого Didox не присылает никогда,
  — то есть флаг не срабатывал бы ни разу.

Гейты: `pytest tests/` — **2073 passed, 759 skipped**, ruff и mypy чисто.

### Сессия 21.08.2026, часть 4 — недостающая дверь: договор → 007

Аудит показал, что `edi_service.create_document` **не вызывает никто**: рельс Didox был собран
целиком — шлюз, билдеры, двухходовая подпись, поллер, админка — но шага «превратить наш договор
в их документ» не существовало. Из-за этого `signing_provider='didox'` был флагом без последствий,
а ветка подписи в портале — недостижимым кодом. Хуже: `signing_provider` вообще отсутствовал во
входной схеме, так что поставить договор на этот рельс было нельзя ни через какой API.

**Что добавлено**

- `edi/contract_docs.py` — сборка 007 из договора. Три решения, у каждого есть правдоподобный
  неверный вариант: **владелец документа — продавец, а не инициатор** (ЭСФ выставляет продавец и
  цитирует номер этого документа); **текст берётся из уже отрендеренного договора** (разделы `<h2>`
  вынимаются как `Parts` — второй источник правды для текста, который обе стороны подписывают,
  недопустим); **ничего не выдумывается** — нет ИКПУ, нет подтверждённой личности, пустое тело →
  исключение, а не значение по умолчанию.
- Маршруты `GET`/`POST /portal/companies/{id}/didox/contracts/{contract_id}/document`. Prefill
  возвращает **все** блокеры сразу: продавец, который узнаёт о трёх проблемах за три захода,
  каждый раз вводя пароль от ключа, справедливо решит, что фича сломана.
- `signing_provider` в `ContractCreateIn` + сервис + роутер, и селектор «Как подписываем» на
  экране создания договора. Тест ловит и то, что роутер поле не теряет.
- `DidoxDocumentCard` в портале: у продавца кнопка, у покупателя — состояние; карточка исчезает,
  как только документ создан, и управление переходит к подписи.

**Попутно найдено и исправлено:** у **уже верифицированной** компании не было способа подтвердить
личность через ЭЦП — условие в UI требовало статус `draft`/`pending_verification`. На бэкенде
такого ограничения никогда не было. То есть любая компания, верифицированная до появления рельса
Didox, не могла отправить ни одного документа: ПИНФЛ и ФИО подписанта обязательны.

**Проверено в браузере, целиком.** Продавец IMEX: сессия Didox настоящим ключом → подтверждение
личности обеих сторон → договор №26 создан **на рельсе didox** (в БД `signing_provider=didox`) →
отправлен → покупатель KRAEMER (уже со своего аккаунта) принял → продавец нажал «Создать документ
в Didox» → **реальный документ у оператора**: `didox_id f37ba38e9d82…`, `contractid 6a8886160a7a…`,
владелец 31, партнёр 30. В payload: реквизиты с ПИНФЛ обеих сторон, три раздела договора дословно,
позиция с ИКПУ `03901001001000000` и НДС. Карточка исчезла, появилась кнопка подписи по рельсу
Didox (`contract-sign-didox`).

**Замечено, не чинил (нужно решение):**

- **Роль в договоре считается по всем компаниям аккаунта, а не по активной.** Пока обе тестовые
  компании принадлежали одному аккаунту, покупатель видел действия инициатора, а `accept` отвечал
  `only_counterparty`. Развёл компании по разным аккаунтам — всё встало на место. В проде это
  редкость, но пользователь, состоящий в обеих сторонах сделки, увидит не свои кнопки.
- **В списке шаблонов договора предлагается шаблон письма-обязательства** (`kind='sample_letter'`).
  Договор из него составить нельзя, а выбрать — можно.

Гейты: `pytest tests/` — **2088 passed, 759 skipped**, ruff и mypy чисто; портал lint/typecheck/
build чисто.

### Документация приведена в соответствие

- `backend/CLAUDE.md`: цепочка миграций была «`0001`→`0034`» при head `0041` — переписана с
  описанием, что именно принесла `0041`; в карту доменов добавлен `edi/` (их 21, а не 20);
  раздел про Didox дополнен Stage 2 — четыре факта, добытые на живом контуре (создание не требует
  оферты, а отправка требует; `/sign` 500-ит на черновике и вместо него `PUT /send`; `vatRegStatus`
  отвечает только про свою компанию; поиск ИКПУ идёт по корзине, а `origin` они не возвращают).
- `portal/CLAUDE.md`: в карту `features/` добавлены sample-letter, didox-session, didox-sign,
  didox-contract-document, ikpu-picker; новый раздел про E-IMZO и рельс Didox — чей `window.CAPIWS`,
  почему `signBase64` не роскошь, и что верификаторов **два** (Didox против нашего сайдкара).
- `deploy/CLAUDE.md`: отмечено, что Stage 2 **не добавляет переменных** — он включается рантайм-
  настройкой `didox_mode`, и перечислены два условия, без которых включать бессмысленно
  (подписанная оферта и подтверждённые личности с обеих сторон).

### Перепроверка 24.08.2026 — «исправили» не подтвердилось, зато нашёлся наш баг

Didox сообщили, что дефекты исправлены. Прогнали всё заново **обоими** аккаунтами
(`312616547` — тот, что регистрировали на их сайте, и `590640341`): `offer/create` →
`500 Failed to store offer`, `/sign` → `500 Undefined variable $isDraft`, `vatRegStatus` →
`failed` для любого ИНН кроме своего, `PUT /send` → тот же 422 «подпишите публичную оферту».
Единственное отличие — у `GET /v1/profile` сменился текст ошибки (`Failed to get tin info` →
`Failed to get Phis By Tin Info info`), статус тот же. Отчёт для поддержки дополнен таблицей
«было/стало» и просьбой уточнить, что и на каком контуре правили.

**А вот наш собственный баг эта проверка вскрыла.** Портал утверждал «E-IMZO не найден», хотя
модуль работал и отвечал на прямые вызовы. Причина: `handshake()` мемоизирует промис — и
мемоизировал его **вместе с провалом**. Кто открыл кабинет раньше, чем запустил E-IMZO, получал
«модуль не найден» до конца жизни страницы: каждая следующая попытка переиспользовала уже
отклонённый промис. `resetHandshake` не спасал — он срабатывает только на отказ по api-key, а не
на транспортную ошибку. Лечится одной строкой: провал не запоминаем.

Диагностика заняла время, потому что две правдоподобные гипотезы оказались неверными, и хорошо,
что я их проверил, а не «починил»: параллельные сокеты (два хранилища `pfx`/`ytks` опрашиваются
через `Promise.allSettled`) модуль переносит спокойно, и handshake-подряд-с-вызовом — тоже.

Два теста на фейковом сокете (`e2e/eimzo-bridge.spec.ts`): вызов, упавший пока модуль лежал,
проходит со второй попытки; мост восстанавливается **без перезагрузки страницы**. Проверено и
живьём: после правки `probe()` вернул true и список из пяти реальных ключей, сессия Didox для
312616547 выдана настоящей подписью.

Статус рельса не изменился: 007 создаётся, подписывается, штампуется — и не отправляется,
потому что оферту невозможно подписать. Ждём Didox.

### Подсказка Didox про `taxIdOrPinfl` (24.08.2026)

Поддержка: «в запросе `offer/create` заполните `taxIdOrPinfl` значением ИНН вашей организации».
Проверено — **не помогает**, ответ прежний `500 Failed to store offer`. Прогнано обоими нашими
аккаунтами и во всех разумных вариантах: `taxIdOrPINFL` / `taxidorpinfl` / `TaxIdOrPinfl` / `tin` /
`taxId`, JSON и multipart, полный PDF и 12-символьный. Без `document` их валидатор отвечает
`422 «Поле document обязательно»` — то есть поле он принимает, а падает **после** валидации, на
сохранении. Их же следующий шаг это подтверждает: `POST /v1/documents/offer/sign` отвечает
`422 «Document not found»` — подписывать нечего, потому что создание ничего не сохранило.

Поле всё равно добавлено в клиент: они его просили, валидатор его принимает, стоит это ноль.
Заодно закреплены тестами две особенности этого эндпоинта, обе стоили времени:

- **`Partner-Authorization` шлётся голым токеном.** Это единственный эндпоинт, который
  JWT-верифицирует само значение заголовка, поэтому префикс `Bearer`, который остальной API
  спокойно принимает, ломает его в `500 JsonWebTokenError`. Наш `_headers()` не добавлял префикс
  никогда — мы совпали случайно, и теперь тест не даст «исправить» это обратно.
- **`taxIdOrPinfl` — ИНН действующей компании**, прокинут через `offer_to_sign` из уже известного
  роуту `company.tax_id`.

Гейты: `pytest tests/` — 2090 passed, ruff и mypy чисто.

### 25.08.2026 — ОФЕРТА ПОДПИСАНА. Рельс открылся

Didox добавили карточку компании `312616547` в свой справочник — и `offer/create` заработал
с первого раза. Вся цепочка пройдена ключом `DSDEV3126165470001`:

```
auth_by_eimzo (подпись над ИНН + timestamp)   → user-key
GET  /v1/newoffer/base64                      → 200, 499 284 симв.
POST /v1/documents/offer/create               → 200  ← ГЛАВНОЕ
     buyer: {"name":"\"IMEX INDUSTRIAL GROUP CA\" MCHJ",
              "address":"Gʻani Aʼzamov MFY, Gʼalaba koʻchasi, 9-uy"}
     documentid 3a9c996aa04b11f1bf5c1ebdd6719e71
POST /v1/dsvs/timestamp (подпись над document_json) → 8 536 симв.
POST /v1/documents/offer/sign                 → 200 {"status":"success"}
```

Блок `buyer` подтверждает диагноз от 24.08: эндпоинт читает карточку компании из их
справочника, и `Failed to store offer` был не про наш запрос, а про то, что нашего ИНН там
не было. Добавили — заработало.

**Что изменилось сразу после подписания оферты** (всё проверено живьём, тем же ключом):

| эндпоинт | 21–24.08 | 25.08, после подписи оферты |
|---|---|---|
| `POST /{id}/sign` | `500 Undefined variable $isDraft` | **валидирует подпись**: на мусор — `Eimzo fail: CMSException`, на настоящую — бизнес-проверки |
| `PUT /{id}/send` | 422 «подпишите публичную оферту» | `422 Неподдерживаемый тип документа` (для 007) |
| `tosign` action=send/create | 500 | `500 Unsupported action` (то же), `accept` → 200 |

То есть `Undefined variable $isDraft` был **симптомом неподписанной оферты**, а не отдельным
багом, и вчерашняя маршрутизация «черновик уходит через `PUT /send`» стала неверной.
`submit_signature` возвращён на `POST /{id}/sign` для обеих сторон (разница только в
`join_signatures` для входящего), `send_document` оставлен в клиенте с описанием, почему он
существует и почему им никто не ходит. Тесты переписаны под новое знание.

**Где стоим сейчас — и это уже содержательные проверки роуминга, а не ошибки интеграции:**

* покупатель `302936161` (реальный ИНН из их документации) → `422 [03901001001000000] не
  включены в список избранных ИКПУ`. Наш код в нашей корзине есть (`productClassCodes` его
  отдаёт, `check/{code}` подтверждает), значит претензия к корзине **контрагента**;
* покупатель `590640341` (наш аккаунт по паролю) → `422 ИНН/ПИНФЛ заказчика некорректный` —
  его нет в их справочнике;
* наполнить корзину этого аккаунта нельзя: `bind` отвечает `ПИНФЛ киритилмаган` — у аккаунта
  по паролю нет ЭЦП-личности.

**Чего не хватает для сквозной отправки:** второй настоящей компании — ИНН, который есть в
справочнике Didox, которым мы управляем, и у которой есть ключ E-IMZO (чтобы привязать ИКПУ).
Ключ KRAEMER удалён, в модуле остались только два ключа на `312616547`.

Гейты: `pytest tests/` — 2090 passed, ruff и mypy чисто.

## Сессия 25.08.2026 — рельс 007 пройден целиком из интерфейса; остался чужой блокер

База снесена под ноль по просьбе заказчика («тестировать только на том, что существует
в реальности»). Всё, что ниже, сделано **в живом браузере через chrome-devtools MCP**, кликами
по реальному интерфейсу, а не скриптами.

**Две стороны заведены заново, разными аккаунтами.** `ooo treadway inc` (562353400,
`+998901112233`) — покупатель; `dev_imex industrial group ca mchj` / `"IMEX INDUSTRIAL GROUP CA"
MCHJ` (312616547, `+998901112244`) — продавец. Обе прошли мастер регистрации, подтверждение
личности ЭЦП, штабную верификацию в дашборде и получили статус `verified`.

**Что впервые отработало живьём:**

* **онбординг Didox из карточки компании** — `signup` по ЭЦП и подписание публичной оферты,
  оба настоящим ключом; `didox_companies` получил `signup_at` + `offer_signed_at`, user-key лёг
  в Redis. Раньше оферта была блокером №1;
* **ИКПУ-подборщик** — корзина пустая → «Добавить код» → `bind` → чтение обратно с упаковкой
  «тонна». Код взят настоящий, из `tasnif.soliq.uz` (ТИФ ТН 3902 → позиция `03902001` →
  `03902001002000002` «Полимер пропилена (полипропилен) марки 21012, 21020, 21030, 21060»);
* **госреестр при регистрации** — Didox подставил адрес, дату регистрации, ОКЭД 46900 и статус
  «Действующие и имеющие налоговые обязательства» для 312616547;
* **дверь договор → 007** — `POST /v1/documents/007/create/ru` → `_id
  1f4ff286a06611f1984c1ebdd6719e71`. Payload собрался правильно: настоящие ПИНФЛ/ФИО подписанта,
  ОКЭД из снимка реестра, банковские реквизиты, ИКПУ с упаковкой, а `Parts` подняты из `<h2>`
  отрендеренного договора;
* **`POST /v1/dsvs/timestamp`** — 200 на настоящей подписи. Эта нога до сегодня ни разу не
  проходила живьём.

**Где встали:** `POST /v1/documents/{id}/sign` → `422 ИНН/ПИНФЛ заказчика некорректный.
ИНН/ПИНФЛ: 562353400`. Проверка `utils/info` по всем доступным ключам: в реестре Didox есть
**только** 312616547; 562353400, 514890380, 575013376, 537302090 и наш 590640341 — пустой конверт.
Двусторонний документ невозможен, пока Didox не даст вторую валидную сторону. Просьба оформлена
в `docs/didox-support-report.md`.

**Отменённое решение 21.08.** Тогда исходящий черновик отправлялся через `PUT /{id}/send`, потому
что `/sign` отвечал `500 Undefined variable $isDraft`. Сегодня всё наоборот и совпадает с их
документацией (§9): `/sign` работает, а `/send` для 007 отвечает `422 Неподдерживаемый тип
документа`. Тот 500 был симптомом неподписанной оферты. `submit_signature` вернулся на
`sign_document`; `send_document` оставлен на клиенте для типов, которые им пользуются.

**Две ошибки интерфейса, которые нашлись только кликами** (обе исправлены):

* кнопка подписания на рельсе Didox переиспользовала строку `contracts.actions.sign` —
  «Подписать через **E-IMZO**». Подписант не мог отличить, какой из двух рельсов подписывает.
  Добавлен ключ `didox.signAndSend` в ru/uz/en;
* приём условий контрагентом не сбрасывал кэш префилла Didox, поэтому продавец продолжал читать
  «договор ещё не отправлен на подпись» до перезагрузки страницы. `act()` в `ContractDetailPage`
  инвалидирует `["didox","contract-prefill"]`.

**Ограничение локального контура, зафиксировано отдельно:** `EIMZO_STUB=true` и образа сайдкара
UNICON здесь нет, поэтому НАШУ подпись настоящим ключом проверить локально нельзя — `_stub_verify`
не разберёт настоящий PKCS#7. Подтверждение личности обеих компаний сделано через штатный
dev-стаб, но **с настоящими данными сертификата** (список сертификатов брался из модуля, синтетической
была только криптография). Всё, что проверяет Didox, подписано по-настоящему.

**Открытый вопрос к бизнесу, не к коду:** в 007 ушло `HasVat: true` и ставка 12%, тогда как
`vatRegStatus`/снимок реестра говорят, что 312616547 **не** плательщик НДС. Для договора это
коммерческое условие, но для ЭСФ `002` расхождение недопустимо — ставку надо брать из статуса
плательщика, а не из умолчания объявления.

### Правка 25.08.2026 — отказ оператора теперь виден, и приходит до ключа

Вопрос «почему 422» имел два ответа, и оба были нашими недоработками, а не Didox.

**422 ставим мы сами** — `_provider_error` в `edi/api_portal.py` переводит их 4xx в наш ответ
`{"error": "didox_rejected", "message": …}`. Код честный: запрос действительно необрабатываемый.
Плохо было другое — **текст отказа не доезжал до человека**. `useDidoxSign` сводил всё
нераспознанное к `failed`, а `ApiError.detail` хранит ВЕСЬ ответ, поэтому наш объект лежит на
уровень глубже (`detail.detail`): чтение `detail.error` возвращало `undefined` и молча падало в
общую строку «Не удалось подписать документ». Ловушка невидима для типов — `detail` объявлен как
`unknown`. Теперь: новая ветка ошибки `rejected`, `errorMessage` наружу из хука, строка
`didox.signErrors.rejected` в ru/uz/en, и `description` (их собственное «что делать») в приоритете
над `message`. Прибито тестом `e2e/didox-rejection.spec.ts`, который собирает НАСТОЯЩИЙ `ApiError`
с настоящей обёрткой FastAPI.

**Отказ приходил после пароля.** `ИНН/ПИНФЛ заказчика некорректный` — состояние, которое известно
заранее: один вызов `GET /v1/utils/info/{tin}`. Добавлен `_counterparty_blocker` и блокер
`counterparty_unknown` в префилл, рядом с остальными, — ровно за этим `_prefill` и заведён
(«три недостающие вещи за три захода, каждый после загрузки ключа, читаются как сломанная
функция»). Правила: спрашиваем только про ПОКУПАТЕЛЯ (нас Didox знает, иначе не выдал бы user-key);
не спрашиваем, если документ уже создан; **сбой самого справочника — молчание, а не вердикт**, иначе
наш простой превратится в обвинение реальной компании (тот же запрет, что уже держат
`StubGovRegistryClient` и `CompanyNotFound`).

Проверено в живом браузере: на новом договоре с тем же контрагентом карточка показывает
«ИНН контрагента не найден в справочнике оператора ЭДО» ДО всякой подписи. Отрисовку самого
текста отказа на экране подтвердить живьём не удалось — для этого нужен ввод пароля в модуле;
проверены обе половины по отдельности: разбор ответа (браузерным тестом с настоящим `ApiError`)
и подстановка в строку (через реальный экземпляр i18n на странице).

Гейты: `ruff` чисто, `mypy` 309 файлов, `pytest tests/` 2099 passed; портал `lint`/`typecheck` чисто.

### 25.08.2026, вечер — на ИНН самого Didox шлагбаум сдвинулся на следующий

Взяли контрагентом их собственную демо-компанию `310529901` «DIDOX TECH» (есть в реестре, как и
`302936161` «VENKON GROUP»; все четыре тестовых ключа E-IMZO и наш `590640341` — нет).

**Что это дало.** Отказ «ИНН/ПИНФЛ заказчика некорректный» **исчез**. Заодно проба показала, что
`Clients[0].FizTin`/`.Fio` можно оставить ПУСТЫМИ — Didox создал документ
(`71f711f8a07611f1b4b01ebdd6719e71`). То есть личность покупателя оператору не нужна, и наше
правило «личность обязательна у обеих сторон» строже, чем требует рельс. Правку не делали —
это продуктовое решение, оставлено открытым вопросом (см. ниже).

**Следующий шлагбаум.** `POST /{id}/sign` → `422 [03902001002000002] не включены в список
избранных ИКПУ!`. Проверили адресно, `productClasses/check/{code}`: у продавца `312616547` код
есть (`[("1644530","тонна")]`), у покупателя `310529901` — `танланган МХИКлар рўйхатида мавжуд
эмас`. Значит, **договор уходит только тому контрагенту, кто сам заявил этот ИКПУ у себя в Didox**.
Чужую корзину пополнить нельзя, поэтому добавлен второй предварительный блокер
`counterparty_ikpu_missing` рядом с `counterparty_unknown` — показать можно, починить нельзя.
Вопрос Didox (в отчёте): задумано ли это для типа `007`, ведь покупатель не обязан заранее
декларировать чужую номенклатуру.

**Ещё две находки по дороге, обе наши.**

* `get_document(owner=True)` уходило на провод как `owner=true`, и Didox отвечал голым 500. `bool`
  для mypy — валидный `int`, так что типы это пропускают. Теперь `int(owner)` в клиенте плюс тест.
  Полчаса диагностики ушло на то, что выглядело как их сбой.
* «Ввод пароля отменен» показывалось как «Не удалось подписать документ». Все сегодняшние тупики с
  подписью были именно закрытым окном модуля. Добавлены `CapiwsError.isCancelled`, ветка `cancelled`
  в `useDidoxSign` и строка, которая прямо говорит: окно закрыто, нажмите ещё раз и введите пароль.

**Открытый вопрос (продуктовый, не технический):** снимать ли требование подтверждённой личности с
ПОКУПАТЕЛЯ. За: Didox её не требует; контрагент, который не наш пользователь, никогда не заведёт у
нас `company_person_data`; на этом рельсе мы и так никогда не видим его PKCS#7 (потому и не пишем
`contract_signatures`). Против: сейчас правило гарантирует, что обе стороны на документе —
проверенные нами компании. Факты зафиксированы тестами в `test_edi_buyer_identity_optional.py`,
само правило не тронуто.

Гейты: `ruff` чисто, `mypy` 309 файлов, `pytest tests/` 2109 passed; портал `lint`/`typecheck` чисто.

### 27.08.2026 — Didox снял шлагбаум по ИКПУ; за ним стоял следующий, и он опроверг наш вчерашний вывод

Didox сообщил, что корзина покупателя ни при чём — достаточно добавить код в
`https://dev-s0.rouming.uz/faktura/uz/tools`. Проверили на том же пробном документе
`71f711f8a07611f1b4b01ebdd6719e71` (007 на их `310529901`), настоящим ключом `312616547`.
Одного `load_key` хватило на обе подписи — вход и документ — потому что `openSession` держит
ключ открытым; пароль спрашивают один раз.

**Ошибка по ИКПУ ушла.** При этом в корзине `310529901` кода по-прежнему нет
(`productClasses/check` отвечает «танланган МХИКлар рўйхатида мавжуд эмас»), то есть корзина
получателя действительно была ни при чём — проверка живёт в роуминге.

**За ней открылся следующий отказ:**

```
POST /v1/documents/{id}/sign → 422
  ПИНФЛ уполномоченного физ.лица заказчика не указан!;
  ФИО уполномоченного физ.лица заказчика не указано!
```

**Это опровергает вчерашний вывод, и его надо назвать прямо.** 25.08 мы записали, что «личность
покупателя оператору не нужна», потому что `create` принял документ с пустыми
`Clients[0].FizTin`/`Fio`. Вывод был сделан по неверному эндпоинту: `create` эти поля не
валидирует, `sign` — валидирует. Наше правило «личность обязательна у обеих сторон» верно;
неверным было его обоснование (мы выводили его из обязательности `Owner.FizTin`, то есть про
продавца). Открытый вопрос «снимать ли требование с покупателя» закрыт: **не снимать**.

Практическое следствие жёсткое: контрагентом на документе Didox может быть только компания, чью
личность подписанта мы держим у себя, — то есть **верифицированный пользователь нашего кабинета**,
а не просто ИНН из налогового реестра. `310529901` есть в реестре, но её подписанта мы не знаем и
выдумывать не станем; `562353400` подписанта имеет, но её нет в реестре. Просьба к Didox
переформулирована соответственно.

Файл `test_edi_buyer_identity_optional.py` переименован в `..._required.py`, докстрока
переписана — он документировал опровергнутый вывод.

Обобщение, которое стоит помнить: **провайдер, который валидирует одно и то же тело на двух шагах,
научит вас неправде, если вы дошли только до первого.**
