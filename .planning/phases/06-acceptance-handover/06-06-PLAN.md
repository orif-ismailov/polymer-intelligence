---
phase: 06-acceptance-handover
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-05"]
files_modified:
  - .planning/phases/06-acceptance-handover/06-ACCEPTANCE.md
  - docs/deployment-guide.md
  - docs/admin-guide-ru.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "06-ACCEPTANCE.md has one row per TZ §6.1.1–§6.1.6 with procedure, current automated-proxy evidence, the blocked-on customer input, and a sign-off line"
    - "06-ACCEPTANCE.md consolidates and supersedes the deferred deploy-time UAT items from 02-UAT/03-UAT/05-UAT into one deploy-day checklist"
    - "The deployment guide stands the system up from scratch: prerequisites, env/secrets matrix, TLS via certbot, first-run migrate+seed, aiogram webhook + userbot session, backup cron"
    - "The admin guide (Russian) covers add-source (site + channel), alert-rule builder, needs_review queue, token-budget monitoring"
    - "Neither doc leaks real secrets; the secrets matrix uses placeholders and instructs on credential storage/rotation"
  artifacts:
    - path: ".planning/phases/06-acceptance-handover/06-ACCEPTANCE.md"
      provides: "Consolidated Phase-1 customer-facing sign-off spine (D-01)"
      contains: "Blocked on"
    - path: "docs/deployment-guide.md"
      provides: "First-run deployment guide — secrets matrix, TLS, webhook/userbot setup, backup cron (D-05.2, English)"
      contains: "certbot"
    - path: "docs/admin-guide-ru.md"
      provides: "Russian non-developer operator guide (D-05.3)"
      contains: "Telegram"
  key_links:
    - from: ".planning/phases/06-acceptance-handover/06-ACCEPTANCE.md"
      to: "the executable proofs (06-02/06-03/06-05) + existing green proxies"
      via: "cites the passing test/harness as automated evidence per §6.1.x"
      pattern: "pytest|smoke|restore"
    - from: "docs/deployment-guide.md"
      to: "deploy/docker-compose.yml"
      via: "first-run stands up the production compose"
      pattern: "docker-compose.yml"
---

<objective>
Author the three documentation artifacts whose content depends on the executable proofs landing
first: the consolidated `06-ACCEPTANCE.md` (D-01), the deployment guide (D-05.2, English), and the
admin guide (D-05.3, Russian). The acceptance doc is the customer-facing Phase-1 sign-off spine —
one row per TZ §6.1.1–§6.1.6 with the exact procedure, the current automated-proxy evidence already
green, the specific customer input each live drill is blocked on, and a sign-off line — and it
CONSOLIDATES and SUPERSEDES the deferred deploy-time UAT items from 02-UAT / 03-UAT / 05-UAT into a
single deploy-day checklist.

Purpose: satisfies Phase-6 SC#1 (acceptance items pass on review, with the customer-gated portions
documented as deploy-day steps) and SC#4 (deployment + admin docs handed over). These docs cite the
06-02 restore drill, the 06-03 channel-close test, the 06-05 smoke, and the existing green proxies as
evidence — so they run AFTER those proofs (wave 3).

Output: `06-ACCEPTANCE.md`, `docs/deployment-guide.md`, `docs/admin-guide-ru.md`. (D-06 language
split: acceptance + deployment guide in English; admin guide in Russian.)
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `.planning/phases/06-acceptance-handover/06-ACCEPTANCE.md` — NEW consolidated sign-off spine
- `docs/deployment-guide.md` — NEW first-run deployment guide (English)
- `docs/admin-guide-ru.md` — NEW Russian operator guide
</artifacts_produced>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acceptance-handover/06-CONTEXT.md
@.planning/phases/06-acceptance-handover/06-PATTERNS.md
@.planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md
@.planning/phases/02-ingest-core-uzex/02-UAT.md
@.planning/phases/03-client-circuit/03-UAT.md
@.planning/phases/05-telegram-monitoring-ai/05-UAT.md
@docs/polymer-intelligence-tz.md
@docs/runbook-backup-restore.md
@deploy/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-ACCEPTANCE.md — one row per TZ §6.1.1–§6.1.6 + consolidated deploy-day checklist</name>
  <files>.planning/phases/06-acceptance-handover/06-ACCEPTANCE.md</files>
  <read_first>
    - .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md (FULL file — the per-criterion
      section structure: Full criterion → Automated CI Proxy (bash + coverage) → Deploy-Time Live
      Drill (numbered) → Pass criteria; the condensed checklist; the Deferred Deploy-Time UAT table)
    - docs/polymer-intelligence-tz.md §6 lines 173-181 (the verbatim §6.1.1–§6.1.6 criteria) and §7
      lines 190-196 (risk allocation — userbot account/AI-quality thresholds borne by customer, used
      to word the sign-off/blocked-on conditions)
    - .planning/phases/02-ingest-core-uzex/02-UAT.md, 03-UAT.md, 05-UAT.md (the deferred deploy-time
      UAT items being consolidated — extract each into the single deploy-day checklist)
    - .planning/STATE.md Deferred Items table (the Phase-2/3 deferred UAT rows + their prerequisites
      and CI gates — the evidence anchors)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the 06-ACCEPTANCE.md adaptation notes:
      add a "Blocked on" column, consolidate the per-phase UAT, retire SC#5 caveat once D-03 passes)
  </read_first>
  <action>
    Create `.planning/phases/06-acceptance-handover/06-ACCEPTANCE.md` following the
    `04-ACCEPTANCE.md` structure but keyed to TZ §6.1.1–§6.1.6 (six items, not five SCs). For EACH
    item produce: the verbatim TZ criterion; the current automated-proxy evidence already green
    (cite the SPECIFIC passing test/harness — §6.1.1 request≤10s/notify≤30s →
    `test_request_sla.py` 4/4; §6.1.2 UZEX accuracy → the accuracy harness 100%/55 positions;
    §6.1.3 channel recall/precision → `tests/parsing/test_telegram_accuracy.py` 100/100 on
    fixtures; §6.1.4 source isolation+alert → the 06-05 smoke + `test_source_failure_alert.py`;
    §6.1.5 restore ≤2h → the 06-02 `test_restore_local.sh` measured wall-clock; §6.1.6 admin adds
    site+channel / failed-test can't enable → 06-03 `test_telegram_channel_close.py` + the
    html_table/rss wizard end-to-end from Phase 4); the exact deploy-time live-drill steps; a
    NEW "Blocked on customer input" column/line naming the specific gating input (real
    BOT_TOKEN+public HTTPS for §6.1.1; live source-failure on the VPS for §6.1.4; real userbot
    account/session+live channel for §6.1.6 ingestion; customer 100-message sample+synonyms+trader
    sign-off for §6.1.3 80/85; restore rerun on the real VPS for §6.1.5); and a sign-off line.
    Add a single CONSOLIDATED "Deploy-Day Checklist" section that supersedes the per-phase deferred
    UAT items from 02-UAT/03-UAT/05-UAT (no parallel per-phase lists). Mark the §6.1.6
    telegram-channel slice as CLOSED locally (cite 06-03) with only live-account ingestion remaining
    deploy-day. Status header: "Pending deploy-time customer-gated verification" with the locally-proven
    items marked green. Do NOT execute the customer-gated drills here — they are documented rows only.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-acceptance-handover/06-ACCEPTANCE.md && for n in 6.1.1 6.1.2 6.1.3 6.1.4 6.1.5 6.1.6; do grep -q "$n" .planning/phases/06-acceptance-handover/06-ACCEPTANCE.md || { echo "MISSING $n"; exit 1; }; done && grep -qi 'Blocked on' .planning/phases/06-acceptance-handover/06-ACCEPTANCE.md && grep -qi 'Deploy-Day Checklist\|deploy-day' .planning/phases/06-acceptance-handover/06-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-ACCEPTANCE.md` exists and contains a section/row for each of §6.1.1, §6.1.2, §6.1.3,
      §6.1.4, §6.1.5, §6.1.6
    - Each item cites a SPECIFIC automated-proxy evidence artifact (a named test/harness/smoke/restore)
    - The doc has a "Blocked on" column/line naming the customer input each live drill awaits
    - A single consolidated deploy-day checklist supersedes the 02-UAT/03-UAT/05-UAT deferred items
      (the doc references all three prior UAT sources as consolidated-here)
    - §6.1.6 is marked closed-locally citing 06-03, with only live-account ingestion remaining deploy-day
    - The doc contains no real secret values
  </acceptance_criteria>
  <done>06-ACCEPTANCE.md is the single customer-facing sign-off spine: one row per §6.1.x with procedure, cited green-proxy evidence, the blocked-on customer input, and a sign-off line, consolidating the prior per-phase UAT into one deploy-day checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/deployment-guide.md (English) — first-run, secrets matrix, TLS, webhook/userbot, backup cron</name>
  <files>docs/deployment-guide.md</files>
  <read_first>
    - docs/runbook-backup-restore.md (the section structure: TOC, numbered steps, fenced exact
      commands, callout notes — the deployment guide mirrors this style; reference the runbook for
      restore rather than duplicating it)
    - deploy/docker-compose.yml (the production stack the guide brings up — service set, the api
      migrate+seed command, nginx TLS ingress)
    - deploy/docker-compose.dev.yml userbot block lines 158-197 (the customer-side prerequisites
      comment: TG_API_ID/HASH from my.telegram.org, TG_SESSION_STRING via userbot/session.py)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the deployment-guide section list:
      prerequisites, env/secrets matrix table, TLS via certbot, first-run, webhook, userbot session,
      backup cron, lines 374-413)
    - deploy/backup/README.md (the backup cron — referenced, not duplicated)
    - tests/smoke/test_smoke_full_stack.sh (the smoke sequence — the guide's "verify it works" step
      references `make smoke` so stand-up and prove-it-works are one sequence per D-02)
  </read_first>
  <action>
    Create `docs/deployment-guide.md` (English, D-06) mirroring the runbook's doc style (TOC,
    numbered steps, fenced exact commands, callout notes). Sections: (1) Prerequisites (server OS,
    domain/DNS, open ports 80/443 only); (2) Env/secrets matrix as a table with columns
    `VAR | Description | Source | Example(placeholder)` covering POSTGRES_PASSWORD, REDIS, S3/MinIO
    creds, JWT_SECRET, BOT_TOKEN, WEBHOOK_SECRET, PUBLIC_WEBAPP_URL, TG_API_ID, TG_API_HASH,
    TG_SESSION_STRING — PLACEHOLDERS only, with an explicit note that `.env` lives OUTSIDE the repo
    and secrets must be rotated/stored securely (never committed); (3) TLS via certbot (obtain
    letsencrypt certs into the path `nginx.conf` references); (4) First run: `docker compose -f
    deploy/docker-compose.yml up -d`, the migrate+seed happens via the api entrypoint, verify
    `/health`, then run `make smoke` to confirm the stack (cite D-02); (5) Aiogram bot webhook
    setup (set BOT_TOKEN, register the webhook URL at the public HTTPS endpoint, WEBHOOK_SECRET);
    (6) Userbot session setup (TG_API_ID/HASH from my.telegram.org, generate TG_SESSION_STRING via
    `python -m userbot.session`); (7) Backup cron — reference `deploy/backup/README.md` +
    `pg_backup.sh` and link the restore runbook (do NOT duplicate restore). No real secret values
    anywhere.
  </action>
  <verify>
    <automated>test -f docs/deployment-guide.md && grep -qi 'certbot' docs/deployment-guide.md && grep -q 'docker-compose.yml' docs/deployment-guide.md && grep -qi 'WEBHOOK_SECRET\|BOT_TOKEN' docs/deployment-guide.md && grep -qi 'TG_SESSION_STRING\|userbot.session' docs/deployment-guide.md && grep -qi 'backup' docs/deployment-guide.md && ! grep -REn 'sk-ant-[A-Za-z0-9]{20,}|[0-9]{8,}:[A-Za-z0-9_-]{30,}' docs/deployment-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/deployment-guide.md` exists (English) with a secrets matrix table listing
      BOT_TOKEN, WEBHOOK_SECRET, TG_API_ID, TG_API_HASH, TG_SESSION_STRING, POSTGRES_PASSWORD,
      and S3/JWT secrets — all placeholders
    - It covers certbot TLS, first-run `docker compose -f deploy/docker-compose.yml up`, the
      `/health` + `make smoke` verification, aiogram webhook setup, and `python -m userbot.session`
    - It references `deploy/backup/README.md` for the backup cron and links the restore runbook
      (does not duplicate restore steps)
    - It contains NO real secret values (no live token/key patterns)
  </acceptance_criteria>
  <done>The deployment guide stands the production stack up from scratch — prerequisites, placeholder secrets matrix, certbot TLS, first-run migrate+seed+smoke, webhook + userbot session, backup cron — with no leaked secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/admin-guide-ru.md (Russian) — add-source, alert rules, needs_review, token budget</name>
  <files>docs/admin-guide-ru.md</files>
  <read_first>
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the admin-guide section: Russian,
      task-oriented, screenshot-light; the 5 sections — add site, add channel, alert-rule builder,
      needs_review, token budget; lines 416-428 + "No Analog Found" note)
    - .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md (the add-source wizard flow,
      enable-gate behavior, alert-rule builder predicates, the needs_review chip — the source of
      truth for what the operator actually does in the dashboard)
    - .planning/phases/05-telegram-monitoring-ai/05-* SUMMARY/CONTEXT (the needs_review queue +
      per-source 7-day token-budget monitoring behavior to describe accurately)
    - docs/polymer-intelligence-tz.md §9 line 221 ("инструкция администратора (источники, правила
      алертов, подтверждение отчётов)") — the §9 requirement this satisfies
  </read_first>
  <action>
    Create `docs/admin-guide-ru.md` in RUSSIAN (D-06) — task-oriented, screenshot-light prose for
    the customer's Russian-speaking operator (keep technical English terms Docker/Telegram/API
    as-is). Sections (per CONTEXT D-05.3): (1) «Добавление источника — сайт (html_table / rss)» —
    open the add-source wizard, the auto-built form from config_schema, run Test for a ≤10-row
    preview, the enable-gate (источник с неуспешным тестом включить нельзя); (2) «Добавление
    источника — Telegram-канал» — same wizard for telegram_channel, Test, enable-gate, that signals
    appear in the feed once the userbot is running; (3) «Построитель правил алертов» — product /
    volume/price threshold / urgency / channel (per-rule chat_id), dedupe; (4) «Очередь
    needs_review» — what a needs_review item is (confidence < 0.5), how to review/act; (5)
    «Мониторинг бюджета токенов» — per-source 7-day token spend, the daily-limit fallback behavior,
    the budget admin alert. Do NOT include any real secrets, tokens, or credentials — this is an
    operator UI guide, not a deploy guide.
  </action>
  <verify>
    <automated>test -f docs/admin-guide-ru.md && grep -q 'needs_review' docs/admin-guide-ru.md && grep -qi 'Telegram' docs/admin-guide-ru.md && grep -qE 'алерт|правил|бюджет|источник' docs/admin-guide-ru.md && ! grep -REn 'sk-ant-[A-Za-z0-9]{20,}|[0-9]{8,}:[A-Za-z0-9_-]{30,}' docs/admin-guide-ru.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/admin-guide-ru.md` exists and is written in Russian prose (Cyrillic headings present)
    - It has the 5 task sections: add site source, add telegram channel, alert-rule builder,
      needs_review queue, token-budget monitoring
    - It describes the enable-gate (failed test cannot enable) in operator terms
    - It contains NO real secret values
  </acceptance_criteria>
  <done>The Russian admin guide gives the operator task-oriented instructions for add-source (site + channel), alert rules, the needs_review queue, and token-budget monitoring — satisfying the §9 "инструкция администратора" deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → customer | Handover docs are read by the customer/operator; leaked secrets here are a direct disclosure |
| deployment guide → production secrets | The secrets matrix describes (must not contain) production credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Information disclosure | secrets in deployment/admin/acceptance docs | mitigate | Secrets matrix uses PLACEHOLDERS only; an explicit note states `.env` lives outside the repo and secrets must be stored/rotated securely; a grep gate in `<verify>` asserts no live token/key patterns are present in any doc |
| T-06-17 | Spoofing | webhook setup guidance | mitigate | The deployment guide instructs setting WEBHOOK_SECRET and registering the webhook only over public HTTPS — preventing unauthenticated/plaintext webhook spoofing |
| T-06-18 | Tampering | acceptance evidence overstated | mitigate | Each §6.1.x row cites a SPECIFIC named passing artifact; customer-gated items are explicitly marked "Blocked on" and NOT claimed as passed — no false sign-off |
| T-06-SC | Tampering | pip/uv installs | mitigate | Docs-only plan; no packages installed. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `06-ACCEPTANCE.md`: one row per §6.1.1–§6.1.6, cited green-proxy evidence, a "Blocked on" column,
  a single consolidated deploy-day checklist superseding 02/03/05-UAT, §6.1.6 marked closed-locally.
- `docs/deployment-guide.md`: placeholder secrets matrix, certbot TLS, first-run + smoke, webhook +
  userbot session, backup cron; no real secrets.
- `docs/admin-guide-ru.md`: Russian, 5 operator sections; no real secrets.
</verification>

<success_criteria>
The consolidated acceptance spine, the deployment guide, and the Russian admin guide exist, cite the
locally-proven evidence, document the customer-gated steps as deploy-day rows, and leak no secrets —
satisfying Phase-6 SC#1 (acceptance items pass on review) and the documentation portion of SC#4.
</success_criteria>

<output>
Create `.planning/phases/06-acceptance-handover/06-06-SUMMARY.md` when done.
</output>
