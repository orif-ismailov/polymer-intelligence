---
phase: 06-acceptance-handover
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_telegram_channel_close.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "An admin adds a telegram_channel source via the wizard, runs Test, and the enable-gate refuses to enable it without a passing test"
    - "The enable-gate invariant is_enabled = true ⇒ last_test_ok_at IS NOT NULL is enforced server-side for telegram_channel"
    - "A fixture MTProto message fed through parse_telegram_item produces an extracted signal"
    - "The extracted signal is queryable via v_live_feed — closing the §6.1.6 channel slice with no real account"
  artifacts:
    - path: "backend/tests/test_telegram_channel_close.py"
      provides: "Fixture/mock-driven local close of the §6.1.6 telegram_channel slice (D-03)"
      min_lines: 80
      contains: "parse_telegram_item"
  key_links:
    - from: "backend/tests/test_telegram_channel_close.py"
      to: "app.tasks.parse_telegram.parse_telegram_item"
      via: "feeds a fixture raw_item through the orchestrator and asserts a signal"
      pattern: "parse_telegram_item"
    - from: "backend/tests/test_telegram_channel_close.py"
      to: "PATCH /api/v1/sources enable-gate"
      via: "is_enabled=True with last_test_ok_at=NULL → 422"
      pattern: "422"
---

<objective>
Close the §6.1.6 telegram_channel slice locally, fixture/mock-driven, with no real account
(D-03) — finally retiring the SC#5 cross-phase caveat carried since Phase 4. Prove the full
deterministic chain in one test module: wizard adds a `telegram_channel` source → Test → the
server-side enable-gate refuses to enable a source with no passing test
(`is_enabled = true ⇒ last_test_ok_at IS NOT NULL`) → a fixture MTProto message is fed through
`parse_telegram_item` → the extracted signal appears in `v_live_feed`.

Purpose: this is the executable proof behind Phase-6 SC#3 / TZ §6.1.6 (admin onboards a channel
with no developer; failed-test source cannot be enabled). It mirrors how Phase 5 closed the eval
gate key-free on committed fixtures. The real-account live ingestion stays the deploy-day drill
(05-UAT item 1), recorded in 06-ACCEPTANCE.md (06-06). The explicit retirement of the SC#5
caveat in ROADMAP / 04-CONTEXT happens in the capstone (06-07), gated on this test passing.

Output: `backend/tests/test_telegram_channel_close.py`.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `backend/tests/test_telegram_channel_close.py` — NEW test module proving the §6.1.6 channel slice
</artifacts_produced>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acceptance-handover/06-CONTEXT.md
@.planning/phases/06-acceptance-handover/06-PATTERNS.md
@backend/tests/test_parse_telegram.py
@backend/tests/test_source_wizard.py
@backend/tests/parsing/test_telegram_accuracy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable-gate proof for telegram_channel (wizard add → Test → cannot-enable-without-pass)</name>
  <files>backend/tests/test_telegram_channel_close.py</files>
  <behavior>
    - Test: POST /api/v1/sources with adapter=telegram_channel saves the source disabled
      (is_enabled=False, last_test_ok_at=NULL) — the pending state.
    - Test: PATCH /api/v1/sources/{id} with is_enabled=True while last_test_ok_at IS NULL → 422
      (the server-side enable-gate invariant is_enabled=true ⇒ last_test_ok_at IS NOT NULL).
    - Test: after a passing Test sets last_test_ok_at, PATCH is_enabled=True succeeds (200).
    - Test: a non-admin attempting the POST/PATCH is rejected (admin-only writes).
  </behavior>
  <read_first>
    - backend/tests/test_source_wizard.py (the `_make_staff_user` + `_auth_headers` +
      `_make_mock_source` + `_make_client_with_user_and_db` helpers lines 34-99; the existing
      `test_enable_gate` (422) and `test_pending_source` patterns referenced in the module docstring)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the `test_telegram_channel_close.py`
      section + the `TestTelegramChannelClose` skeleton, lines 144-220)
    - .planning/phases/04-dashboard-source-constructor/04-ACCEPTANCE.md (the §6.1.6 enable-gate
      invariant wording and the telegram pending-state caveat, the SC#5 section)
  </read_first>
  <action>
    Create `backend/tests/test_telegram_channel_close.py` with a `TestTelegramChannelClose`
    class. Reuse the `_make_staff_user`, `_auth_headers`, `_make_mock_source`, and
    `_make_client_with_user_and_db` helpers (copy/import the pattern from `test_source_wizard.py`)
    so no live DB is required. Assert the enable-gate chain for adapter `telegram_channel`:
    saved-pending (`is_enabled=False`, `last_test_ok_at=None`); PATCH `is_enabled=True` with
    `last_test_ok_at=None` → 422 with the enable-gate message; once `last_test_ok_at` is set,
    PATCH `is_enabled=True` → 200; non-admin → 403. Use the key-free contract (safe under
    placeholder env, no live MTProto/Anthropic calls).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_telegram_channel_close.py::TestTelegramChannelClose -k "enable_gate or pending or non_admin" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_telegram_channel_close.py -k "enable_gate"` exits 0
    - A test asserts PATCH `is_enabled=True` with `last_test_ok_at=NULL` for a `telegram_channel`
      source returns 422
    - A test asserts the saved `telegram_channel` source has `is_enabled=False` and
      `last_test_ok_at=None` (pending)
    - A test asserts a non-admin write is rejected
    - The module makes no live API calls (runs green under placeholder env vars)
  </acceptance_criteria>
  <done>The wizard→Test→enable-gate chain for telegram_channel is proven deterministically: a source with no passing test cannot be enabled (422), a passing test unlocks enable, non-admin is rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture MTProto message → parse_telegram_item → signal in v_live_feed</name>
  <files>backend/tests/test_telegram_channel_close.py</files>
  <behavior>
    - Test: a fixture raw_item (MTProto-style channel message text) fed through
      parse_telegram_item with extract_signal patched to return a relevant ExtractionResult
      produces exactly one parse_runs journal row THEN one signal (attribution order G5).
    - Test: the produced signal carries the telegram source's source_id and the extracted fields
      (kind/product/volume/price), and the signal shape is what v_live_feed surfaces (the feed
      view selects from signals) — assert the signal row satisfies the v_live_feed column contract.
    - Test: confidence < 0.5 routes the signal to needs_review (ai.needs_review=true) — boundary
      preserved, still appears in the stream.
  </behavior>
  <read_first>
    - backend/tests/test_parse_telegram.py (the `_make_raw_item`, `_make_extraction_result`,
      `_make_journal`, `_import_task` helpers lines 30-109; the happy-path test that patches
      `extract_signal` and asserts parse_runs-before-signal and the needs_review boundary)
    - backend/app/tasks/parse_telegram.py (the `parse_telegram_item` orchestrator at line 190 —
      budget→extract/fallback→journal→signal→confidence<0.5 needs_review)
    - backend/app/schemas/dashboard.py and backend/app/api/feed.py (the v_live_feed column
      contract the produced signal must satisfy)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the key-free fixture contract +
      the `test_fixture_message_through_parse_telegram_item` / `test_signal_lands_in_v_live_feed`
      skeleton, lines 195-220)
  </read_first>
  <action>
    Add to `test_telegram_channel_close.py` the message-flow proof. Build a fixture `raw_item`
    from a `telegram_channel` source (use the `_make_raw_item` helper shape from
    `test_parse_telegram.py`, `source_id` pointing at the channel source). Patch
    `extract_signal` (at its import site in `app.tasks.parse_telegram`) to return a relevant
    `ExtractionResult` + journal, run `parse_telegram_item`, and assert: a `parse_runs` row is
    written BEFORE the `signal` (attribution invariant G5), the resulting signal carries the
    channel `source_id` and the extracted kind/product/volume/price, and the signal satisfies the
    `v_live_feed` column contract (the feed view's selected fields are all populated) — i.e. the
    extracted signal "appears in v_live_feed". Add the `confidence < 0.5 → ai.needs_review=true`
    boundary case so a low-confidence channel signal still lands in the stream as needs_review.
    No live MTProto or Anthropic calls — fully mocked/fixtured per the key-free contract.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_telegram_channel_close.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && pytest tests/test_telegram_channel_close.py -x -q` exits 0 (whole module green)
    - A test feeds a fixture telegram raw_item through `parse_telegram_item` and asserts a signal
      is produced with the channel `source_id` and extracted fields
    - The produced signal satisfies the `v_live_feed` column contract (assertable against the
      feed view's selected columns)
    - A test asserts confidence < 0.5 → `ai.needs_review=true`
    - The module is key-free (no live MTProto/Anthropic network calls; green under placeholder env)
  </acceptance_criteria>
  <done>A fixture channel message flows through parse_telegram_item into an extracted signal that satisfies the v_live_feed contract, closing the §6.1.6 channel slice deterministically with no real account.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted channel message → parse_telegram_item | Channel text is attacker-controllable content entering the extraction/signal pipeline |
| client (admin) → PATCH /sources enable | Enabling a source is a privileged state change gated by the test invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Elevation of privilege | source enable-gate | mitigate | The test re-demonstrates the server-side invariant `is_enabled=true ⇒ last_test_ok_at IS NOT NULL` for telegram_channel (422 on unverified enable) — a source cannot be activated without a passing test |
| T-06-07 | Tampering / injection | fixture channel message into parse_telegram_item | mitigate | The test exercises the existing strict-JSON extraction + confidence<0.5→needs_review routing on fixture content; malformed/low-confidence input is quarantined to needs_review, never auto-trusted |
| T-06-08 | Spoofing | real MTProto credentials | accept | No real account/session used — fixture/mock only; live-account ingestion is the deploy-day drill recorded in 06-ACCEPTANCE.md (customer-borne userbot risk per TZ §7.2) |
| T-06-SC | Tampering | pip/uv installs | mitigate | No new packages; reuses telethon/anthropic/instructor already audited in Phase 5 (T-05-SC). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest tests/test_telegram_channel_close.py` exits 0 (whole module).
- Enable-gate: telegram_channel with no passing test → 422; pending state asserted; non-admin rejected.
- Fixture message → `parse_telegram_item` → signal with channel source_id + extracted fields,
  satisfying the v_live_feed contract; confidence<0.5 → needs_review.
- Key-free (no live MTProto/Anthropic calls).
</verification>

<success_criteria>
The §6.1.6 telegram_channel slice is closed locally and deterministically — admin adds a channel,
a failed-test source cannot be enabled, and a fixture message becomes a signal in v_live_feed —
satisfying Phase-6 SC#3 (TZ §6.1.6) at the procedure level and unblocking the SC#5-caveat
retirement in the 06-07 capstone.
</success_criteria>

<output>
Create `.planning/phases/06-acceptance-handover/06-03-SUMMARY.md` when done.
</output>
