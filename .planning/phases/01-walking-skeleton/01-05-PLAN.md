---
phase: 01-walking-skeleton
plan: 05
type: gap_closure
gap_closure: true
wave: 4
depends_on: ["01-04"]
files_modified:
  - deploy/docker-compose.dev.yml
  - deploy/nginx/nginx.conf
  - backend/Dockerfile
autonomous: true
requirements: ["REQ-nfr-security", "REQ-nfr-observability"]
must_haves:
  truths:
    - "docker compose -f deploy/docker-compose.dev.yml config resolves the nginx service and all three backend build blocks without a 'failed to read dockerfile' error (SC#1)"
    - "An nginx service exists in the dev compose stack, mounts deploy/nginx/nginx.conf read-only, depends on api, and publishes a proxy port (SC#1)"
    - "nginx -t accepts deploy/nginx/nginx.conf — the config contains a top-level events {} block before http {} (SC#1)"
    - "Security headers are emitted on hashed static-asset responses, not dropped by the location-level Cache-Control add_header (REQ-nfr-security, CR-06)"
  artifacts:
    - path: "deploy/docker-compose.dev.yml"
      provides: "Dev stack with nginx service plus resolvable api/worker/beat build blocks"
      contains: "nginx:"
    - path: "deploy/nginx/nginx.conf"
      provides: "Structurally valid nginx config with events block and inherited security headers"
      contains: "events {"
    - path: "backend/Dockerfile"
      provides: "Backend image build target resolved by the compose build context ../backend"
      contains: "FROM python:3.12-slim"
  key_links:
    - from: "deploy/docker-compose.dev.yml"
      to: "backend/Dockerfile"
      via: "context ../backend + dockerfile Dockerfile"
      pattern: "dockerfile: Dockerfile"
    - from: "deploy/docker-compose.dev.yml"
      to: "deploy/nginx/nginx.conf"
      via: "nginx service volume mount read-only"
      pattern: "nginx.conf:/etc/nginx/nginx.conf:ro"
    - from: "deploy/nginx/nginx.conf"
      to: "api:8000"
      via: "proxy_pass (already wired) under a config that now parses"
      pattern: "events \\{"
---

<objective>
Close Gap 1 (SC#1 — "docker compose up brings up api, worker, beat, postgres, redis, nginx; /health returns OK"). The dev compose stack currently cannot stand up: the nginx service is absent, all three backend build blocks point at a non-existent backend/Dockerfile, and nginx.conf lacks the mandatory top-level events {} block so nginx refuses to start. Also fix CR-06: server-level security headers are silently wiped in the static-asset location that adds its own Cache-Control header.

Purpose: The "walking skeleton" must actually walk — a deployable end-to-end compose stack is the foundation every later phase plugs into.
Output: A compose stack whose config resolves, an nginx service wired to a parseable config, a backend/Dockerfile that the compose build context resolves, and security headers that survive per-location add_header in nginx.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-VERIFICATION.md
@.planning/phases/01-walking-skeleton/01-04-SUMMARY.md
@deploy/Dockerfile.backend
</context>

<artifacts_this_phase_produces>
New symbols introduced by this gap-closure plan (existing edited symbols not listed):
- Service `nginx` in `deploy/docker-compose.dev.yml` (image `nginx:stable`, read-only mount of `deploy/nginx/nginx.conf`, depends_on api, publishes proxy ports 80/443).
- File `backend/Dockerfile` — a real backend image build target (FROM python:3.12-slim) resolved by the compose build context `../backend`, behaviorally equivalent to `deploy/Dockerfile.backend` (same system deps, pip install from pyproject, non-root appuser, uvicorn CMD).
- Top-level `events { worker_connections 1024; }` block in `deploy/nginx/nginx.conf`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add events block and fix security-header inheritance in nginx.conf</name>
  <files>deploy/nginx/nginx.conf</files>
  <read_first>
    - deploy/nginx/nginx.conf (the file being modified — note: it currently opens with `http {` at line 10, has five `add_header ... always` security directives at server level lines 71-78, and a static-asset location at lines 128-131 that adds `Cache-Control` and thereby drops the inherited security headers per nginx add_header non-additive semantics)
    - .planning/phases/01-walking-skeleton/01-REVIEW.md (CR-01 and CR-06 sections — exact target behavior)
  </read_first>
  <action>
    Add a top-level `events { worker_connections 1024; }` block as a sibling of `http {`, positioned BEFORE the `http {` block (i.e. before current line 10). Also add `worker_processes auto;` as a top-level directive above the events block. Do not nest events inside http.

    Fix CR-06 header inheritance: inside the static-asset nested location `location ~* \.(js|css|woff2?|png|svg|ico)$` (currently lines 128-131, which adds `Cache-Control "public, immutable"`), re-add all five server-level security headers using the `always` parameter so they are not dropped when that block introduces its own add_header. The five headers to repeat verbatim: `X-Content-Type-Options "nosniff" always`, `X-Frame-Options "DENY" always`, `Content-Security-Policy "frame-ancestors 'none'" always`, `Strict-Transport-Security "max-age=31536000; includeSubDomains" always`, `Referrer-Policy "strict-origin-when-cross-origin" always`. Keep the existing `expires 1y;` and `add_header Cache-Control "public, immutable";` lines in that block.

    Do not modify the proxy_pass targets, the limit_req_zone/limit_req rate-limit directives, the TLS cert paths, or the HTTP→HTTPS redirect server — those are VERIFIED and out of scope.
  </action>
  <verify>
    <automated>grep -v '^#' /Users/kholmumin/WebstormProjects/polymer-intelligence/deploy/nginx/nginx.conf | grep -qE '^\s*events\s*\{' && grep -c 'worker_connections' /Users/kholmumin/WebstormProjects/polymer-intelligence/deploy/nginx/nginx.conf | grep -q '^1$' && echo EVENTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/nginx/nginx.conf` contains a top-level `events {` block (a line matching `events {` that is NOT inside the `http {` block) appearing before the `http {` line.
    - The events block contains `worker_connections 1024;`.
    - `worker_processes auto;` appears as a top-level directive.
    - The static-asset location block `location ~* \.(js|css|woff2?|png|svg|ico)$` contains all five security headers (`X-Content-Type-Options`, `X-Frame-Options`, `Content-Security-Policy`, `Strict-Transport-Security`, `Referrer-Policy`) each with the `always` parameter, in addition to its existing `Cache-Control` add_header.
    - The total count of `X-Content-Type-Options` occurrences in the file is exactly 2 (server level + static-asset location).
    - `nginx -t -c /etc/nginx/nginx.conf` run inside an `nginx:stable` container against this file (with cert paths stubbed/ignored as needed) reports `syntax is ok` for the events/http structure (cert-file-not-found is the only acceptable remaining error, since real certs are not present locally).
  </acceptance_criteria>
  <done>nginx.conf has a top-level events block before http, defines worker_connections 1024, and repeats the five security headers (with `always`) inside the static-asset location so they are not dropped by its Cache-Control header.</done>
</task>

<task type="auto">
  <name>Task 2: Create backend/Dockerfile and add nginx service + fix backend build blocks in compose</name>
  <files>backend/Dockerfile, deploy/docker-compose.dev.yml</files>
  <read_first>
    - deploy/Dockerfile.backend (source-of-truth: FROM python:3.12-slim; installs libpq5/libffi8/curl; `COPY pyproject.toml ./`; creates stub `app/__init__.py`; `pip install ".[dev]"`; `COPY . .`; non-root appuser uid 1001; HEALTHCHECK curl /api/v1/health; EXPOSE 8000; CMD uvicorn app.main:app — these COPY paths assume build context = backend/)
    - deploy/docker-compose.dev.yml (the file being modified — api block build at lines 54-56, worker at 82-84, beat at 102-104, all using `context: ../backend` + `dockerfile: Dockerfile`; postgres/redis define the service style, ports, healthcheck, env_file patterns to match)
    - deploy/nginx/nginx.conf (to confirm nginx listens on 80 and 443 and proxies to api:8000, so the nginx service publishes the right ports and depends on api)
  </read_first>
  <action>
    Create `backend/Dockerfile` as a real backend image build target whose build context is `backend/` (so the existing compose `context: ../backend` + `dockerfile: Dockerfile` resolves to it). Make it behaviorally equivalent to `deploy/Dockerfile.backend`: `FROM python:3.12-slim`; `RUN apt-get update && apt-get install -y --no-install-recommends libpq5 libffi8 curl && rm -rf /var/lib/apt/lists/*`; `WORKDIR /app`; `COPY pyproject.toml ./`; `RUN mkdir -p app && touch app/__init__.py && pip install --no-cache-dir --upgrade pip && pip install --no-cache-dir ".[dev]"`; `COPY . .`; create non-root `appuser` (uid 1001) and chown /app; `USER appuser`; `HEALTHCHECK` curl `http://localhost:8000/api/v1/health`; `EXPOSE 8000`; `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`. This keeps the CI step `docker build -f deploy/Dockerfile.backend ... backend/` working unchanged AND makes compose resolve. Do NOT change the compose build blocks' context or dockerfile keys — the new backend/Dockerfile is the chosen approach so they stay `context: ../backend` + `dockerfile: Dockerfile`.

    Add an `nginx` service to `deploy/docker-compose.dev.yml` matching the existing service style (restart: unless-stopped). Use `image: nginx:stable`. Mount the config read-only: `../nginx/nginx.conf` — confirm the correct relative path from the compose file's directory (deploy/) to deploy/nginx/nginx.conf is `./nginx/nginx.conf` — mount it to `/etc/nginx/nginx.conf:ro`. Publish ports `80:80` and `443:443`. Add `depends_on: api`. Because the supplied nginx.conf references TLS cert paths under /etc/letsencrypt that are not present in dev, add a short comment noting cert volumes are wired in the prod compose; the dev service may fail TLS until certs exist but the service definition must be present and structurally valid. Update the file header comment block (lines 10-11) so the "Services:" line lists nginx.

    Verify deploy/Dockerfile.backend's COPY paths still resolve under context backend/ (they do — unchanged) and that the new backend/Dockerfile's COPY paths also resolve under context backend/.
  </action>
  <verify>
    <automated>test -f /Users/kholmumin/WebstormProjects/polymer-intelligence/backend/Dockerfile && grep -q 'FROM python:3.12-slim' /Users/kholmumin/WebstormProjects/polymer-intelligence/backend/Dockerfile && grep -qE '^\s*nginx:' /Users/kholmumin/WebstormProjects/polymer-intelligence/deploy/docker-compose.dev.yml && grep -q 'nginx.conf:/etc/nginx/nginx.conf:ro' /Users/kholmumin/WebstormProjects/polymer-intelligence/deploy/docker-compose.dev.yml && echo COMPOSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/Dockerfile` exists, begins with `FROM python:3.12-slim`, contains `pip install --no-cache-dir ".[dev]"`, contains `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`, and creates a non-root `appuser`.
    - `deploy/docker-compose.dev.yml` contains a service named `nginx` with `image: nginx:stable` (or a pinned `nginx:stable-*` equivalent).
    - The nginx service mounts the config read-only via a volume entry ending in `nginx.conf:/etc/nginx/nginx.conf:ro`.
    - The nginx service publishes ports `80:80` and `443:443` and has `depends_on` listing `api`.
    - The api, worker, and beat build blocks still resolve: `context: ../backend` + `dockerfile: Dockerfile` now points to the newly created `backend/Dockerfile` (which exists).
    - `docker compose -f deploy/docker-compose.dev.yml config` exits 0 (parses and resolves all build contexts and the nginx service without a "failed to read dockerfile" error). [Human/CI verification — requires Docker; static check is the grep above.]
    - The compose file header "Services:" comment line includes `nginx`.
  </acceptance_criteria>
  <done>backend/Dockerfile exists and is a working backend image target resolved by the unchanged compose build blocks; the compose stack defines an nginx service mounting the config read-only, depending on api, and publishing 80/443; `docker compose config` resolves with no missing-dockerfile error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → nginx | Untrusted client traffic terminates TLS and is rate-limited at nginx before reaching the api container |
| nginx → api:8000 | Internal proxy hop; only /api/ and /webapp/ are exposed, internal services (postgres, redis) are not published through nginx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | nginx TLS termination | mitigate | TLS-ready config preserved; events block added so nginx actually starts and HTTPS server is reachable (SC#1) |
| T-04-03 | Tampering / Info Disclosure | nginx security headers | mitigate | Re-add the five security headers (`always`) inside the static-asset location so CR-06 header-drop no longer silently disables clickjacking/MIME/HSTS protection on hashed assets |
| T-04-05 | Elevation of Privilege | exposed internal services | mitigate | nginx service publishes only 80/443; postgres/redis remain on the internal compose network (unchanged) |
| T-04-06 | Spoofing (credential stuffing) | /api/v1/auth/login | mitigate | limit_req_zone rate=10r/m on the login location preserved; previously moot because nginx would not start — now effective once events block lands |
| T-03-SC | Tampering | container base images (nginx:stable, python:3.12-slim) | accept | Official Docker Hub images; no third-party package installs added by this plan; image build itself covered by SC#5 plan 01-06 |
</threat_model>

<verification>
Run these after both tasks:
- `grep -v '^#' deploy/nginx/nginx.conf | grep -qE '^\s*events\s*\{'` → top-level events block present.
- `grep -c 'X-Content-Type-Options' deploy/nginx/nginx.conf` → returns 2 (server + static-asset location).
- `test -f backend/Dockerfile && grep -q 'FROM python:3.12-slim' backend/Dockerfile` → backend Dockerfile present.
- `grep -E '^\s*nginx:' deploy/docker-compose.dev.yml` → nginx service present.
- `grep 'nginx.conf:/etc/nginx/nginx.conf:ro' deploy/docker-compose.dev.yml` → read-only config mount.
- (CI/human, requires Docker) `docker compose -f deploy/docker-compose.dev.yml config` exits 0; `nginx -t` inside nginx:stable reports syntax ok for the events/http structure.
</verification>

<success_criteria>
SC#1 is closed: the dev compose stack defines postgres, redis, api, worker, beat, AND nginx; all three backend build blocks resolve to an existing Dockerfile; nginx.conf has the mandatory events block and parses; security headers are not silently dropped on static assets. `docker compose up` can bring the stack up and `/health` is reachable through the proxy.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-05-SUMMARY.md` when done.
</output>
