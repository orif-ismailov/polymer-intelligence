---
phase: 06-acceptance-handover
plan: 05
type: execute
wave: 2
depends_on: ["06-04"]
files_modified:
  - tests/smoke/test_smoke_full_stack.sh
  - Makefile
autonomous: true
requirements: []
must_haves:
  truths:
    - "The full production compose stack stands up locally against synthetic/fixture data"
    - "After compose up + migrate+seed, /health returns ok"
    - "A synthetic request submitted via the API appears in v_live_feed"
    - "A deliberately-failing fake source is isolated (siblings keep running) and raises exactly one source_failure alert"
    - "The smoke doubles as the deployment-guide smoke test (stand-up + prove-it-works are one sequence)"
  artifacts:
    - path: "tests/smoke/test_smoke_full_stack.sh"
      provides: "Local full-stack docker-compose smoke against synthetic data (D-02)"
      min_lines: 70
      contains: "docker-compose.yml"
    - path: "Makefile"
      provides: "make smoke target invoking the smoke script (Claude's discretion: scripted target)"
      contains: "smoke"
  key_links:
    - from: "tests/smoke/test_smoke_full_stack.sh"
      to: "deploy/docker-compose.yml"
      via: "compose up -f deploy/docker-compose.yml the full prod stack"
      pattern: "deploy/docker-compose.yml"
    - from: "tests/smoke/test_smoke_full_stack.sh"
      to: "run_source_fetch_isolated + source_failure alert"
      via: "force a fake-source failure 3x and assert isolation + one alert"
      pattern: "source_failure"
---

<objective>
Run a local full-stack docker-compose smoke against synthetic/fixture data (D-02): compose up the
production `deploy/docker-compose.yml` → migrate+seed → `/health` → submit a synthetic request →
see it in `v_live_feed` → force a deliberately-failing fake source → observe per-source isolation
+ a single `source_failure` alert. The smoke doubles as live validation that the production compose
(06-04) + deployment guide (06-06) actually stand the system up — "stand it up" and "prove it
works locally" are the same sequence.

Purpose: a fresh end-to-end confidence check before handover (Phase-6 SC#1 surface: request→feed,
source isolation+alert / TZ §6.1.1+§6.1.4 at the procedure level on synthetic data). It does NOT
replace the customer-gated live drills (real BOT_TOKEN/HTTPS, live sources) — those stay as
deploy-day rows in 06-ACCEPTANCE.md (06-06).

Output: `tests/smoke/test_smoke_full_stack.sh` and a `make smoke` target (Claude's-discretion:
scripted target chosen over a documented manual sequence for reproducibility).
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `tests/smoke/test_smoke_full_stack.sh` — NEW full-stack smoke script
- `Makefile` — NEW or MODIFIED (`make smoke` target)
</artifacts_produced>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acceptance-handover/06-CONTEXT.md
@.planning/phases/06-acceptance-handover/06-PATTERNS.md
@deploy/docker-compose.yml
@backend/tests/test_source_failure_alert.py
@backend/app/tasks/ingest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/smoke/test_smoke_full_stack.sh (compose up → health → request→feed → forced fake-source failure → isolation+alert)</name>
  <files>tests/smoke/test_smoke_full_stack.sh, Makefile</files>
  <read_first>
    - deploy/docker-compose.yml (the production compose from 06-04 — service names, the api
      migrate+seed command, the nginx ingress; the smoke targets THIS file)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the smoke-script structure +
      "invoke run_source_fetch_isolated and source_failure dedupe" guidance, lines 73-141;
      the shell-header + healthcheck-gated patterns)
    - backend/tests/test_source_failure_alert.py (HOW the forced failure is exercised: patch the
      adapter `fetch()` to raise 3×, assert exactly one `alerts` row with `kind='source_failure'`,
      and assert a sibling source still records success — the isolation contract)
    - backend/app/tasks/ingest.py (`run_source_fetch_isolated(session, source, adapter)` at line 68
      and `_execute_uzex_fetch` loop at lines 176-184 — the failure-isolation entrypoint to drive)
  </read_first>
  <action>
    Create `tests/smoke/test_smoke_full_stack.sh` following the `pg_backup.sh` header convention
    (`#!/usr/bin/env bash`, banner, `set -euo pipefail`, `COMPOSE="docker compose -f
    deploy/docker-compose.yml"`). The script, in order: (1) `$COMPOSE up -d` the full stack with a
    synthetic/placeholder `.env` (placeholder BOT_TOKEN/TG creds — no real secrets; the smoke must
    not require customer credentials); (2) poll `/api/v1/health` through nginx until it returns
    `"status":"ok"` (the api command already runs migrate+seed pre-start); (3) submit a SYNTHETIC
    request through the API and assert it becomes queryable in `v_live_feed` (drive via a small
    `$COMPOSE exec api python -c` step or a fixture seed + a `SELECT COUNT(*) FROM v_live_feed`
    that increases); (4) FORCE a deliberately-failing fake source: insert a fake `sources` row with
    an adapter whose `fetch()` raises, run the isolation entrypoint
    (`run_source_fetch_isolated`) against it 3× alongside a healthy sibling source via a
    `$COMPOSE exec api python -c` step mirroring `test_source_failure_alert.py`, then assert (a) the
    sibling recorded success (isolation — siblings keep running) and (b) exactly ONE `alerts` row
    with `kind='source_failure'` exists for the fake source (the 3-strike deduped alert); (5)
    `$COMPOSE down -v` teardown (trap on exit so a mid-run failure still tears down). Print
    `[smoke] PASSED` only if every step succeeded. Add a `smoke` target to the repo `Makefile`
    that runs the script (create the Makefile if absent). Key-free contract: synthetic data +
    placeholder env only, no production data or real credentials (state this in the banner).
  </action>
  <verify>
    <automated>bash -n tests/smoke/test_smoke_full_stack.sh && grep -q 'set -euo pipefail' tests/smoke/test_smoke_full_stack.sh && grep -q 'deploy/docker-compose.yml' tests/smoke/test_smoke_full_stack.sh && grep -q 'source_failure' tests/smoke/test_smoke_full_stack.sh && grep -q 'v_live_feed' tests/smoke/test_smoke_full_stack.sh && grep -qE '^smoke:|smoke:' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/smoke/test_smoke_full_stack.sh` exits 0 (valid shell)
    - The script targets `deploy/docker-compose.yml` (the production compose, not the dev one)
    - The script polls `/api/v1/health` and asserts `"status":"ok"`
    - The script asserts a synthetic request appears in `v_live_feed`
    - The script forces a fake-source failure 3× and asserts (a) a sibling source still records
      success (isolation) and (b) exactly one `alerts` row with `kind='source_failure'`
    - The script tears the stack down on exit (trap) and prints `[smoke] PASSED` on success
    - `Makefile` has a `smoke` target invoking the script
    - The banner states synthetic data + placeholder env only (no production data / real creds)
  </acceptance_criteria>
  <done>An executable full-stack smoke exists that stands up the production compose on synthetic data, proves /health + request→feed + forced fake-source isolation + a single source_failure alert, and is runnable via `make smoke`.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the smoke end-to-end and capture the run evidence</name>
  <files>tests/smoke/test_smoke_full_stack.sh</files>
  <read_first>
    - tests/smoke/test_smoke_full_stack.sh (the script from Task 1 — the sequence being executed)
    - deploy/docker-compose.yml (the stack being stood up)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the smoke pass criteria)
  </read_first>
  <action>
    Run `make smoke` (or `bash tests/smoke/test_smoke_full_stack.sh`) end-to-end against the local
    Docker engine. If any step fails because the production compose or an entrypoint command is
    wrong (e.g. a service name mismatch, the migrate+seed sequence, the health path), fix the
    smoke script and/or surface the compose defect back as a finding for 06-04 (do NOT silently
    weaken the assertions). Confirm the full sequence prints `[smoke] PASSED`. This run is also the
    deployment-guide smoke validation — note the exact command sequence so 06-06 can reference it
    verbatim. Leave the stack torn down (`$COMPOSE down -v`) after the run.
  </action>
  <verify>
    <automated>bash tests/smoke/test_smoke_full_stack.sh 2>&1 | tail -20 | grep -q '\[smoke\] PASSED'</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/smoke/test_smoke_full_stack.sh` exits 0 and prints `[smoke] PASSED`
    - The run output shows: `/health` ok, the synthetic request appearing in `v_live_feed`, the
      sibling-source success, and exactly one `source_failure` alert
    - The stack is torn down after the run (`docker compose -f deploy/docker-compose.yml ps`
      shows no running smoke services)
  </acceptance_criteria>
  <done>The smoke runs green end-to-end on the production compose with synthetic data, proving stand-up + request→feed + source isolation + source_failure alert, and validating the deployment-guide stand-up sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke env (.env) → stack | The smoke supplies a placeholder `.env`; must never require/embed real secrets |
| fake source adapter → ingest pipeline | The forced-failure source is deliberately hostile input to prove isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Denial of service | one source failing taking down others | mitigate | The smoke asserts `run_source_fetch_isolated` keeps siblings running when one adapter raises 3× (per-source isolation, SC#5/T-02-17) and that exactly one deduped `source_failure` alert fires — proving the reliability property on the live stack |
| T-06-14 | Information disclosure | smoke using real credentials/data | accept→mitigate | Smoke runs on synthetic data + a placeholder `.env` only (no BOT_TOKEN/TG/HTTPS required); banner states no production data or real credentials are used |
| T-06-15 | Tampering | stack left running after smoke | mitigate | A bash `trap` tears the stack down (`compose down -v`) even on mid-run failure, so no orphaned containers/volumes with synthetic-but-stale state remain |
| T-06-SC | Tampering | image/package provenance | mitigate | Smoke reuses the audited compose images; no new third-party packages installed. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `bash -n` clean; script targets `deploy/docker-compose.yml`, asserts /health ok, request→feed,
  forced fake-source isolation + one `source_failure` alert, trap teardown, `[smoke] PASSED`.
- `make smoke` runs green end-to-end and leaves the stack torn down.
</verification>

<success_criteria>
The production stack stands up locally on synthetic data and the smoke proves /health, a synthetic
request reaching v_live_feed, and per-source failure isolation with a single source_failure alert —
a pre-handover end-to-end confidence check and validation of the deployment-guide stand-up sequence
(Phase-6 SC#1 surface, TZ §6.1.1+§6.1.4 at procedure level). Customer-gated live drills remain
deploy-day rows in 06-ACCEPTANCE.md.
</success_criteria>

<output>
Create `.planning/phases/06-acceptance-handover/06-05-SUMMARY.md` when done.
</output>
