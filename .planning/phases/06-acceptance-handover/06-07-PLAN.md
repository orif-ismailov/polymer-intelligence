---
phase: 06-acceptance-handover
plan: 07
type: execute
wave: 4
depends_on: ["06-03", "06-06"]
files_modified:
  - HANDOVER.md
  - .planning/ROADMAP.md
  - .planning/phases/04-dashboard-source-constructor/04-CONTEXT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "HANDOVER.md is a single entry point linking every TZ §9 deliverable"
    - "Every linked §9 deliverable path in HANDOVER.md actually exists in the repo"
    - "The SC#5 telegram cross-phase caveat is explicitly retired in ROADMAP and 04-CONTEXT once D-03 (06-03) passes"
  artifacts:
    - path: "HANDOVER.md"
      provides: "Single §9 handover index linking every deliverable (D-05.4)"
      contains: "Deliverables"
  key_links:
    - from: "HANDOVER.md"
      to: "deploy/docker-compose.yml, docs/deployment-guide.md, docs/runbook-backup-restore.md, docs/admin-guide-ru.md, docs/extraction-schema.json, 06-ACCEPTANCE.md"
      via: "table rows linking each §9 artifact"
      pattern: "docker-compose.yml|deployment-guide|runbook|admin-guide|extraction-schema|ACCEPTANCE"
    - from: ".planning/ROADMAP.md"
      to: "the SC#5 telegram cross-phase caveat"
      via: "marks the caveat retired, citing 06-03"
      pattern: "retired|RETIRED"
---

<objective>
Capstone (D-05.4 + the SC#5-caveat retirement): author `HANDOVER.md` — the single §9 entry point
linking every handover deliverable — and explicitly RETIRE the long-standing SC#5 telegram
cross-phase caveat now that 06-03 has proven the channel slice locally. This plan runs last (wave
4): the HANDOVER index links the docs authored in 06-06 and the artifacts from 06-01..06-05, and
the caveat retirement is gated on the 06-03 channel-close test passing.

Purpose: closes Phase-6 SC#4 (deliverables handed over) by giving the customer one index page
covering everything in TZ §9, and removes the cross-phase caveat carried in ROADMAP SC#5 /
04-CONTEXT since Phase 4 — the source-constructor acceptance is now fully accounted for.

Output: `HANDOVER.md` (English, D-06) + edited `ROADMAP.md` and `04-CONTEXT.md` retiring the caveat.
</objective>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `HANDOVER.md` — NEW §9 handover index (repo root)
- `.planning/ROADMAP.md` — MODIFIED (SC#5 caveat marked retired)
- `.planning/phases/04-dashboard-source-constructor/04-CONTEXT.md` — MODIFIED (caveat marked retired)
</artifacts_produced>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acceptance-handover/06-CONTEXT.md
@.planning/phases/06-acceptance-handover/06-PATTERNS.md
@docs/polymer-intelligence-tz.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author HANDOVER.md — single §9 deliverable index</name>
  <files>HANDOVER.md</files>
  <read_first>
    - docs/polymer-intelligence-tz.md §9 line 221 (the verbatim §9 deliverable list: source code,
      docker-compose, DB migrations, deployment + restore docs, prompt/extraction-schema
      descriptions, admin guide)
    - .planning/phases/06-acceptance-handover/06-PATTERNS.md (the HANDOVER.md structure + the
      deliverables table skeleton, lines 431-463)
    - .planning/phases/05-telegram-monitoring-ai/05-UAT.md (the index/table front-matter pattern)
    - docs/extraction-schema.json (a REUSED §9 deliverable the index links — confirm the path exists)
  </read_first>
  <action>
    Create `HANDOVER.md` at the repo root (English, D-06) as the single §9 entry point. Include a
    Deliverables table with one row per §9 artifact, each linking the ACTUAL repo path:
    source code (repo), production compose (`deploy/docker-compose.yml`), DB migrations
    (`backend/alembic/` or the migrations dir), deployment guide (`docs/deployment-guide.md`),
    backup/restore runbook (`docs/runbook-backup-restore.md`), admin guide
    (`docs/admin-guide-ru.md`), prompt + extraction-schema descriptions (`docs/extraction-schema.json`
    + the `parsing/prompts/extract_v1.md` prompt), and the acceptance sign-off
    (`.planning/phases/06-acceptance-handover/06-ACCEPTANCE.md`). Reference (do not duplicate) the
    existing reused docs (runbook, extraction-schema, the three spec docs). Every linked path must
    resolve to a file that exists in the repo at this point. No real secrets.
  </action>
  <verify>
    <automated>test -f HANDOVER.md && for f in deploy/docker-compose.yml docs/deployment-guide.md docs/runbook-backup-restore.md docs/admin-guide-ru.md docs/extraction-schema.json .planning/phases/06-acceptance-handover/06-ACCEPTANCE.md; do grep -q "$f" HANDOVER.md && test -f "$f" || { echo "MISSING link/file $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `HANDOVER.md` exists at repo root with a §9 deliverables table
    - Every linked path resolves to a file that exists: `deploy/docker-compose.yml`,
      `docs/deployment-guide.md`, `docs/runbook-backup-restore.md`, `docs/admin-guide-ru.md`,
      `docs/extraction-schema.json`, `06-ACCEPTANCE.md`
    - The index references the prompt + extraction-schema descriptions and the DB migrations dir
    - No real secret values present
  </acceptance_criteria>
  <done>HANDOVER.md is the single §9 index linking every handover deliverable, and every linked path exists in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the SC#5 telegram cross-phase caveat (gated on 06-03 passing)</name>
  <files>.planning/ROADMAP.md, .planning/phases/04-dashboard-source-constructor/04-CONTEXT.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase-4 SC#5 row with the "Cross-phase caveat" text at line 151 —
      "telegram_channel/llm_page are config-saveable in a PENDING state only … Phase-5/6 acceptance item")
    - .planning/phases/06-acceptance-handover/06-03-PLAN.md and (when present) 06-03-SUMMARY.md
      (confirm the channel-close test passed — the gate for this retirement)
    - .planning/phases/04-dashboard-source-constructor/04-CONTEXT.md (the cross-phase boundary note
      carrying the same caveat)
  </read_first>
  <action>
    First confirm 06-03 passed (the `test_telegram_channel_close.py` suite is green — the channel
    slice is closed locally). Then edit `.planning/ROADMAP.md`: amend the Phase-4 SC#5 caveat text
    to mark it RETIRED, citing 06-03 (e.g. "Cross-phase caveat RETIRED 2026-06-19 — the
    telegram_channel slice was closed locally in Phase 6 (06-03 `test_telegram_channel_close.py`);
    live-account ingestion remains the deploy-day drill in 06-ACCEPTANCE.md"). Apply the matching
    retirement note to the cross-phase boundary note in
    `.planning/phases/04-dashboard-source-constructor/04-CONTEXT.md`. Do not delete the historical
    context — mark it retired with the citation so the trail is preserved. If 06-03 did NOT pass,
    STOP and do not retire the caveat (surface the blocker instead).
  </action>
  <verify>
    <automated>grep -qiE 'retired|RETIRED' .planning/ROADMAP.md && grep -qi '06-03\|telegram_channel_close' .planning/ROADMAP.md && grep -qiE 'retired|RETIRED' .planning/phases/04-dashboard-source-constructor/04-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` SC#5 caveat text is marked RETIRED and cites 06-03 /
      `test_telegram_channel_close.py`
    - `.planning/phases/04-dashboard-source-constructor/04-CONTEXT.md` cross-phase boundary note is
      marked RETIRED with the same citation
    - The historical caveat text is preserved (marked retired, not deleted)
  </acceptance_criteria>
  <done>The SC#5 telegram cross-phase caveat is explicitly retired in ROADMAP and 04-CONTEXT, citing the 06-03 channel-close proof, with live-account ingestion noted as the remaining deploy-day item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HANDOVER.md → customer | The index is the customer's map to all deliverables; broken/incorrect links undermine handover integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Information disclosure | HANDOVER.md / planning-doc edits | mitigate | The index and caveat edits contain no secrets; a `<verify>` grep gate confirms every linked path resolves to an existing file (no dangling links pointing at non-existent/placeholder artifacts) |
| T-06-20 | Repudiation | overstated acceptance status | mitigate | The caveat is only retired AFTER 06-03 passes (gated); historical text is preserved-with-citation so the acceptance trail is auditable, not silently rewritten |
| T-06-SC | Tampering | pip/uv installs | mitigate | Docs/planning-only plan; no packages installed. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `HANDOVER.md` exists; every linked §9 deliverable path resolves to an existing file.
- ROADMAP SC#5 caveat + 04-CONTEXT boundary note marked RETIRED, citing 06-03, gated on 06-03 green.
</verification>

<success_criteria>
The customer has a single §9 handover index linking every deliverable, and the SC#5 telegram
cross-phase caveat carried since Phase 4 is explicitly retired — closing Phase-6 SC#4 and the
source-constructor acceptance accounting.
</success_criteria>

<output>
Create `.planning/phases/06-acceptance-handover/06-07-SUMMARY.md` when done.
</output>
