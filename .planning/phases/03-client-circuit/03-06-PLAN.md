---
phase: 03-client-circuit
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-05"]
files_modified:
  - backend/tests/test_request_sla.py
  - deploy/docker-compose.dev.yml
  - docs/phase-03-acceptance.md
autonomous: false
requirements: [REQ-nfr-performance, REQ-request-wizard, REQ-my-requests, REQ-bot-clients]
tags: [acceptance, sla, e2e, minio, performance, checkpoint]

must_haves:
  truths:
    - "A submitted request is queryable on the backend within 10 s of POST (TZ §6.1.1 SLA, automated proxy test)"
    - "A status change enqueues the notify push on the notify queue within the ≤30 s budget (TZ §6.1.1 SLA, automated proxy test)"
    - "The full client circuit runs live on docker-compose: /start greeting, wizard submit → REQ number, file → MinIO, status change → bot push"
  artifacts:
    - path: "backend/tests/test_request_sla.py"
      provides: "automated SLA proxy: create→readback latency + notify enqueue timing"
      contains: "def test_request_readback_within_10s"
    - path: "docs/phase-03-acceptance.md"
      provides: "live drill checklist + recorded results for the 5 ROADMAP success criteria"
      contains: "TZ §6.1.1"
  key_links:
    - from: "backend/tests/test_request_sla.py"
      to: "POST /webapp/requests + GET /webapp/requests"
      via: "create then immediate readback, assert < 10 s"
      pattern: "create_request|getRequests|/webapp/requests"
    - from: "docs/phase-03-acceptance.md"
      to: "deploy/docker-compose.dev.yml"
      via: "the live stack (api+worker+beat+minio) the drill runs against"
      pattern: "docker compose"
---

<objective>
Prove the Phase-3 acceptance criteria end-to-end: the two TZ §6.1.1 SLAs (request queryable
≤10 s, status notification ≤30 s) via automated proxy tests, and the full client circuit
(/start → wizard submit → REQ number → file in MinIO → status push) via a live docker-compose
drill captured in an acceptance doc. This is the phase's verification gate.

Purpose: Closes the acceptance side of REQ-nfr-performance / REQ-request-wizard / REQ-my-requests
/ REQ-bot-clients — the 5 ROADMAP success criteria for the phase. The deploy-time live drill
mirrors the Phase-2 02-07 pattern (automated gate now, full live verification at deploy).
Output: an SLA test suite, the worker/minio compose confirmation, and an acceptance checklist doc.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-client-circuit/03-CONTEXT.md
@.planning/phases/03-client-circuit/03-02-SUMMARY.md
@.planning/phases/03-client-circuit/03-03-SUMMARY.md
@.planning/phases/03-client-circuit/03-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols/paths created by THIS plan (new — exclude from drift verification):
- `backend/tests/test_request_sla.py` — `test_request_readback_within_10s`, `test_status_change_enqueues_notify_promptly`
- `docs/phase-03-acceptance.md` — the live-drill acceptance checklist + recorded results
- Possibly a minimal edit to `deploy/docker-compose.dev.yml` IF the live drill reveals a missing worker `-Q notify` or minio bucket-bootstrap gap (the notify queue is already in the worker `-Q` list; verify only)
- Consumes everything from 03-01..03-05 (request_service, /webapp API, bot, notify task, MinIO, frontend)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Automated SLA proxy tests — create→readback ≤10 s, status-change→notify enqueue timing</name>
  <files>backend/tests/test_request_sla.py</files>
  <behavior>
    - test_request_readback_within_10s: POST /webapp/requests then immediately GET /webapp/requests returns the new request; the create→readback round trip (in-process, mocked DB) completes well under 10 s and the created number appears in the readback (proves the create path persists synchronously, no async-write lag — the dev-spec contract that satisfies SC#1).
    - test_status_change_enqueues_notify_promptly: calling request_service.transition_status (allowed transition) enqueues send_status_change_notification synchronously within the same request (not on a delayed schedule), so the ≤30 s budget is bounded only by Celery delivery — assert apply_async is called with queue="notify" and no countdown/eta argument (immediate dispatch).
    - The notify task itself returns quickly (no long sleeps); assert the task body has no blocking sleep > a small bound (source-level: no time.sleep with a large constant).
  </behavior>
  <read_first>
    - backend/tests/test_webapp_requests_api.py (03-02 — the TestClient + dependency_overrides + MagicMock DB pattern to reuse)
    - backend/tests/test_request_service.py (03-02 — transition_status + apply_async assertion pattern)
    - backend/app/services/request_service.py (03-02 — create_request/transition_status enqueue immediacy)
    - backend/app/tasks/notify.py (03-03 — send_status_change_notification, assert no large blocking sleep)
    - docs/polymer-intelligence-tz.md §6.1.1 (the ≤10 s / ≤30 s SLA definitions — TZ has priority over dev-spec)
  </read_first>
  <action>
    Create backend/tests/test_request_sla.py.
    test_request_readback_within_10s: using the 03-02 TestClient with overridden get_current_client + a MagicMock DB that records the created Request and returns it on the subsequent list query, POST a valid request, capture the returned number, GET /webapp/requests, assert the number is present and `(t_after_get - t_before_post) < 10.0` seconds. This is a proxy for the SC#1 "queryable within 10 s" criterion — the live drill (Task 2 doc) does the real wall-clock measurement on docker-compose.
    test_status_change_enqueues_notify_promptly: patch send_status_change_notification.apply_async; call request_service.transition_status(db, request(status=new), RequestStatus.viewed); assert apply_async was called once with queue="notify" and that the call kwargs contain no `countdown`/`eta` (immediate dispatch → the ≤30 s budget is delivery-only).
    test_notify_task_no_long_sleep: import the notify module source and assert (via inspect.getsource) it contains no `time.sleep(` with a multi-second constant (defends the ≤30 s budget against an accidental blocking sleep).
    Keep all tests in-process and DB-mocked (CI-safe, no live infra) — consistent with the 02-07 automated-gate-now / live-drill-at-deploy split.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_request_sla.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_request_sla.py` contains `def test_request_readback_within_10s` and a status-change enqueue-timing test.
    - The readback test asserts the elapsed create→read time `< 10.0` s and that the created number appears in the list.
    - The status-change test asserts apply_async(queue="notify") with no countdown/eta (immediate dispatch).
    - `pytest tests/test_request_sla.py` exits 0; full suite `python -m pytest -q` stays green.
  </acceptance_criteria>
  <done>Automated SLA proxies prove synchronous request readback and immediate notify dispatch; CI-safe; green.</done>
</task>

<task type="auto">
  <name>Task 2: Acceptance doc + compose verification for the live client-circuit drill</name>
  <files>docs/phase-03-acceptance.md, deploy/docker-compose.dev.yml</files>
  <read_first>
    - deploy/docker-compose.dev.yml (03-01 added minio + S3_ENDPOINT; worker command already includes `-Q ingest,parse,notify,default` — verify the notify queue is consumed and minio is healthy before the api migrates)
    - .planning/phases/03-client-circuit/03-02-SUMMARY.md, 03-03-SUMMARY.md, 03-05-SUMMARY.md (the actual endpoints, env vars, and Web App URL the drill exercises)
    - docs/polymer-intelligence-tz.md §6.1.1 (the SLA acceptance text to cite verbatim)
    - .planning/ROADMAP.md (Phase 3 — the 5 success criteria the doc must check off)
  </read_first>
  <action>
    Verify deploy/docker-compose.dev.yml: confirm the worker consumes the `notify` queue (the `-Q ...,notify,...` flag), that `minio` is healthy-gated before the api, and that BOT_TOKEN/WEBHOOK_SECRET/PUBLIC_WEBAPP_URL/S3_* flow to the api+worker via env. If any of these is genuinely missing (e.g. PUBLIC_WEBAPP_URL not passed to the api/worker environment, or a minio bucket not auto-created), make the minimal fix; otherwise leave the file unchanged and note "verified, no change".
    Create docs/phase-03-acceptance.md — a live-drill checklist mapped to the 5 ROADMAP Phase-3 success criteria, citing TZ §6.1.1, to be executed at deploy time (mirrors 02-07's deferred-live-drill pattern). Sections:
      1. Prereqs: real BOT_TOKEN + a public HTTPS PUBLIC_WEBAPP_URL (ngrok/tunnel acceptable for the drill) so Telegram can reach the webhook and initData verifies; `.env` filled; `docker compose -f deploy/docker-compose.dev.yml up`.
      2. SC#1 — open the Web App via /start, complete the 4-step wizard, submit; record the confirmation REQ-YYYY-MM-DD-NNNNN; query the backend (GET /webapp/requests or DB) and record the wall-clock seconds from submit to queryable (PASS if ≤10 s).
      3. SC#2 — attach a PDF + a JPG; confirm both land in the MinIO `polymer-files` bucket under requests/{id}/ and a wrong-type/oversize file is rejected.
      4. SC#3 — change the request status on the backend (Phase-4 lacks the UI, so use a DB update or a transition_status call); record the wall-clock seconds until the bot push arrives (PASS if ≤30 s) and confirm the push shows the D-10 client label + a deep-link button; confirm "Мои заявки" shows the new status + history.
      5. SC#4 — confirm RU/UZ toggle, Telegram theme adaptation, and the recorded bundle gzip size (from 03-05) ≤300 KB; note the first-paint observation on a throttled 3G profile.
      6. SC#5 — confirm /start greets with the Web App button and the status push routed via the notify queue/deliveries.
      Each item has a PASS/FAIL/recorded-value field. Mark the doc "deferred to deploy time" for the live wall-clock items (consistent with the Phase-2 acceptance deferral), with the automated proxies (Task 1) standing in for CI.
  </action>
  <verify>
    <automated>test -f docs/phase-03-acceptance.md && grep -q "6.1.1" docs/phase-03-acceptance.md && grep -q "notify" deploy/docker-compose.dev.yml && grep -q "minio" deploy/docker-compose.dev.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-03-acceptance.md` exists, cites TZ §6.1.1, and has a checklist item for each of the 5 ROADMAP Phase-3 success criteria with PASS/FAIL/recorded-value fields.
    - The doc records the ≤10 s and ≤30 s SLA checks as live-drill items deferred to deploy, with Task-1 automated proxies named as the CI stand-in.
    - docker-compose.dev.yml is confirmed to consume the `notify` queue and gate the api on a healthy minio (either already true and noted, or minimally fixed).
  </acceptance_criteria>
  <done>The acceptance doc maps the 5 success criteria to a deploy-time live drill with SLA recording fields; the compose stack is confirmed to support the circuit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live client-circuit drill sign-off (deploy-time)</name>
  <what-built>
    The automated SLA proxies pass and docs/phase-03-acceptance.md defines the live drill.
    This checkpoint is the human sign-off on the end-to-end client circuit against the 5
    ROADMAP success criteria. Like Phase-2 02-07, the full wall-clock live drill may be
    deferred to deploy time (when a real BOT_TOKEN + public HTTPS URL exist); the automated
    proxies cover CI in the meantime.
  </what-built>
  <action>
    This is a human gate — no autonomous code change. Present the verification steps in <how-to-verify>, pause for the human, and proceed only on the resume signal. Record the outcome in the plan SUMMARY.
  </action>
  <how-to-verify>
    Either run the drill now (if a real BOT_TOKEN + public PUBLIC_WEBAPP_URL are available) or
    approve deferral to deploy:
    1. `docker compose -f deploy/docker-compose.dev.yml up` with `.env` filled (real BOT_TOKEN, public HTTPS PUBLIC_WEBAPP_URL).
    2. Send /start to the bot → greeting + Web App button appears (RU or UZ by your language_code).
    3. Open the Web App, complete the wizard, submit → confirmation REQ number; verify it is queryable on the backend ≤10 s (record the value in docs/phase-03-acceptance.md).
    4. Attach a PDF/JPG → confirm it is in the MinIO bucket; a .txt/oversize file is rejected.
    5. Change the request status → confirm the bot push arrives ≤30 s with the D-10 label + deep-link, and "Мои заявки" reflects it.
    6. Record each PASS/FAIL/value in docs/phase-03-acceptance.md.
  </how-to-verify>
  <read_first>
    - docs/phase-03-acceptance.md (the checklist to fill in)
  </read_first>
  <verify>
    <human-check>Human completes the steps in <how-to-verify> and types the resume signal.</human-check>
  </verify>
  <acceptance_criteria>
    - Human either records live PASS results for the 5 success criteria in docs/phase-03-acceptance.md, OR approves deferral to deploy time (with the automated proxies + build gates standing as the phase gate), matching the Phase-2 deferral precedent.
    - Type "approved" (with any recorded values or an explicit deferral note).
  </acceptance_criteria>
  <done>Human verified the steps and approved (or recorded defects/deferral); the resume signal was given.</done>
  <resume-signal>Type "approved" (optionally noting recorded SLA values or "deferred to deploy"), or describe failures to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Telegram → webhook + initData | The drill exercises the real trust boundaries from 03-01/03-02/03-03 end-to-end under a real bot token |
| Web App → MinIO via backend | The drill confirms files persist only through the validated backend path, not directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Spoofing/Tampering | end-to-end auth + upload integrity under live conditions | mitigate | The live drill (Task 2/3) re-exercises initData verify, IDOR scoping, magic-byte upload, and webhook secret from 03-01..03-03 against a real bot — confirming the mitigations hold outside unit tests |
| T-03-22 | Repudiation | SLA non-compliance unrecorded | mitigate | docs/phase-03-acceptance.md records measured ≤10 s / ≤30 s values per TZ §6.1.1 (Task 2) |
| T-03-SC | Tampering | no new package installs in this plan | accept | This plan adds no dependencies — no install surface |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_request_sla.py -q` — SLA proxies green.
- `cd backend && python -m pytest -q` — full suite green.
- `test -f docs/phase-03-acceptance.md && grep -q "6.1.1" docs/phase-03-acceptance.md` — acceptance doc present + TZ-cited.
- Human sign-off (Task 3) records live results or an approved deferral.
</verification>

<success_criteria>
- Automated proxies prove synchronous request readback (≤10 s bound) and immediate notify dispatch (REQ-nfr-performance, SC#1/SC#3).
- The acceptance doc maps all 5 ROADMAP Phase-3 success criteria to a recorded live drill (REQ-request-wizard, REQ-my-requests, REQ-bot-clients).
- The compose stack supports the circuit (notify queue consumed, MinIO healthy-gated).
- Human sign-off recorded (live results or approved deploy-time deferral, per the Phase-2 precedent).
</success_criteria>

<output>
Create `.planning/phases/03-client-circuit/03-06-SUMMARY.md` when done.
</output>
