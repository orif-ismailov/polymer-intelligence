---
phase: 04-dashboard-source-constructor
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - dashboard/components/feed/LiveFeedTable.tsx
  - dashboard/components/feed/FeedFilters.tsx
  - dashboard/components/shared/KpiCard.tsx
  - dashboard/components/shared/StatusChip.tsx
  - dashboard/components/shared/UrgencyChip.tsx
  - dashboard/components/shared/KindChip.tsx
  - dashboard/components/feed/AiMarketSignalsPanel.tsx
  - dashboard/app/(dashboard)/page.tsx
  - dashboard/app/(dashboard)/signals/page.tsx
  - dashboard/app/(dashboard)/offers/page.tsx
autonomous: true
requirements: [REQ-live-feed]
user_setup: []

must_haves:
  truths:
    - "The Live Market Feed renders v_live_feed rows in a TanStack table with period/product/kind/source/urgency filters that re-query the backend"
    - "A new signal pushed over SSE makes the feed refresh without a page reload (invalidateQueries on SSE message); 30s polling fallback covers SSE failure (DEC-realtime-sse-not-websocket)"
    - "Dashboard home shows 5 KPI cards; the AI-dependent Hot Leads KPI and AI Market Signals panel render in final layout with honest placeholders (D-01)"
    - "All chips/cards use Tailwind token classes (no hardcoded hex); times display in Asia/Tashkent (DEC-tz-handling)"
  artifacts:
    - path: "dashboard/components/feed/LiveFeedTable.tsx"
      provides: "TanStack feed table consuming /feed with keyset pagination + SSE"
      contains: "useSSE"
    - path: "dashboard/components/feed/AiMarketSignalsPanel.tsx"
      provides: "D-01 placeholder-aware AI panel (final layout, no dead card)"
      contains: "after Phase 5"
    - path: "dashboard/app/(dashboard)/page.tsx"
      provides: "Dashboard home with KPI cards + feed + AI panels"
      contains: "KpiCard"
  key_links:
    - from: "dashboard/components/feed/LiveFeedTable.tsx"
      to: "/api/v1/feed"
      via: "useQuery(['feed', filters]) + useSSE invalidate"
      pattern: "\\['feed'"
    - from: "dashboard/components/feed/LiveFeedTable.tsx"
      to: "dashboard/hooks/useSSE.ts"
      via: "useSSE('/api/v1/feed/stream', invalidate)"
      pattern: "feed/stream"
---

<objective>
Build the Live Market Feed frontend (REQ-live-feed): a TanStack-Table feed with filters wired to
`GET /feed`, refreshed in real time by `useSSE` (invalidateQueries) with a 30s polling fallback,
plus the Dashboard home (5 KPI cards, feed panel, AI Market Signals placeholder panel D-01) and the
`/signals` + `/offers` list pages reusing the same table.

Purpose: REQ-live-feed (FR-10) end-to-end on the frontend — the team's primary read surface that
self-refreshes. D-01 placeholder contract keeps AI surfaces final-shaped now, data-filled in Phase 5.
Output: feed table + filters, shared chips/KPI, AI panel, home + signals + offers pages.
</objective>

<execution_context>
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/workflows/execute-plan.md
@/Users/kholmumin/WebstormProjects/polymer-intelligence/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-source-constructor/04-CONTEXT.md
@.planning/phases/04-dashboard-source-constructor/04-RESEARCH.md
@.planning/phases/04-dashboard-source-constructor/04-PATTERNS.md
@.planning/phases/04-dashboard-source-constructor/04-UI-SPEC.md
@.planning/phases/04-dashboard-source-constructor/04-01-SUMMARY.md
@.planning/phases/04-dashboard-source-constructor/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared chips + KPI card + feed table/filters with SSE</name>
  <files>dashboard/components/shared/KpiCard.tsx, dashboard/components/shared/StatusChip.tsx, dashboard/components/shared/UrgencyChip.tsx, dashboard/components/shared/KindChip.tsx, dashboard/components/feed/LiveFeedTable.tsx, dashboard/components/feed/FeedFilters.tsx</files>
  <read_first>
    - dashboard/hooks/useSSE.ts + dashboard/lib/api.ts + dashboard/lib/tz.ts + dashboard/lib/queryClient.ts (from 04-02)
    - 04-PATTERNS.md status/urgency/kind token-class maps (lines 618-671) — STATUS_CLASSES, chip tokens
    - 04-UI-SPEC.md Color table (status/urgency/kind tokens), Live Market Feed panel, KPI card anatomy, Requests table chip specs, Accessibility (icon+color, <time>)
    - 04-RESEARCH.md Pattern 2 TanStack Query integration (useQuery ['feed'] + invalidateQueries on SSE)
    - backend/app/schemas/dashboard.py (FeedItem/FeedPage shape from 04-01)
    - dashboard/package.json (@tanstack/react-table, @tanstack/react-query, lucide-react)
  </read_first>
  <action>
    Create shared chips using token classes only (no hex): `StatusChip` (maps RequestStatus →
    `text-status-* border-status-*` per UI-SPEC), `UrgencyChip` (icon + color: High=Flame, Medium=Users,
    Low=Download — color never alone per Accessibility), `KindChip` (BUYER/SELLER/REQUEST/DEAL kind tokens).
    Create `KpiCard` (icon 24px foreground-muted + 12px/600 label + 28px/600 value + delta chip).
    Create `FeedFilters`: Period select (7d/30d/90d), Product select, Kind select, Source select, Urgency
    select; filters persisted in URL search params (`useSearchParams`).
    Create `LiveFeedTable` (`"use client"`): `useQuery({ queryKey: ['feed', filters], queryFn })` calling
    `apiFetch('/feed?...')` with keyset cursor (Prev/Next using next_cursor_event_at/next_cursor_id);
    render with TanStack Table; wire `useSSE('/feed/stream', () => queryClient.invalidateQueries({ queryKey: ['feed'] }))`.
    Relative times via `relativeTime()` wrapped in `<time datetime={iso}>`. Empty state per UI-SPEC copy
    ("No market activity yet" / "Signals will appear here as sources report data.").
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `dashboard/components/feed/LiveFeedTable.tsx` contains `['feed'` query key, `useSSE`, and `feed/stream`.
    - Source: chips/KPI contain no hardcoded hex (`grep -rE "#[0-9a-fA-F]{6}" dashboard/components/shared/ dashboard/components/feed/` returns nothing).
    - Source: `UrgencyChip.tsx` renders an icon (lucide import) in addition to color (color-not-alone accessibility).
    - Test: `cd dashboard && npm run typecheck` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Feed table queries /feed, refreshes on SSE, filters persist in URL; chips/KPI token-only; typecheck+lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard home (KPIs + AI placeholder panels) + /signals + /offers pages</name>
  <files>dashboard/components/feed/AiMarketSignalsPanel.tsx, dashboard/app/(dashboard)/page.tsx, dashboard/app/(dashboard)/signals/page.tsx, dashboard/app/(dashboard)/offers/page.tsx</files>
  <read_first>
    - dashboard/components/feed/LiveFeedTable.tsx + KpiCard.tsx (from Task 1)
    - 04-UI-SPEC.md Dashboard Home (5 KPI cards incl. Hot Leads placeholder), AI Market Signals panel (D-01 — 3 placeholder rows, no spinner, no blank card), Signals/Offers screens
    - 04-CONTEXT.md D-01 (AI elements render final-shape with graceful placeholder; no hidden sections/dead cards)
    - 04-UI-SPEC.md Copywriting Contract (AI placeholder strings)
  </read_first>
  <action>
    Create `AiMarketSignalsPanel` (D-01): final-shape panel with 3 placeholder rows — icon + "AI analysis
    available after Phase 5" (`text-foreground-subtle`) — NOT a blank card and NOT a spinner.
    Build `app/(dashboard)/page.tsx`: KPI cards row (Total Buyers, Total Sellers, Active Requests, Hot Leads,
    Price Alerts). Hot Leads is AI-dependent: render final layout, numeric = real count of
    `requests WHERE lead_score IS NOT NULL` (0 in Phase 4) with "high priority" chip + Flame icon — no hidden
    card. Below: Live Market Feed panel (LiveFeedTable, compact), Price Trends panel placeholder ("View all"
    → /prices, chart wired in 04-08), AI Market Signals panel, Top Buyer Requests / Top Seller Offers panels.
    Create `app/(dashboard)/signals/page.tsx`: full-page LiveFeedTable; add a "Needs Review" filter chip
    rendered disabled with tooltip "Available after Phase 5 AI" (UI-SPEC). Create
    `app/(dashboard)/offers/page.tsx`: LiveFeedTable pre-filtered `kind=sell_offer`.
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `dashboard/components/feed/AiMarketSignalsPanel.tsx` contains the literal "after Phase 5" and is not a spinner-only/empty component (renders 3 placeholder rows).
    - Source: `dashboard/app/(dashboard)/page.tsx` imports and renders `KpiCard` and `LiveFeedTable` and contains a "Hot Leads" KPI rendered in final layout (not hidden).
    - Source: `/offers` page applies `kind=sell_offer`; `/signals` page renders the disabled "Needs Review" chip with the Phase 5 tooltip.
    - Test: `cd dashboard && npm run typecheck` exits 0, `npm run lint` exits 0, `npm run build` completes.
  </acceptance_criteria>
  <done>Home + signals + offers render; AI surfaces final-shaped with placeholders; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /feed, /feed/stream | Reads cross to backend (auth enforced server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering (XSS) | feed rows, filter values | mitigate | All feed text rendered as React text (escaped by default); no dangerouslySetInnerHTML; no hardcoded hex |
| T-04-09 | Info Disclosure | feed query | accept | Feed reads are JWT-guarded server-side (04-01); UI only renders what the API returns |
| T-04-SC | Tampering | npm installs | accept | No new packages — uses deps installed in 04-02; no [ASSUMED]/[SUS] additions |
</threat_model>

<verification>
- `cd dashboard && npm run typecheck && npm run lint && npm run build` green.
- Manual (deferred to 04-09 / deploy): SSE updates feed without reload; dark-theme visual smoke.
</verification>

<success_criteria>
- Live feed table filters + SSE refresh wired to /feed.
- Dashboard home + signals + offers render with token-only styling and D-01 placeholders.
</success_criteria>

## Artifacts this phase produces (Plan 03)

- Components: `LiveFeedTable`, `FeedFilters`, `AiMarketSignalsPanel`, `KpiCard`, `StatusChip`, `UrgencyChip`, `KindChip`
- Pages: `app/(dashboard)/page.tsx` (home, filled), `app/(dashboard)/signals/page.tsx`, `app/(dashboard)/offers/page.tsx`

<output>
Create `.planning/phases/04-dashboard-source-constructor/04-03-SUMMARY.md` when done.
</output>
